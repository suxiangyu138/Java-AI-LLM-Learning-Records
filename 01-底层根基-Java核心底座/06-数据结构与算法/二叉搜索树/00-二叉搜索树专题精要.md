# 二叉搜索树专题精要
> 中序有序的核心性质——BST 的验证、搜索、插入、删除四大操作与构造

## 📚 目录
1. [核心概念](#1-核心概念)
2. [完整代码模板](#2-完整代码模板)
3. [删除节点的三情况](#3-删除节点的三情况)
4. [BST 的进阶应用](#4-bst-的进阶应用)
5. [高频题目](#5-高频题目)
6. [面试要点与追问](#6-面试要点与追问)

---

## 1. 核心概念

### 1.1 BST 的定义

```
二叉搜索树（Binary Search Tree, BST）：
  左子树所有节点 < 根 < 右子树所有节点
  且左右子树自身也是 BST

     8
    / \
   3   10
  / \    \
 1   6    14
    / \   /
   4   7 13

关键推论：BST 中序遍历 = 从小到大有序输出！
```

### 1.2 识别信号

```
"有序数组 ↔ 树" → BST（108 转换）
"第 K 小/大" → BST（230 中序）
"验证有序结构" → BST（98）
"二叉搜索树" 关键词 → BST 专项

⚠️ BST 相关题目：
  核心是"中序有序"这条性质
  利用有序性简化问题
```

### 1.3 BST 操作复杂度

| 操作 | 平均 | 最坏（退化） | 实现 |
|------|:---:|:---:|------|
| 搜索 | O(log n) | O(n) | 二分式比较 |
| 插入 | O(log n) | O(n) | 二分式定位叶子 |
| 删除 | O(log n) | O(n) | 三情况分类 |
| 第 K 小 | O(n) | O(n) | 中序遍历 |

```
⚠️ 退化风险：插入有序序列 → 变链表 → O(n)
  平衡方案：AVL / 红黑树（Java TreeMap 用红黑树）
  → 面试必答："BST 退化成链表，平衡树保证 log n"
```

---

## 2. 完整代码模板

### 2.1 98. 验证 BST（区间约束）

```java
/**
 * LeetCode 98. 验证二叉搜索树
 * ⚠️ 只比较左右孩子是不够的！
 *   （左孩子的右子树里可能有大于根的值）
 *   → 必须区间约束或中序递增
 */
public boolean isValidBST(TreeNode root) {
    return validate(root, null, null);
}

private boolean validate(TreeNode node, Integer lo, Integer hi) {
    if (node == null) return true;

    // ⚠️ 区间约束：值必须在 (lo, hi) 内
    if (lo != null && node.val <= lo) return false;
    if (hi != null && node.val >= hi) return false;

    return validate(node.left, lo, node.val)     // 左子树：上界收紧
        && validate(node.right, node.val, hi);   // 右子树：下界收紧
}
// 反例：5→1→4→3→6（只看孩子误判合法）
// 区间法：6 在根 5 的右子树 → 必须 > 5 → 6 > 5 违反？不，6>5 合法
// 真正的反例：根 5，右子树有 3 → 3 必须 > 5 → 区间法捕获 ✓

// 解法二：中序递增（BST 中序必须严格递增）
public boolean isValidBST_Inorder(TreeNode root) {
    List<Integer> list = new ArrayList<>();
    inorder(root, list);
    for (int i = 1; i < list.size(); i++) {
        if (list.get(i) <= list.get(i - 1)) return false;
    }
    return true;
}
```

### 2.2 700/701. 搜索与插入

```java
/**
 * LeetCode 700. BST 中的搜索（二分式）
 */
public TreeNode searchBST(TreeNode root, int val) {
    if (root == null || root.val == val) return root;
    if (val < root.val) return searchBST(root.left, val);
    return searchBST(root.right, val);
}
// 迭代版：while (root != null && root.val != val)
//   root = val < root.val ? root.left : root.right;

/**
 * LeetCode 701. BST 中的插入
 * 新节点总是插到叶子位置（无需结构调整）
 */
public TreeNode insertIntoBST(TreeNode root, int val) {
    if (root == null) return new TreeNode(val);   // 找到插入位置
    if (val < root.val) {
        root.left = insertIntoBST(root.left, val);
    } else {
        root.right = insertIntoBST(root.right, val);
    }
    return root;
}
```

### 2.3 230. 第 K 小（中序计数）

```java
/**
 * LeetCode 230. 二叉搜索树中第 K 小的元素
 * 中序遍历天然有序 → 第 k 个访问到的就是答案
 */
public int kthSmallest(TreeNode root, int k) {
    int[] count = new int[]{0};
    int[] result = new int[1];
    inorder(root, k, count, result);
    return result[0];
}

private void inorder(TreeNode node, int k, int[] count, int[] result) {
    if (node == null || count[0] >= k) return;   // 剪枝

    inorder(node.left, k, count, result);
    if (++count[0] == k) {
        result[0] = node.val;                    // 第 k 个访问到
        return;
    }
    inorder(node.right, k, count, result);
}
// 进阶：频繁查询 → 节点加"左子树大小"字段 → O(log n)
```

---

## 3. 删除节点的三情况

### 3.1 450. 删除 BST 节点

```java
/**
 * LeetCode 450. 删除 BST 中的节点（面试高频）
 * 删除后仍必须是 BST。
 *
 * 三种情况：
 *   ① 叶子节点 → 直接删除（返回 null）
 *   ② 只有一个孩子 → 用孩子顶替（返回孩子）
 *   ③ 两个孩子 → 用右子树最小节点（中序后继）顶替：
 *        找到后继 → 复制值 → 删除右子树中的后继
 */
public TreeNode deleteNode(TreeNode root, int key) {
    if (root == null) return null;

    if (key < root.val) {
        root.left = deleteNode(root.left, key);       // 去左子树删
    } else if (key > root.val) {
        root.right = deleteNode(root.right, key);     // 去右子树删
    } else {
        // 找到要删除的节点 root

        // 情况 ②：只有一个孩子 → 返回孩子顶替
        if (root.left == null) return root.right;
        if (root.right == null) return root.left;

        // 情况 ③：两个孩子 → 找右子树最小值（中序后继）
        TreeNode successor = findMin(root.right);
        root.val = successor.val;                     // 复制值
        root.right = deleteNode(root.right, successor.val);  // 删除后继
    }
    return root;
}

private TreeNode findMin(TreeNode node) {
    while (node.left != null) node = node.left;   // 一路向左
    return node;
}
```

### 3.2 为什么用"后继"顶替

```
中序后继（右子树最小值）是唯一满足 BST 性质的替代者：
  - 比左子树所有节点大（来自右子树，右子树全比根大）
  - 比右子树其他节点小（它是右子树最小）
  → 用后继值顶替，BST 性质保持 ✓
  （也可以用左子树最大值，两种对称）

⚠️ 面试必答：
"删两个孩子的节点用中序后继顶替——
 它满足'大于左子树全部、小于右子树其余'，
 是唯一合法的替代值。"
```

### 3.3 删除的复杂度

```
搜索路径 O(h) + 递归删除 O(h) → O(h)
  平衡时 h = log n → O(log n)
  退化时 h = n → O(n)

⚠️ 与插入/搜索一致：
  所有 BST 操作都是 O(h)
  → 平衡性决定性能
```

---

## 4. BST 的进阶应用

### 4.1 有序数组转 BST（108）

```java
/**
 * LeetCode 108. 将有序数组转换为平衡二叉搜索树
 * 选中间元素为根 → 左右递归 → 自然平衡
 */
public TreeNode sortedArrayToBST(int[] nums) {
    return build(nums, 0, nums.length - 1);
}

private TreeNode build(int[] nums, int lo, int hi) {
    if (lo > hi) return null;
    int mid = lo + (hi - lo) / 2;          // 中间元素做根
    TreeNode root = new TreeNode(nums[mid]);
    root.left = build(nums, lo, mid - 1);
    root.right = build(nums, mid + 1, hi);
    return root;
}
// O(n) 时间 O(log n) 空间（递归栈）
// ⚠️ 为什么平衡？每次取中位 → 左右等大
```

### 4.2 前序+中序构造（105）

```java
/**
 * LeetCode 105. 从前序与中序遍历序列构造二叉树
 * 原理：
 *   ① 前序第一个元素 = 根
 *   ② 中序中找根 → 左边是左子树，右边是右子树
 *   ③ 递归构造
 * 优化：哈希表记录中序值→下标，O(1) 定位
 */
public TreeNode buildTree(int[] preorder, int[] inorder) {
    Map<Integer, Integer> idxMap = new HashMap<>();
    for (int i = 0; i < inorder.length; i++) {
        idxMap.put(inorder[i], i);           // 中序值 → 下标
    }
    return build(preorder, 0, preorder.length - 1,
                 inorder, 0, inorder.length - 1, idxMap);
}

private TreeNode build(int[] pre, int preLo, int preHi,
                       int[] in, int inLo, int inHi,
                       Map<Integer, Integer> idxMap) {
    if (preLo > preHi) return null;

    int rootVal = pre[preLo];                    // 前序第一个 = 根
    TreeNode root = new TreeNode(rootVal);
    int rootIdxIn = idxMap.get(rootVal);         // 中序中根的位置

    int leftSize = rootIdxIn - inLo;             // 左子树大小

    root.left = build(pre, preLo + 1, preLo + leftSize,
                      in, inLo, rootIdxIn - 1, idxMap);
    root.right = build(pre, preLo + leftSize + 1, preHi,
                       in, rootIdxIn + 1, inHi, idxMap);
    return root;
}
// ⚠️ 关键：leftSize = rootIdxIn - inLo 划分子树
```

### 4.3 538. 累加树（反向中序）

```java
/**
 * LeetCode 538. 把二叉搜索树转换为累加树
 * 每个节点的值 = 原树中大于等于它的值之和
 * 反向中序（右根左）→ 累积和
 */
public TreeNode convertBST(TreeNode root) {
    int[] sum = new int[1];
    reverseInorder(root, sum);
    return root;
}

private void reverseInorder(TreeNode node, int[] sum) {
    if (node == null) return;
    reverseInorder(node.right, sum);     // ⚠️ 先右（更大值）
    sum[0] += node.val;                  // 累积
    node.val = sum[0];                   // 更新
    reverseInorder(node.left, sum);
}
// 反向中序 = 降序输出（利用中序有序性质）
```

---

## 5. 高频题目

| 题号 | 题目 | 核心技巧 | 难度 |
|:---:|------|------|:---:|
| 98 | 验证 BST | 中序递增 / 区间约束 | 🟡 |
| 700 | BST 搜索 | 二分式比较 | 🟢 |
| 701 | BST 插入 | 找叶子位置 | 🟡 |
| 450 | BST 删除 | 三情况 + 后继顶替 | 🟡 |
| 230 | 第 K 小 | 中序计数 / size 优化 | 🟡 |
| 108 | 有序数组转 BST | 中间元素为根 | 🟢 |
| 105 | 前序+中序构造 | 哈希表 + 左子树大小 | 🟡 |
| 538 | 累加树 | 反向中序 | 🟡 |
| 501 | BST 众数 | 中序 + 计数 | 🟢 |
| 235 | BST 的 LCA | 二分式分叉 | 🟢 |

---

## 6. 面试要点与追问

### 6.1 面试话术模板

```
"BST 的一切源于中序有序。删除分三情况：
 叶子直接删、单孩子用孩子顶替、
 双孩子用中序后继顶替（复制值后递归删后继）。
 验证用区间约束（lo, hi）——只比较孩子
 会漏掉左孩子的右子树里的大值。
 复杂度 O(h)，平衡时 O(log n)。"
```

### 6.2 高频追问 TOP 8

| # | 追问 | 标准回答 |
|:---:|------|------|
| 1 | 为什么验证不能只比孩子？ | 左孩子的右子树可能有大于根的值 |
| 2 | 删除三情况？ | 叶子/单孩子/双孩子（后继顶替） |
| 3 | 为什么用后继？ | 唯一满足"大于左全部、小于右其余" |
| 4 | 退化风险？ | 有序插入变链表 → O(n)；平衡树保证 |
| 5 | 第 K 小优化？ | 节点存左子树大小 → O(log n) |
| 6 | 构造的核心？ | 根定位（哈希）+ leftSize 划分 |
| 7 | 中序的性质？ | 有序输出 → 验证/第 K/累加树 |
| 8 | 与 AVL/红黑树？ | BST 平衡化版本（TreeMap 用红黑树） |

### 6.3 常见错误

| 错误 | 后果 | 修正 |
|------|------|------|
| 验证只比孩子 | 漏判 | 区间约束 |
| 删除忘处理空 | 空指针 | 基线判断 |
| 双孩子直接删 | 丢结构 | 后继顶替 |
| leftSize 算错 | 构造错 | rootIdxIn - inLo |
| 反向中序方向反 | 累加错 | 先右后左 |

> 🎯 **核心要点**：BST = **中序有序**一条性质贯穿所有操作。验证（区间约束）、删除（三情况+后继）、构造（哈希+leftSize）是三大核心；"只比孩子会错"与"为什么用后继"是两个必答点。

---

**返回总览**：[00-算法汇总知识体系总览](../00-算法汇总知识体系总览.md) | **完整体系**：[树](../../树/00-树知识体系总览.md)
