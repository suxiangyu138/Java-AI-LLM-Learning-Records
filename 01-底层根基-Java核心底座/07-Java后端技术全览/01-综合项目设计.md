# 01 — 综合项目设计

> 项目是简历的核心，也是面试中最有说服力的部分。一个好的项目胜过烂熟的理论背诵。本文从选项目原则开始，到三个完整项目设计、部署上线、简历展示，一步步教你打造让人印象深刻的项目作品集。

---

## 目录

1. [项目选择原则](#一项目选择原则)
2. [Project 1: Spring Boot 单体博客系统](#二project-1-spring-boot-单体博客系统)
3. [Project 2: Spring Cloud 微服务商城](#三project-2-spring-cloud-微服务商城)
4. [Project 3: Java + AI 应用](#四project-3-java--ai-应用)
5. [项目 README 写作指南](#五项目-readme-写作指南)
6. [项目部署方案](#六项目部署方案)
7. [GitHub 主页优化](#七github-主页优化)
8. [项目面试话术](#八项目面试话术)

---

## 一、项目选择原则

### 1.1 三个核心原则

#### 原则 1: 简历价值

项目必须能为你的简历加分。问自己：

```
这个项目展示了我的什么能力？
- 后端开发能力？ → 需要：复杂业务逻辑、数据库设计、API 设计
- 架构设计能力？ → 需要：微服务拆分、分布式组件使用
- 工程化能力？ → 需要：测试、CI/CD、部署文档
- 新技术视野？ → 需要：AI 集成、云原生
```

#### 原则 2: 技术广度

项目应覆盖多个 Stage 的知识点，展示全面的技术栈。理想的项目覆盖：

```
Stage 1 Java 基础      → 集合、并发、IO、异常处理
Stage 2 数据库          → MySQL 设计、索引优化、Redis 缓存
Stage 3 Web 基础        → HTTP、RESTful API、Tomcat
Stage 5 SSM 框架        → Spring Boot、MyBatis、Spring Security
Stage 6 分布式与微服务  → Nacos、Gateway、Sentinel、Seata(可选)
Stage 7 AI 集成         → LLM API、RAG、向量数据库(可选)
```

#### 原则 3: 技术深度

项目中要有 2-3 个值得深入讲解的技术点：

```
不好的项目讲述：
"我用 Spring Boot 做了一个 CRUD" → 毫无亮点

好的项目讲述：
"在博客系统中，我使用了 Redis 做多级缓存，
解决了热点文章的缓存穿透问题，
还用了 Caffeine + Redis 双级缓存策略" → 有深度
```

### 1.2 项目选择决策树

```
你想展示什么能力？
├── 完整的 CRUD + 工程化能力
│   └── Project 1: 博客系统 (必做)
│
├── 微服务 + 分布式架构能力
│   └── Project 2: 商城系统 (推荐)
│
└── AI 集成 + 前沿技术探索
    └── Project 3: AI 应用 (差异化)

做得多不如做得精：1 个完整部署的项目 > 3 个半成品
```

### 1.3 项目分级

| 级别 | 要求 | 时间 | 适用 |
|------|------|------|------|
| L1: MVP | 核心功能可用，已部署 | 1-2 周 | 快速完善简历 |
| L2: 完善 | 功能完整，有文档，有测试 | 3-4 周 | 面试前准备好 |
| L3: 优秀 | 性能优化，架构优雅，有亮点 | 5-8 周 | 大厂面试加分 |

**建议策略**：先做 L1（快速上线），迭代到 L2，选 1-2 个方向做到 L3。

---

## 二、Project 1: Spring Boot 单体博客系统

### 2.1 项目概述

一个功能完善的博客/在线学习内容管理系统。这是 Java 开发者的「Hello World」级项目，但要做到「完整、漂亮、有深度」。

**核心价值**：
- 展示完整的 CRUD 能力
- 展示 Spring Boot 生态集成
- 展示数据库设计和缓存策略
- 展示安全认证和文件处理

### 2.2 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                     客户端层                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  Browser  │  │  Mobile  │  │  Postman  │  │   API    │   │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘  │  Client  │   │
│        │              │              │       └──────────┘   │
└────────┼──────────────┼──────────────┼──────────────────────┘
         │              │              │
┌────────┼──────────────┼──────────────┼──────────────────────┐
│   HTTP │  (Nginx)     │              │    负载均衡/反向代理  │
└────────┼──────────────┼──────────────┼──────────────────────┘
         │              │              │
┌────────┴──────────────┴──────────────┴──────────────────────┐
│                      API 网关层                               │
│  Spring Security Filter Chain                                │
│  ├── JWT Authentication Filter                              │
│  ├── Rate Limiting Filter                                   │
│  └── CORS Filter                                            │
└─────────────────────────────────────────────────────────────┘
         │                          │
┌────────┴──────────────────────────┴─────────────────────────┐
│                    业务逻辑层                                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐    │
│  │  Auth    │  │  Article  │  │  Comment  │  │  File    │    │
│  │ Service  │  │  Service  │  │  Service  │  │  Service │    │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘    │
│  ┌────┴─────┐  ┌────┴─────┐  ┌────┴─────┐  ┌────┴─────┐    │
│  │  User    │  │  Tag/Cat │  │  Like    │  │  Search  │    │
│  │ Service  │  │  Service │  │  Service │  │  Service │    │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘    │
└─────────────────────────────────────────────────────────────┘
         │                          │
┌────────┴──────────────────────────┴─────────────────────────┐
│                    数据访问层                                  │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────────────┐  │
│  │ MyBatis Mapper │  │  Redis       │  │  Elasticsearch   │  │
│  │ (MySQL)        │  │  (Cache)     │  │  (Search)        │  │
│  └─────────────┘  └──────────────┘  └───────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 技术栈

| 层次 | 技术 | 用途 |
|------|------|------|
| **后端框架** | Spring Boot 3.x | 应用框架 |
| **ORM** | MyBatis / MyBatis-Plus | 数据库访问 |
| **数据库** | MySQL 8.x | 关系型存储 |
| **缓存** | Redis 7.x | 缓存、分布式锁 |
| **安全** | Spring Security + JWT | 认证授权 |
| **API 文档** | SpringDoc OpenAPI | 接口文档 |
| **文件存储** | 本地 / OSS | 图片资源 |
| **搜索** | MySQL LIKE / Elasticsearch | 文章搜索 |
| **前端** | Thymeleaf / Vue 3 + Element Plus | 页面展示 |
| **部署** | Docker / Railway / 阿里云 | 上线运行 |

### 2.4 功能清单

#### MVP 核心功能（必做）

```
☑ 用户注册与登录（JWT 认证）
☑ 文章发布、编辑、删除
☑ 文章列表：分页、按分类/标签筛选
☑ 文章详情页
☑ 评论：发布、列表
☑ 基础搜索功能
```

#### 完善功能（推荐）

```
☑ 分类与标签管理
☑ 文章点赞/收藏
☑ 文件/图片上传
☑ Markdown 编辑器
☑ 文章阅读量统计（Redis）
☑ 热门文章排行
☑ 用户个人信息管理
☑ 文章封面图
```

#### 高级功能（加分）

```
☑ 角色权限管理（管理员/普通用户）
☑ 评论审核机制
☑  RSS 订阅
☑ 文章导出为 PDF
☑ 邮件通知
☑ 数据统计看板
☑ 搜索引擎集成（ES）
☑ 文章定时发布
```

### 2.5 数据库设计

#### ER 图（实体关系）

```
┌──────────────┐       ┌──────────────────┐
│    User      │       │    Article       │
│──────────────│       │──────────────────│
│ id (PK)      │──┐    │ id (PK)          │
│ username     │  │    │ title            │
│ password     │  │    │ content (TEXT)   │
│ email        │  │    │ summary          │
│ avatar       │  │    │ cover_image      │
│ bio          │  │    │ status           │
│ role         │  │    │ view_count       │
│ created_at   │  │    │ like_count       │
│ updated_at   │  │    │ author_id (FK)───┼──┐
└──────────────┘  │    │ category_id (FK) │  │
                  │    │ created_at       │  │
                  │    │ updated_at       │  │
                  │    └──────────────────┘  │
                  │                          │
┌──────────────┐  │    ┌──────────────────┐  │
│   Comment    │  │    │  Article_Tag     │  │
│──────────────│  │    │──────────────────│  │
│ id (PK)      │  │    │ article_id (FK)──┼──┘
│ content      │  │    │ tag_id (FK)      │
│ article_id   ├──┼────└──────────────────┘
│ (FK)         │  │
│ user_id (FK) │  │    ┌──────────────────┐
│ parent_id    │  │    │    Category      │
│ created_at   │  │    │──────────────────│
└──────────────┘  │    │ id (PK)          │
                  └────│ name             │
┌──────────────┐       │ description      │
│     Tag      │       │ parent_id        │
│──────────────│       │ sort_order       │
│ id (PK)      │       │ created_at       │
│ name         │       └──────────────────┘
│ created_at   │
└──────────────┘

┌──────────────┐       ┌──────────────────┐
│   Like       │       │    Favorite      │
│──────────────│       │──────────────────│
│ id (PK)      │       │ id (PK)          │
│ user_id (FK) │       │ user_id (FK)     │
│ article_id   │       │ article_id (FK)  │
│ (FK)         │       │ created_at       │
│ created_at   │       └──────────────────┘
└──────────────┘
```

#### 核心表 SQL 设计

```sql
-- 用户表
CREATE TABLE `user` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
    `username`    VARCHAR(50)  NOT NULL COMMENT '用户名',
    `password`    VARCHAR(100) NOT NULL COMMENT '密码(加密)',
    `email`       VARCHAR(100) DEFAULT NULL COMMENT '邮箱',
    `avatar`      VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
    `bio`         VARCHAR(500) DEFAULT NULL COMMENT '个人简介',
    `role`        VARCHAR(20)  NOT NULL DEFAULT 'user' COMMENT '角色: user/admin',
    `status`      TINYINT      NOT NULL DEFAULT 1 COMMENT '状态: 1正常 0禁用',
    `created_at`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_username` (`username`),
    UNIQUE KEY `uk_email` (`email`),
    KEY `idx_created_at` (`created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 文章表
CREATE TABLE `article` (
    `id`          BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
    `title`       VARCHAR(200) NOT NULL COMMENT '文章标题',
    `content`     LONGTEXT     NOT NULL COMMENT '文章内容(Markdown)',
    `summary`     VARCHAR(500) DEFAULT NULL COMMENT '文章摘要',
    `cover_image` VARCHAR(255) DEFAULT NULL COMMENT '封面图',
    `status`      TINYINT      NOT NULL DEFAULT 1 COMMENT '状态: 1草稿 2发布 3删除',
    `view_count`  INT          NOT NULL DEFAULT 0 COMMENT '阅读量',
    `like_count`  INT          NOT NULL DEFAULT 0 COMMENT '点赞数',
    `favorite_count` INT      NOT NULL DEFAULT 0 COMMENT '收藏数',
    `comment_count`  INT      NOT NULL DEFAULT 0 COMMENT '评论数',
    `is_top`      TINYINT      NOT NULL DEFAULT 0 COMMENT '是否置顶: 0否 1是',
    `author_id`   BIGINT       NOT NULL COMMENT '作者ID',
    `category_id` BIGINT       DEFAULT NULL COMMENT '分类ID',
    `created_at`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    `updated_at`  DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_author_id` (`author_id`),
    KEY `idx_category_id` (`category_id`),
    KEY `idx_status_created` (`status`, `created_at`),
    KEY `idx_view_count` (`view_count`),
    FULLTEXT KEY `ft_title_content` (`title`, `content`) WITH PARSER ngram
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章表';

-- 分类表
CREATE TABLE `category` (
    `id`          BIGINT      NOT NULL AUTO_INCREMENT COMMENT '主键',
    `name`        VARCHAR(50) NOT NULL COMMENT '分类名称',
    `description` VARCHAR(200) DEFAULT NULL COMMENT '分类描述',
    `parent_id`   BIGINT      DEFAULT NULL COMMENT '父分类ID',
    `sort_order`  INT         NOT NULL DEFAULT 0 COMMENT '排序',
    `created_at`  DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_name` (`name`),
    KEY `idx_parent_id` (`parent_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='分类表';

-- 标签表
CREATE TABLE `tag` (
    `id`         BIGINT      NOT NULL AUTO_INCREMENT COMMENT '主键',
    `name`       VARCHAR(50) NOT NULL COMMENT '标签名称',
    `created_at` DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_name` (`name`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='标签表';

-- 文章-标签关联表
CREATE TABLE `article_tag` (
    `id`         BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
    `article_id` BIGINT NOT NULL COMMENT '文章ID',
    `tag_id`     BIGINT NOT NULL COMMENT '标签ID',
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_article_tag` (`article_id`, `tag_id`),
    KEY `idx_tag_id` (`tag_id`),
    CONSTRAINT `fk_article_tag_article` FOREIGN KEY (`article_id`) REFERENCES `article` (`id`) ON DELETE CASCADE,
    CONSTRAINT `fk_article_tag_tag` FOREIGN KEY (`tag_id`) REFERENCES `tag` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='文章标签关联表';

-- 评论表
CREATE TABLE `comment` (
    `id`         BIGINT       NOT NULL AUTO_INCREMENT COMMENT '主键',
    `content`    TEXT         NOT NULL COMMENT '评论内容',
    `article_id` BIGINT       NOT NULL COMMENT '文章ID',
    `user_id`    BIGINT       NOT NULL COMMENT '评论用户ID',
    `parent_id`  BIGINT       DEFAULT NULL COMMENT '父评论ID(回复)',
    `status`     TINYINT      NOT NULL DEFAULT 1 COMMENT '状态: 1正常 0隐藏',
    `created_at` DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_article_id` (`article_id`),
    KEY `idx_user_id` (`user_id`),
    KEY `idx_parent_id` (`parent_id`),
    CONSTRAINT `fk_comment_article` FOREIGN KEY (`article_id`) REFERENCES `article` (`id`) ON DELETE CASCADE,
    CONSTRAINT `fk_comment_user` FOREIGN KEY (`user_id`) REFERENCES `user` (`id`) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='评论表';

-- 点赞表
CREATE TABLE `like_record` (
    `id`         BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
    `user_id`    BIGINT NOT NULL COMMENT '用户ID',
    `article_id` BIGINT NOT NULL COMMENT '文章ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_article` (`user_id`, `article_id`),
    KEY `idx_article_id` (`article_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='点赞记录表';

-- 收藏表
CREATE TABLE `favorite` (
    `id`         BIGINT NOT NULL AUTO_INCREMENT COMMENT '主键',
    `user_id`    BIGINT NOT NULL COMMENT '用户ID',
    `article_id` BIGINT NOT NULL COMMENT '文章ID',
    `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_user_article` (`user_id`, `article_id`),
    KEY `idx_article_id` (`article_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='收藏表';
```

### 2.6 API 设计

#### RESTful 接口规范

```
Base URL: http://localhost:8080/api/v1

响应格式:
{
    "code": 200,
    "message": "success",
    "data": { ... },
    "timestamp": 1700000000000
}

分页响应:
{
    "code": 200,
    "data": {
        "records": [ ... ],
        "total": 100,
        "page": 1,
        "size": 10,
        "pages": 10
    }
}
```

#### 核心接口列表

```
┌──────────────┬──────────────────────────────────────────────┬──────────────────────────┐
│ Method       │ Endpoint                                     │ Description              │
├──────────────┼──────────────────────────────────────────────┼──────────────────────────┤
│              │ ★ 认证模块 (Auth)                             │                          │
│ POST         │ /api/v1/auth/register                        │ 用户注册                 │
│ POST         │ /api/v1/auth/login                           │ 用户登录                 │
│ POST         │ /api/v1/auth/refresh                         │ 刷新 Token               │
│ POST         │ /api/v1/auth/logout                          │ 退出登录                 │
├──────────────┼──────────────────────────────────────────────┼──────────────────────────┤
│              │ ★ 用户模块 (User)                             │                          │
│ GET          │ /api/v1/users/{id}                           │ 获取用户信息             │
│ PUT          │ /api/v1/users/{id}                           │ 更新用户信息             │
│ PUT          │ /api/v1/users/{id}/password                  │ 修改密码                 │
├──────────────┼──────────────────────────────────────────────┼──────────────────────────┤
│              │ ★ 文章模块 (Article)                          │                          │
│ GET          │ /api/v1/articles                             │ 文章列表(分页)           │
│ GET          │ /api/v1/articles/{id}                        │ 文章详情                 │
│ POST         │ /api/v1/articles                             │ 创建文章                 │
│ PUT          │ /api/v1/articles/{id}                        │ 更新文章                 │
│ DELETE       │ /api/v1/articles/{id}                        │ 删除文章                 │
│ PUT          │ /api/v1/articles/{id}/status                 │ 更新文章状态             │
├──────────────┼──────────────────────────────────────────────┼──────────────────────────┤
│              │ ★ 分类模块 (Category)                         │                          │
│ GET          │ /api/v1/categories                           │ 分类列表                 │
│ POST         │ /api/v1/categories                           │ 创建分类                 │
│ PUT          │ /api/v1/categories/{id}                      │ 更新分类                 │
│ DELETE       │ /api/v1/categories/{id}                      │ 删除分类                 │
├──────────────┼──────────────────────────────────────────────┼──────────────────────────┤
│              │ ★ 标签模块 (Tag)                              │                          │
│ GET          │ /api/v1/tags                                 │ 标签列表                 │
│ POST         │ /api/v1/tags                                 │ 创建标签                 │
│ DELETE       │ /api/v1/tags/{id}                            │ 删除标签                 │
├──────────────┼──────────────────────────────────────────────┼──────────────────────────┤
│              │ ★ 评论模块 (Comment)                          │                          │
│ GET          │ /api/v1/articles/{id}/comments               │ 文章评论列表             │
│ POST         │ /api/v1/articles/{id}/comments               │ 发表评论                 │
│ DELETE       │ /api/v1/comments/{id}                        │ 删除评论                 │
├──────────────┼──────────────────────────────────────────────┼──────────────────────────┤
│              │ ★ 互动模块 (Interaction)                      │                          │
│ POST         │ /api/v1/articles/{id}/like                   │ 点赞/取消点赞            │
│ POST         │ /api/v1/articles/{id}/favorite               │ 收藏/取消收藏            │
│ GET          │ /api/v1/users/{id}/favorites                 │ 用户收藏列表             │
├──────────────┼──────────────────────────────────────────────┼──────────────────────────┤
│              │ ★ 搜索模块 (Search)                           │                          │
│ GET          │ /api/v1/search?q=keyword&page=1&size=10      │ 搜索文章                 │
├──────────────┼──────────────────────────────────────────────┼──────────────────────────┤
│              │ ★ 文件模块 (File)                             │                          │
│ POST         │ /api/v1/files/upload                         │ 上传文件                 │
│ GET          │ /api/v1/files/{id}                           │ 获取文件                 │
└──────────────┴──────────────────────────────────────────────┴──────────────────────────┘
```

### 2.7 关键技术实现

#### 2.7.1 Spring Security + JWT 认证

```java
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {

    private final JwtTokenProvider jwtTokenProvider;
    private final UserDetailsService userDetailsService;

    @Override
    protected void doFilterInternal(
            @NonNull HttpServletRequest request,
            @NonNull HttpServletResponse response,
            @NonNull FilterChain filterChain
    ) throws ServletException, IOException {
        // 1. 从请求头提取 Token
        String token = extractToken(request);
        if (token != null && jwtTokenProvider.validateToken(token)) {
            // 2. 解析用户信息
            String username = jwtTokenProvider.getUsernameFromToken(token);
            UserDetails userDetails = userDetailsService.loadUserByUsername(username);

            // 3. 设置 SecurityContext
            UsernamePasswordAuthenticationToken authentication =
                new UsernamePasswordAuthenticationToken(
                    userDetails, null, userDetails.getAuthorities()
                );
            authentication.setDetails(
                new WebAuthenticationDetailsSource().buildDetails(request)
            );
            SecurityContextHolder.getContext().setAuthentication(authentication);
        }
        filterChain.doFilter(request, response);
    }

    private String extractToken(HttpServletRequest request) {
        String bearerToken = request.getHeader("Authorization");
        if (bearerToken != null && bearerToken.startsWith("Bearer ")) {
            return bearerToken.substring(7);
        }
        return null;
    }
}
```

```java
// JWT 工具类
@Component
public class JwtTokenProvider {

    @Value("${jwt.secret}")
    private String jwtSecret;

    @Value("${jwt.expiration}")
    private long jwtExpiration;

    public String generateToken(UserDetails userDetails) {
        Date now = new Date();
        Date expiryDate = new Date(now.getTime() + jwtExpiration);

        return Jwts.builder()
                .setSubject(userDetails.getUsername())
                .claim("role", userDetails.getAuthorities())
                .setIssuedAt(now)
                .setExpiration(expiryDate)
                .signWith(getSigningKey(), SignatureAlgorithm.HS256)
                .compact();
    }

    public boolean validateToken(String token) {
        try {
            Jwts.parserBuilder()
                    .setSigningKey(getSigningKey())
                    .build()
                    .parseClaimsJws(token);
            return true;
        } catch (JwtException | IllegalArgumentException e) {
            return false;
        }
    }

    private Key getSigningKey() {
        byte[] keyBytes = Decoders.BASE64.decode(jwtSecret);
        return Keys.hmacShaKeyFor(keyBytes);
    }
}
```

#### 2.7.2 Redis 缓存策略

```java
@Service
public class ArticleCacheService {

    @Autowired
    private StringRedisTemplate redisTemplate;

    private static final String CACHE_KEY_PREFIX = "article:";
    private static final String HOT_RANK_KEY = "hot:articles";
    private static final long CACHE_TTL = 30; // 分钟
    private static final long HOT_CACHE_TTL = 5; // 热点缓存 TTL

    /**
     * 多级缓存策略：
     * 1. 先查 Redis
     * 2. Redis 没有则查 MySQL
     * 3. 数据回填 Redis（设置过期时间）
     * 4. 热点文章单独处理（短 TTL + 主动更新）
     */
    public ArticleVO getArticleById(Long articleId) {
        // 1. 先查 Redis 缓存
        String cacheKey = CACHE_KEY_PREFIX + articleId;
        String cachedJson = redisTemplate.opsForValue().get(cacheKey);

        if (cachedJson != null) {
            // 缓存命中
            ArticleVO article = JSON.parseObject(cachedJson, ArticleVO.class);
            // 异步增加阅读量
            incrementViewCountAsync(articleId);
            return article;
        }

        // 2. 缓存未命中，查数据库
        ArticleVO article = articleMapper.selectArticleVOById(articleId);
        if (article == null) {
            return null;
        }

        // 3. 判断是否是热点文章
        if (isHotArticle(articleId)) {
            // 热点文章：短 TTL，主动更新
            redisTemplate.opsForValue().set(
                cacheKey, JSON.toJSONString(article),
                HOT_CACHE_TTL, TimeUnit.MINUTES
            );
        } else {
            // 普通文章：较长 TTL
            redisTemplate.opsForValue().set(
                cacheKey, JSON.toJSONString(article),
                CACHE_TTL, TimeUnit.MINUTES
            );
        }

        return article;
    }

    /**
     * 热点文章判断：阅读量 > 1000 或 近期评论多
     */
    private boolean isHotArticle(Long articleId) {
        Double score = redisTemplate.opsForZSet().score(HOT_RANK_KEY,
            String.valueOf(articleId));
        return score != null && score > 1000;
    }

    /**
     * 文章更新时：删除缓存
     */
    public void evictCache(Long articleId) {
        String cacheKey = CACHE_KEY_PREFIX + articleId;
        redisTemplate.delete(cacheKey);
    }

    /**
     * 缓存穿透防护：布隆过滤器 + 空值缓存
     */
    public ArticleVO safeGetArticleById(Long articleId) {
        // 1. 布隆过滤器检查
        if (!bloomFilter.mightContain(articleId)) {
            return null; // 一定不存在
        }

        // 2. 检查是否是空值缓存（防止穿透）
        String cacheKey = CACHE_KEY_PREFIX + articleId;
        String cached = redisTemplate.opsForValue().get(cacheKey);
        if (cached != null) {
            if ("NULL".equals(cached)) {
                return null; // 空值缓存命中
            }
            return JSON.parseObject(cached, ArticleVO.class);
        }

        // 3. 查数据库
        ArticleVO article = articleMapper.selectArticleVOById(articleId);
        if (article == null) {
            // 设置空值缓存（短 TTL）
            redisTemplate.opsForValue().set(
                cacheKey, "NULL", 5, TimeUnit.MINUTES
            );
            return null;
        }

        // 4. 回填缓存
        redisTemplate.opsForValue().set(
            cacheKey, JSON.toJSONString(article),
            CACHE_TTL, TimeUnit.MINUTES
        );

        return article;
    }
}
```

#### 2.7.3 分页查询

```java
/**
 * 使用 MyBatis-Plus 分页插件
 */
@Configuration
public class MyBatisPlusConfig {
    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();
        interceptor.addInnerInterceptor(
            new PaginationInnerInterceptor(DbType.MYSQL)
        );
        return interceptor;
    }
}

// 文章分页查询 Service
@Service
public class ArticleService {

    public PageResult<ArticleVO> listArticles(ArticleQuery query) {
        // 构建分页对象
        Page<Article> page = new Page<>(query.getPage(), query.getSize());

        // 构建查询条件
        LambdaQueryWrapper<Article> wrapper = new LambdaQueryWrapper<>();
        wrapper.eq(Article::getStatus, ArticleStatus.PUBLISHED);

        // 按分类筛选
        if (query.getCategoryId() != null) {
            wrapper.eq(Article::getCategoryId, query.getCategoryId());
        }

        // 按标签筛选（关联表查询需要特殊处理）
        if (query.getTagId() != null) {
            // 使用 EXISTS 或 IN 子查询
            wrapper.inSql(Article::getId,
                "SELECT article_id FROM article_tag WHERE tag_id = "
                    + query.getTagId());
        }

        // 按关键词搜索
        if (StringUtils.hasText(query.getKeyword())) {
            wrapper.and(w -> w
                .like(Article::getTitle, query.getKeyword())
                .or()
                .like(Article::getContent, query.getKeyword())
            );
        }

        // 排序策略
        switch (query.getSortBy()) {
            case "hot":
                wrapper.orderByDesc(Article::getViewCount);
                break;
            case "newest":
                wrapper.orderByDesc(Article::getCreatedAt);
                break;
            case "trending":
                // 按最近 7 天的评论数 + 点赞数排序
                wrapper.last("ORDER BY (like_count + comment_count * 2) DESC");
                break;
            default:
                wrapper.orderByDesc(Article::getIsTop)
                       .orderByDesc(Article::getCreatedAt);
        }

        // 执行分页查询
        Page<Article> result = articleMapper.selectPage(page, wrapper);

        // 转换为 VO
        List<ArticleVO> records = result.getRecords().stream()
            .map(this::convertToVO)
            .collect(Collectors.toList());

        return PageResult.of(records, result.getTotal(),
            result.getCurrent(), result.getSize());
    }
}
```

#### 2.7.4 文件上传

```java
@RestController
@RequestMapping("/api/v1/files")
public class FileController {

    @Autowired
    private FileService fileService;

    @PostMapping("/upload")
    public Result<String> uploadFile(
            @RequestParam("file") MultipartFile file,
            @RequestParam(value = "type", defaultValue = "image") String type
    ) {
        // 文件校验
        if (file.isEmpty()) {
            return Result.error("文件不能为空");
        }

        // 检查文件类型
        String originalFilename = file.getOriginalFilename();
        String extension = originalFilename.substring(
            originalFilename.lastIndexOf("."));
        Set<String> allowedExtensions = Set.of(
            ".jpg", ".jpeg", ".png", ".gif", ".webp", ".bmp");
        if (!allowedExtensions.contains(extension.toLowerCase())) {
            return Result.error("不支持的文件格式");
        }

        // 检查文件大小（最大 5MB）
        if (file.getSize() > 5 * 1024 * 1024) {
            return Result.error("文件大小不能超过 5MB");
        }

        // 上传文件
        String fileUrl = fileService.upload(file, type);
        return Result.success(fileUrl);
    }
}

@Service
public class FileService {

    @Value("${file.upload.path:/data/uploads}")
    private String uploadPath;

    @Value("${file.access.base-url:http://localhost:8080}")
    private String baseUrl;

    public String upload(MultipartFile file, String type) {
        try {
            // 1. 生成唯一文件名
            String originalFilename = file.getOriginalFilename();
            String extension = originalFilename.substring(
                originalFilename.lastIndexOf("."));
            String newFilename = UUID.randomUUID().toString()
                + extension;

            // 2. 按日期组织目录
            String datePath = LocalDate.now()
                .format(DateTimeFormatter.ofPattern("yyyy/MM/dd"));
            String fullPath = uploadPath + "/" + type + "/" + datePath;

            // 3. 创建目录
            File directory = new File(fullPath);
            if (!directory.exists()) {
                directory.mkdirs();
            }

            // 4. 保存文件
            File destFile = new File(fullPath, newFilename);
            file.transferTo(destFile);

            // 5. 返回可访问的 URL
            return baseUrl + "/uploads/" + type + "/"
                + datePath + "/" + newFilename;

        } catch (IOException e) {
            throw new FileUploadException("文件上传失败", e);
        }
    }
}
```

### 2.8 项目结构

```
blog-system/
├── src/main/java/com/blog/
│   ├── BlogApplication.java
│   ├── config/
│   │   ├── SecurityConfig.java          # Spring Security 配置
│   │   ├── RedisConfig.java             # Redis 配置
│   │   ├── WebMvcConfig.java            # MVC 配置（CORS等）
│   │   ├── MyBatisPlusConfig.java       # MyBatis-Plus 配置
│   │   └── OpenApiConfig.java           # API 文档配置
│   ├── common/
│   │   ├── exception/
│   │   │   ├── GlobalExceptionHandler.java   # 全局异常处理
│   │   │   ├── BusinessException.java        # 业务异常
│   │   │   └── ErrorCode.java                # 错误码枚举
│   │   ├── result/
│   │   │   ├── Result.java               # 统一响应
│   │   │   └── PageResult.java           # 分页响应
│   │   ├── constant/
│   │   │   └── CacheConstants.java       # 缓存常量
│   │   └── util/
│   │       ├── JwtTokenProvider.java     # JWT 工具
│   │       └── BeanUtils.java            # Bean 工具
│   ├── auth/
│   │   ├── JwtAuthenticationFilter.java  # JWT 过滤器
│   │   ├── LoginUser.java                # 登录用户信息
│   │   └── CurrentUser.java              # 当前用户注解
│   ├── modules/
│   │   ├── user/
│   │   │   ├── entity/User.java
│   │   │   ├── mapper/UserMapper.java
│   │   │   ├── service/UserService.java
│   │   │   ├── controller/UserController.java
│   │   │   ├── dto/
│   │   │   │   ├── UserRegisterDTO.java
│   │   │   │   ├── UserLoginDTO.java
│   │   │   │   └── UserUpdateDTO.java
│   │   │   └── vo/UserVO.java
│   │   ├── article/
│   │   │   ├── entity/Article.java
│   │   │   ├── mapper/ArticleMapper.java
│   │   │   ├── service/ArticleService.java
│   │   │   ├── service/ArticleCacheService.java
│   │   │   ├── controller/ArticleController.java
│   │   │   ├── dto/
│   │   │   │   ├── ArticleCreateDTO.java
│   │   │   │   ├── ArticleUpdateDTO.java
│   │   │   │   └── ArticleQuery.java
│   │   │   └── vo/ArticleVO.java
│   │   ├── category/
│   │   │   ├── entity/Category.java
│   │   │   ├── mapper/CategoryMapper.java
│   │   │   ├── service/CategoryService.java
│   │   │   └── controller/CategoryController.java
│   │   ├── tag/
│   │   │   ├── entity/Tag.java
│   │   │   ├── mapper/TagMapper.java
│   │   │   ├── service/TagService.java
│   │   │   └── controller/TagController.java
│   │   ├── comment/
│   │   │   ├── entity/Comment.java
│   │   │   ├── mapper/CommentMapper.java
│   │   │   ├── service/CommentService.java
│   │   │   └── controller/CommentController.java
│   │   ├── like/
│   │   │   ├── entity/LikeRecord.java
│   │   │   ├── service/LikeService.java
│   │   │   └── controller/LikeController.java
│   │   └── file/
│   │       ├── service/FileService.java
│   │       └── controller/FileController.java
│   └── search/
│       └── service/SearchService.java
├── src/main/resources/
│   ├── application.yml
│   ├── application-dev.yml
│   ├── application-prod.yml
│   └── mapper/                    # MyBatis XML 映射文件
│       ├── UserMapper.xml
│       ├── ArticleMapper.xml
│       └── ...
├── sql/
│   └── init.sql                   # 建表脚本
├── docker-compose.yml             # 本地开发环境
├── Dockerfile                     # 应用容器化
├── .gitignore
├── pom.xml
└── README.md
```

---

## 三、Project 2: Spring Cloud 微服务商城

### 3.1 项目概述

一个基于微服务架构的在线商城系统。这是展示你分布式系统能力的关键项目。

**核心价值**：
- 展示微服务架构设计能力
- 展示分布式事务处理
- 展示服务治理和服务网格
- 展示消息队列集成

### 3.2 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                          客户端层                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  Browser  │  │  Mobile  │  │  Postman  │  │   Web    │       │
│  └──────────┘  └──────────┘  └──────────┘  │  Client  │       │
│                                             └──────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────┼───────────────────────────────────┐
│                       负载均衡                                 │
│                       Nginx / SLB                              │
└─────────────────────────────┼───────────────────────────────────┘
                              │
┌─────────────────────────────┼───────────────────────────────────┐
│                    ┌────────┴────────┐                          │
│                    │    Gateway      │ ◄── Sentinel 限流        │
│                    │  (Spring Cloud  │                          │
│                    │   Gateway)      │                          │
│                    └────────┬────────┘                          │
│                             │                                   │
│        ┌────────────────────┼────────────────────┐              │
│        │                    │                    │              │
│  ┌─────┴──────┐      ┌─────┴──────┐      ┌─────┴──────┐      │
│  │   User     │      │  Product   │      │   Order    │      │
│  │  Service   │      │  Service   │      │  Service   │      │
│  └─────┬──────┘      └─────┬──────┘      └─────┬──────┘      │
│        │                    │                    │              │
│  ┌─────┴──────┐      ┌─────┴──────┐      ┌─────┴──────┐      │
│  │  MySQL     │      │  MySQL     │      │  MySQL     │      │
│  │  Redis     │      │  Redis     │      │  Redis     │      │
│  └────────────┘      └────────────┘      └────────────┘      │
│                             │                    │              │
│                      ┌─────┴──────┐      ┌─────┴──────┐      │
│                      │ Inventory  │      │ Payment    │      │
│                      │  Service   │      │  Service   │      │
│                      └─────┬──────┘      └─────┬──────┘      │
│                            │                    │              │
│                      ┌─────┴──────┐      ┌─────┴──────┐      │
│                      │  MySQL     │      │  (第三方)  │      │
│                      │  Redis     │      │ 支付网关   │      │
│                      └────────────┘      └────────────┘      │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              基础设施层                                   │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────┐ │   │
│  │  │  Nacos   │  │ Sentinel │  │  Seata   │  │RocketMQ│ │   │
│  │  │ 注册中心 │  │ 熔断限流 │  │ 分布式   │  │ 消息   │ │   │
│  │  │ 配置中心 │  │          │  │ 事务     │  │ 队列   │ │   │
│  │  └──────────┘  └──────────┘  └──────────┘  └────────┘ │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 服务拆分

| 服务 | 职责 | 数据库 | 端口 |
|------|------|--------|------|
| **gateway** | 网关路由、统一认证、限流 | - | 8080 |
| **user-service** | 用户注册、登录、信息管理 | user_db | 8081 |
| **product-service** | 商品 CRUD、分类、搜索、详情 | product_db | 8082 |
| **order-service** | 订单创建、查询、状态流转 | order_db | 8083 |
| **inventory-service** | 库存管理、库存扣减 | inventory_db | 8084 |
| **payment-service** | 支付接口、支付回调 | payment_db | 8085 |

### 3.3 技术栈

| 层次 | 技术 | 用途 |
|------|------|------|
| **注册/配置中心** | Nacos | 服务发现、配置管理 |
| **网关** | Spring Cloud Gateway | 路由、鉴权、限流 |
| **远程调用** | OpenFeign | 服务间调用 |
| **熔断/限流** | Sentinel | 流量控制、熔断降级 |
| **分布式事务** | Seata (AT模式) | 跨服务事务一致性 |
| **消息队列** | RocketMQ | 异步解耦、削峰填谷 |
| **缓存** | Redis | 商品缓存、分布式锁 |
| **数据库** | MySQL 8.x | 持久化存储 |
| **容器化** | Docker / Docker Compose | 本地开发部署 |
| **API 文档** | SpringDoc OpenAPI | 接口文档聚合 |

### 3.4 业务流程

```
用户注册登录 → 浏览商品 → 添加购物车 → 下单 → 扣库存 → 支付
    │            │           │          │       │       │
    │            │           │          │       │       └─ payment-service
    │            │           │          │       │           │
    │            │           │          │       │   支付宝/微信回调
    │            │           │          │       │           │
    │            │           │          │  inventory-        │
    │            │           │          │  service          │
    │            │           │          │  (Seata全局       │
    │            │           │          │   事务)           │
    │            │           │          │                   │
    │            │           │          └── order-service    │
    │            │           │              (状态: 待支付)   │
    │            │           │                               │
    │            │           └── cart-service (Redis)        │
    │            │                                           │
    │            └── product-service                         │
    │                (Redis缓存)                             │
    │                                                        │
    └── user-service                                         │
        (JWT 认证)                                          │
```

### 3.5 关键技术实现

#### 3.5.1 分布式事务（Seata AT 模式）

下单流程涉及 order-service 和 inventory-service 两个服务，需要保证事务一致性。

```java
/**
 * 订单服务 - 创建订单（Seata 分布式事务）
 */
@Service
public class OrderService {

    @Autowired
    private OrderMapper orderMapper;
    @Autowired
    private InventoryClient inventoryClient;
    @Autowired
    private OrderItemMapper orderItemMapper;

    /**
     * Seata @GlobalTransactional 注解开启全局事务
     * 所有参与的服务都会在同一个全局事务中
     */
    @GlobalTransactional(name = "create-order", rollbackFor = Exception.class)
    public OrderVO createOrder(OrderCreateDTO dto) {
        // 1. 创建订单（本地事务）
        Order order = new Order();
        order.setUserId(dto.getUserId());
        order.setTotalAmount(dto.getTotalAmount());
        order.setStatus(OrderStatus.PENDING_PAYMENT);
        order.setOrderNo(generateOrderNo());
        orderMapper.insert(order);

        // 2. 创建订单项
        for (OrderItemDTO item : dto.getItems()) {
            OrderItem orderItem = new OrderItem();
            orderItem.setOrderId(order.getId());
            orderItem.setProductId(item.getProductId());
            orderItem.setQuantity(item.getQuantity());
            orderItem.setPrice(item.getPrice());
            orderItemMapper.insert(orderItem);
        }

        // 3. 调用库存服务扣减库存（远程调用，参与全局事务）
        InventoryDeductDTO deductDTO = new InventoryDeductDTO();
        deductDTO.setOrderId(order.getId());
        deductDTO.setItems(dto.getItems());
        Result result = inventoryClient.deductStock(deductDTO);
        if (!result.isSuccess()) {
            throw new BusinessException("库存不足");
        }

        // 4. 发送订单创建消息（异步通知其他服务）
        rocketMQTemplate.sendMessageInTransaction(
            "order-topic",
            MessageBuilder.withPayload(order).build(),
            null
        );

        return convertToVO(order);
    }
}

/**
 * 库存服务 - 扣减库存
 */
@Service
public class InventoryService {

    @Autowired
    private InventoryMapper inventoryMapper;

    /**
     * 使用分布式锁防止超卖
     */
    public boolean deductStock(InventoryDeductDTO dto) {
        for (InventoryItemDTO item : dto.getItems()) {
            // 使用 Redis 分布式锁
            String lockKey = "lock:inventory:" + item.getProductId();
            RLock lock = redissonClient.getLock(lockKey);
            try {
                // 尝试加锁，等待 3 秒，锁过期 10 秒
                if (lock.tryLock(3, 10, TimeUnit.SECONDS)) {
                    // 扣减库存
                    int affected = inventoryMapper.deductStock(
                        item.getProductId(),
                        item.getQuantity()
                    );
                    if (affected == 0) {
                        throw new BusinessException(
                            "商品 " + item.getProductId() + " 库存不足");
                    }
                } else {
                    throw new BusinessException("获取锁失败，请重试");
                }
            } catch (InterruptedException e) {
                Thread.currentThread().interrupt();
                throw new BusinessException("系统繁忙，请稍后重试");
            } finally {
                lock.unlock();
            }
        }
        return true;
    }
}
```

#### 3.5.2 Gateway 网关配置

```yaml
# gateway application.yml
spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/v1/users/**
          filters:
            - StripPrefix=1
            - name: RequestRateLimiter
              args:
                key-resolver: "#{@userKeyResolver}"
                redis-rate-limiter.replenishRate: 100
                redis-rate-limiter.burstCapacity: 200

        - id: product-service
          uri: lb://product-service
          predicates:
            - Path=/api/v1/products/**
          filters:
            - StripPrefix=1
            - name: RequestRateLimiter
              args:
                key-resolver: "#{@ipKeyResolver}"
                redis-rate-limiter.replenishRate: 200
                redis-rate-limiter.burstCapacity: 400

        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/api/v1/orders/**
          filters:
            - StripPrefix=1
            - name: RequestRateLimiter
              args:
                key-resolver: "#{@userKeyResolver}"
                redis-rate-limiter.replenishRate: 50
                redis-rate-limiter.burstCapacity: 100

      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin
```

#### 3.5.3 Sentinel 限流配置

```java
@Configuration
public class SentinelConfig {

    @Bean
    public SentinelResourceAspect sentinelResourceAspect() {
        return new SentinelResourceAspect();
    }

    /**
     * 热点参数限流
     */
    @PostConstruct
    public void initFlowRules() {
        List<FlowRule> rules = new ArrayList<>();

        // 1. 商品详情限流：每个商品 ID 每秒最多 100 次
        FlowRule productDetailRule = new FlowRule();
        productDetailRule.setResource("getProductDetail");
        productDetailRule.setGrade(RuleConstant.FLOW_GRADE_QPS);
        productDetailRule.setCount(100);
        // 热点参数限流
        ParamFlowRule paramRule = new ParamFlowRule("getProductDetail")
            .setParamIdx(0)  // 对第一个参数（商品ID）限流
            .setCount(10)    // 每个商品 ID 每秒 10 次
            .setDurationInSec(1);
        FlowRuleManager.loadRules(rules);

        // 2. 创建订单限流
        FlowRule createOrderRule = new FlowRule();
        createOrderRule.setResource("createOrder");
        createOrderRule.setGrade(RuleConstant.FLOW_GRADE_QPS);
        createOrderRule.setCount(20);
        rules.add(createOrderRule);

        FlowRuleManager.loadRules(rules);
    }
}

// 在业务代码中使用
@Service
public class ProductService {

    @SentinelResource(
        value = "getProductDetail",
        blockHandler = "handleBlock",
        fallback = "handleFallback"
    )
    public ProductVO getProductDetail(Long productId) {
        Product product = productMapper.selectById(productId);
        if (product == null) {
            throw new BusinessException("商品不存在");
        }
        return convertToVO(product);
    }

    /**
     * 限流后的处理（返回兜底数据）
     */
    public ProductVO handleBlock(Long productId, BlockException e) {
        // 返回缓存中的商品信息，可能不是最新的
        ProductVO cached = cacheService.getProductCache(productId);
        if (cached != null) {
            return cached;
        }
        // 如果缓存也没有，返回基本信息
        return ProductVO.builder()
            .id(productId)
            .name("商品暂时无法查看")
            .status(0)
            .build();
    }

    /**
     * 熔断降级处理
     */
    public ProductVO handleFallback(Long productId, Throwable e) {
        return ProductVO.builder()
            .id(productId)
            .name("服务暂不可用")
            .build();
    }
}
```

#### 3.5.4 RocketMQ 异步订单处理

```java
// 发送异步消息
@Service
public class OrderService {

    @Autowired
    private RocketMQTemplate rocketMQTemplate;

    /**
     * 订单创建成功后，发送异步消息
     * - 通知库存服务锁定库存
     * - 通知物流服务准备配送
     * - 通知用户服务发送通知
     */
    public void sendOrderCreatedEvent(Order order) {
        OrderCreatedEvent event = new OrderCreatedEvent();
        event.setOrderId(order.getId());
        event.setOrderNo(order.getOrderNo());
        event.setUserId(order.getUserId());
        event.setTotalAmount(order.getTotalAmount());
        event.setCreatedAt(LocalDateTime.now());

        // 发送事务消息
        rocketMQTemplate.sendMessageInTransaction(
            "order-topic",
            MessageBuilder.withPayload(event)
                .setHeader("event_type", "ORDER_CREATED")
                .build(),
            order
        );
    }
}

// 消费者处理
@Component
@RocketMQMessageListener(
    topic = "order-topic",
    consumerGroup = "order-consumer-group",
    selectorExpression = "ORDER_CREATED"
)
public class OrderCreatedConsumer
    implements RocketMQListener<OrderCreatedEvent> {

    @Autowired
    private InventoryService inventoryService;
    @Autowired
    private NotificationService notificationService;

    @Override
    public void onMessage(OrderCreatedEvent event) {
        log.info("收到订单创建消息: {}", event.getOrderNo());

        try {
            // 1. 锁定库存
            inventoryService.lockInventory(
                event.getOrderId(), event.getItems());

            // 2. 发送通知（异步）
            notificationService.sendOrderNotification(event);

            log.info("订单 {} 异步处理完成", event.getOrderNo());
        } catch (Exception e) {
            log.error("订单 {} 异步处理失败", event.getOrderNo(), e);
            // 记录失败消息，后续人工处理
            // 或者发送到死信队列
        }
    }
}
```

#### 3.5.5 Docker Compose 本地开发

```yaml
# docker-compose.yml
version: '3.8'

services:
  # 注册中心
  nacos:
    image: nacos/nacos-server:2.2.0
    container_name: mall-nacos
    environment:
      MODE: standalone
      PREFER_HOST_MODE: hostname
    ports:
      - "8848:8848"
      - "9848:9848"

  # 消息队列
  rocketmq-namesrv:
    image: rocketmqinc/rocketmq:4.9.4
    container_name: rmq-namesrv
    command: sh mqnamesrv
    ports:
      - "9876:9876"

  rocketmq-broker:
    image: rocketmqinc/rocketmq:4.9.4
    container_name: rmq-broker
    command: sh mqbroker -n localhost:9876
    depends_on:
      - rocketmq-namesrv
    ports:
      - "10909:10909"
      - "10911:10911"
    volumes:
      - ./docker/rocketmq/broker.conf:/etc/rocketmq/broker.conf

  # 数据库
  mysql-user:
    image: mysql:8.0
    container_name: mall-mysql-user
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: user_db
    ports:
      - "3307:3306"

  mysql-product:
    image: mysql:8.0
    container_name: mall-mysql-product
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: product_db
    ports:
      - "3308:3306"

  mysql-order:
    image: mysql:8.0
    container_name: mall-mysql-order
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: order_db
    ports:
      - "3309:3306"

  # 缓存
  redis:
    image: redis:7-alpine
    container_name: mall-redis
    ports:
      - "6379:6379"

  # 微服务
  user-service:
    build: ./user-service
    container_name: mall-user-service
    depends_on:
      - nacos
      - mysql-user
      - redis
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
      - "8081:8081"

  product-service:
    build: ./product-service
    container_name: mall-product-service
    depends_on:
      - nacos
      - mysql-product
      - redis
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
      - "8082:8082"

  order-service:
    build: ./order-service
    container_name: mall-order-service
    depends_on:
      - nacos
      - mysql-order
      - redis
      - rocketmq-broker
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
      - "8083:8083"

  gateway:
    build: ./gateway
    container_name: mall-gateway
    depends_on:
      - nacos
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
      - "8080:8080"
```

## 四、Project 3: Java + AI 应用

### 4.1 项目概述

一个集成大语言模型（LLM）的智能知识问答系统。这是你与其他 Java 开发者拉开差距的关键项目。

**核心价值**：
- 展示 AI 集成能力（Java 开发者稀缺技能）
- 展示 RAG（检索增强生成）技术栈
- 展示向量数据库和语义搜索
- 展示全栈 AI 应用开发

### 4.2 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                      前端层                                   │
│  ┌──────────────────┐  ┌──────────────────┐                │
│  │  Chat Interface  │  │  Document Upload │                │
│  │  (Vue / React)   │  │  (Vue / React)   │                │
│  └────────┬─────────┘  └────────┬─────────┘                │
│           │                     │                          │
│       SSE/WebSocket          Multipart Upload              │
└───────────┼─────────────────────┼──────────────────────────┘
            │                     │
┌───────────┼─────────────────────┼──────────────────────────┐
│     ┌─────┴─────────────────────┴─────┐                    │
│     │          Spring Boot 后端        │                    │
│     │                                  │                    │
│     │  ┌──────────────────────────┐  │                    │
│     │  │   AI Service Layer       │  │                    │
│     │  │  ├── ChatService         │  │                    │
│     │  │  ├── EmbeddingService    │  │                    │
│     │  │  ├── RAGService          │  │                    │
│     │  │  └── PromptService       │  │                    │
│     │  └──────────────────────────┘  │                    │
│     │                                  │                    │
│     │  ┌──────────────────────────┐  │                    │
│     │  │   Document Processing    │  │                    │
│     │  │  ├── DocumentLoader      │  │                    │
│     │  │  ├── TextSplitter        │  │                    │
│     │  │  └── VectorStore         │  │                    │
│     │  └──────────────────────────┘  │                    │
│     │                                  │                    │
│     │  ┌──────────────────────────┐  │                    │
│     │  │   Data Layer             │  │                    │
│     │  │  ├── Redis (Session)     │  │                    │
│     │  │  ├── PostgreSQL(pgvector)│  │                    │
│     │  │  └── File Storage        │  │                    │
│     │  └──────────────────────────┘  │                    │
│     └──────────────────────────────────┘                    │
│                  │                    │                     │
└──────────────────┼────────────────────┼─────────────────────┘
                   │                    │
┌──────────────────┼────────────────────┼─────────────────────┐
│           ┌──────┴──────┐    ┌────────┴────────┐           │
│           │  LLM API    │    │   Ollama (本地) │           │
│           │ OpenAI/     │    │   Llama 3 /     │           │
│           │ Claude/GPT  │    │   Qwen / DeepSeek│          │
│           └─────────────┘    └─────────────────┘           │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 功能设计

| 功能模块 | 描述 | 技术实现 |
|---------|------|---------|
| **对话界面** | Web 聊天界面，支持流式输出 | SSE + Vue/React |
| **文档管理** | 上传 PDF/Word/TXT，自动处理 | Apache Tika + Java NIO |
| **语义搜索** | 基于向量相似度的文档搜索 | pgvector / Milvus |
| **RAG 问答** | 检索相关内容 + LLM 生成回答 | LangChain4j / Spring AI |
| **提示词模板** | 可配置的 Prompt 管理 | 模板引擎 + 版本管理 |
| **会话管理** | 多轮对话上下文管理 | Redis + MessageWindowChatMemory |
| **模型切换** | 支持多种 LLM 切换 | 策略模式 + 工厂模式 |

### 4.4 核心技术实现

#### 4.4.1 LangChain4j RAG 流程

```java
@Configuration
public class LangChain4jConfig {

    @Value("${openai.api-key}")
    private String openAiApiKey;

    @Value("${openai.model:gpt-4o-mini}")
    private String modelName;

    /**
     * 配置 OpenAI 聊天模型
     */
    @Bean
    public ChatLanguageModel chatLanguageModel() {
        return OpenAiChatModel.builder()
                .apiKey(openAiApiKey)
                .modelName(modelName)
                .temperature(0.7)
                .timeout(Duration.ofSeconds(60))
                .logRequests(true)
                .logResponses(true)
                .build();
    }

    /**
     * 配置嵌入模型（用于向量化文本）
     */
    @Bean
    public EmbeddingModel embeddingModel() {
        return OpenAiEmbeddingModel.builder()
                .apiKey(openAiApiKey)
                .modelName("text-embedding-3-small")
                .build();
    }

    /**
     * 配置向量存储（pgvector）
     */
    @Bean
    public EmbeddingStore<TextSegment> embeddingStore() {
        // 使用 pgvector
        return PgvectorEmbeddingStore.builder()
                .host(System.getenv("DB_HOST"))
                .port(Integer.parseInt(System.getenv("DB_PORT")))
                .database(System.getenv("DB_NAME"))
                .user(System.getenv("DB_USER"))
                .password(System.getenv("DB_PASSWORD"))
                .table("knowledge_embeddings")
                .dimension(1536)  // text-embedding-3-small 维度
                .build();
    }
}

@Service
public class RAGService {

    private final ChatLanguageModel chatModel;
    private final EmbeddingStore<TextSegment> embeddingStore;
    private final EmbeddingModel embeddingModel;

    public RAGService(
            ChatLanguageModel chatModel,
            EmbeddingStore<TextSegment> embeddingStore,
            EmbeddingModel embeddingModel) {
        this.chatModel = chatModel;
        this.embeddingStore = embeddingStore;
        this.embeddingModel = embeddingModel;
    }

    /**
     * RAG 问答流程：
     * 1. 接收用户问题
     * 2. 将问题转为向量
     * 3. 在向量库中搜索相似文档
     * 4. 构建包含上下文和问题的 Prompt
     * 5. 调用 LLM 生成回答
     * 6. 返回结果
     */
    public String answer(String question) {
        // 1. 将问题向量化
        Embedding questionEmbedding = embeddingModel.embed(question).content();

        // 2. 搜索最相似的 3 个文档片段
        List<EmbeddingMatch<TextSegment>> matches = embeddingStore
            .findRelevant(questionEmbedding, 3);

        // 3. 构建上下文
        String context = matches.stream()
            .map(match -> match.embedding().text())
            .collect(Collectors.joining("\n\n---\n\n"));

        // 4. 构建 Prompt
        String prompt = String.format("""
            基于以下已知信息，回答用户的问题。
            如果已知信息不足以回答问题，请说"抱歉，我没有足够的信息来回答这个问题"。
            请用中文回答。

            已知信息：
            %s

            用户问题：%s

            回答：
            """, context, question);

        // 5. 调用 LLM
        return chatModel.generate(prompt);
    }

    /**
     * 文档上传并建立索引
     */
    public void indexDocument(String content, String documentId, String fileName) {
        // 1. 分割文本（按段落、句子或固定长度）
        DocumentSplitter splitter = DocumentSplitters
            .recursive(500, 50);  // 每块 500 字符，重叠 50 字符

        Document document = Document.from(content,
            Metadata.from("document_id", documentId)
                .add("file_name", fileName));

        List<TextSegment> segments = splitter.split(document);

        // 2. 批量向量化
        List<Embedding> embeddings = embeddingModel
            .embedAll(segments).content();

        // 3. 存储到向量数据库
        embeddingStore.addAll(embeddings, segments);

        log.info("文档 {} 已索引，共 {} 个片段", fileName, segments.size());
    }
}
```

#### 4.4.2 流式 SSE 响应

```java
@RestController
@RequestMapping("/api/v1/ai")
public class ChatController {

    private final RAGService ragService;
    private final SseEmitterService sseEmitterService;

    @PostMapping(value = "/chat", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
    public SseEmitter chat(@RequestBody ChatRequest request) {
        // 设置超时时间（30 分钟）
        SseEmitter emitter = new SseEmitter(1800000L);

        // 异步处理，避免阻塞
        CompletableFuture.runAsync(() -> {
            try {
                // 1. 发送上下文检索开始事件
                emitter.send(SseEmitter.event()
                    .name("search")
                    .data("正在检索相关知识库..."));

                // 2. 执行 RAG 检索
                List<RelevantDocument> documents =
                    ragService.searchRelevant(request.getMessage());

                emitter.send(SseEmitter.event()
                    .name("retrieved")
                    .data(documents));

                // 3. 流式生成回答
                emitter.send(SseEmitter.event()
                    .name("start")
                    .data("开始生成回答..."));

                // 使用流式调用
                TokenStream tokenStream = ragService
                    .streamAnswer(request.getMessage(), documents);

                tokenStream
                    .onNext(token -> {
                        // 发送每个 token
                        emitter.send(SseEmitter.event()
                            .name("token")
                            .data(token));
                    })
                    .onComplete( response -> {
                        emitter.send(SseEmitter.event()
                            .name("done")
                            .data(""));
                        emitter.complete();
                    })
                    .onError(error -> {
                        emitter.send(SseEmitter.event()
                            .name("error")
                            .data(error.getMessage()));
                        emitter.completeWithError(error);
                    })
                    .start();

            } catch (IOException e) {
                emitter.completeWithError(e);
            }
        });

        return emitter;
    }

    @PostMapping("/documents/upload")
    public Result<String> uploadDocument(
            @RequestParam("file") MultipartFile file) {

        // 1. 解析文档内容
        String content = documentParser.parse(file);

        // 2. 建立索引
        String documentId = UUID.randomUUID().toString();
        ragService.indexDocument(content, documentId, file.getOriginalFilename());

        return Result.success(documentId);
    }
}
```

#### 4.4.3 多模型支持

```java
/**
 * AI 模型工厂 - 支持多种 LLM
 * 策略模式实现多模型切换
 */
@Component
public class AIModelFactory {

    @Value("${openai.api-key}")
    private String openAiKey;

    @Value("${openai.base-url}")
    private String openAiBaseUrl;

    @Value("${claude.api-key}")
    private String claudeApiKey;

    @Value("${ollama.base-url:http://localhost:11434}")
    private String ollamaBaseUrl;

    /**
     * 根据配置获取聊天模型
     */
    public ChatLanguageModel getChatModel(String provider) {
        return switch (provider.toLowerCase()) {
            case "openai" -> OpenAiChatModel.builder()
                .apiKey(openAiKey)
                .baseUrl(openAiBaseUrl)
                .modelName("gpt-4o-mini")
                .build();

            case "claude" -> AnthropicChatModel.builder()
                .apiKey(claudeApiKey)
                .modelName("claude-3-sonnet-20241022")
                .build();

            case "ollama" -> OllamaChatModel.builder()
                .baseUrl(ollamaBaseUrl)
                .modelName("llama3.2")
                .build();

            case "deepseek" -> OpenAiChatModel.builder()
                .apiKey(System.getenv("DEEPSEEK_API_KEY"))
                .baseUrl("https://api.deepseek.com")
                .modelName("deepseek-chat")
                .build();

            default -> throw new IllegalArgumentException(
                "不支持的模型提供商: " + provider);
        };
    }

    /**
     * 获取嵌入模型
     */
    public EmbeddingModel getEmbeddingModel(String provider) {
        return switch (provider) {
            case "openai" -> OpenAiEmbeddingModel.builder()
                .apiKey(openAiKey)
                .modelName("text-embedding-3-small")
                .build();

            case "ollama" -> OllamaEmbeddingModel.builder()
                .baseUrl(ollamaBaseUrl)
                .modelName("nomic-embed-text")
                .build();

            default -> throw new IllegalArgumentException(
                "不支持的嵌入模型: " + provider);
        };
    }
}

// 对话服务 - 使用模型工厂
@Service
public class ChatService {

    private final AIModelFactory modelFactory;
    private final ChatMemoryStore chatMemoryStore;

    /**
     * 多轮对话
     */
    public String chat(ChatRequest request) {
        // 1. 获取指定的聊天模型
        ChatLanguageModel chatModel = modelFactory
            .getChatModel(request.getProvider());

        // 2. 获取或创建会话记忆
        ChatMemory memory = chatMemoryStore
            .getOrCreate(request.getSessionId());

        // 3. 构建带记忆的对话
        ConversationalChain chain = ConversationalChain.builder()
            .chatLanguageModel(chatModel)
            .chatMemory(memory)
            .build();

        // 4. 执行对话
        return chain.execute(request.getMessage());
    }
}
```

### 4.5 项目结构和 README

```
ai-knowledge-assistant/
├── src/main/java/com/ai/
│   ├── AiApplication.java
│   ├── config/
│   │   ├── LangChain4jConfig.java        # LangChain4j 配置
│   │   ├── RedisConfig.java              # Redis 配置
│   │   └── CorsConfig.java               # CORS 配置
│   ├── controller/
│   │   ├── ChatController.java           # 对话接口
│   │   ├── DocumentController.java       # 文档管理接口
│   │   └── ModelController.java          # 模型管理接口
│   ├── service/
│   │   ├── chat/
│   │   │   ├── ChatService.java          # 对话服务
│   │   │   └── ChatMemoryStore.java      # 对话记忆管理
│   │   ├── rag/
│   │   │   ├── RAGService.java           # RAG 检索服务
│   │   │   ├── DocumentService.java      # 文档处理服务
│   │   │   └── EmbeddingService.java     # 向量化服务
│   │   └── prompt/
│   │       ├── PromptTemplateService.java # 提示词模板管理
│   │       └── PromptTemplate.java       # 提示词模板
│   ├── model/
│   │   ├── entity/
│   │   │   ├── Conversation.java         # 会话实体
│   │   │   ├── Message.java              # 消息实体
│   │   │   └── Document.java             # 文档实体
│   │   ├── dto/
│   │   │   ├── ChatRequest.java
│   │   │   ├── ChatResponse.java
│   │   │   └── DocumentUploadResult.java
│   │   └── vo/
│   │       └── RelevantDocument.java
│   └── util/
│       ├── DocumentParser.java           # 文档解析工具
│       └── TextSplitter.java             # 文本分割工具
├── frontend/                              # 前端代码
│   └── src/
│       ├── components/
│       │   ├── ChatWindow.vue
│       │   └── DocumentUpload.vue
│       └── views/
│           └── Home.vue
├── docker-compose.yml
├── Dockerfile
└── README.md
```

---

## 五、项目 README 写作指南

### 5.1 README 模板

一个好 README 就是最好的简历附件。以下是最优实践模板：

```markdown
# 项目名称

> 一句话描述项目（突出技术亮点和价值）

[![Java](https://img.shields.io/badge/Java-17+-orange)]()
[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.2-brightgreen)]()
[![License](https://img.shields.io/badge/License-MIT-blue)]()

## 项目简介

简要介绍项目背景、解决的问题、面向的用户群体。

**核心功能**：
- 功能 1：简短描述
- 功能 2：简短描述
- 功能 3：简短描述

## 在线演示

- 线上地址：[https://your-project-url.com](https://your-project-url.com)
- API 文档：[https://your-project-url.com/swagger-ui.html](https://your-project-url.com/swagger-ui.html)
- 测试账号：admin / admin123

## 技术栈

| 层次 | 技术 |
|------|------|
| 后端框架 | Spring Boot 3.2 |
| ORM | MyBatis-Plus 3.5 |
| 数据库 | MySQL 8.0 |
| 缓存 | Redis 7 |
| 安全 | Spring Security + JWT |
| 文档 | SpringDoc OpenAPI |
| 前端 | Vue 3 + Element Plus |
| 部署 | Docker + Railway |

## 项目结构

```
项目目录结构（展示 2-3 级即可）
```

## 快速开始

### 环境要求

- JDK 17+
- Maven 3.8+
- MySQL 8.0+
- Redis 7+

### 本地运行

```bash
# 1. 克隆项目
git clone https://github.com/your-username/your-project.git

# 2. 配置数据库
# 修改 application.yml 中的数据库连接信息

# 3. 初始化数据库
mysql -u root -p < sql/init.sql

# 4. 启动项目
mvn spring-boot:run

# 5. 访问
# http://localhost:8080
```

### Docker 运行

```bash
docker-compose up -d
```

## 功能展示

### 核心功能截图

![功能截图1](screenshots/feature1.png)
![功能截图2](screenshots/feature2.png)

## 技术亮点

1. **多级缓存策略**：使用 Caffeine + Redis 双级缓存，热点文章使用短 TTL 和主动更新策略
2. **缓存穿透防护**：使用布隆过滤器 + 空值缓存，有效防止缓存穿透
3. **分布式限流**：基于 Sentinel 的接口限流和热点参数限流
4. **性能优化**：数据库索引优化、慢查询治理、连接池优化

## API 文档

项目集成 SpringDoc OpenAPI，启动后访问：

```
http://localhost:8080/swagger-ui.html
http://localhost:8080/v3/api-docs
```

## 部署

### Docker 部署

```bash
docker build -t your-project .
docker run -d -p 8080:8080 your-project
```

### 云部署

本项目已部署到 [Railway](https://railway.app) / [阿里云](https://aliyun.com)

## 后续计划

- [ ] 集成 Elasticsearch 全文搜索
- [ ] 支持 OAuth2 第三方登录
- [ ] 引入消息队列处理异步任务

## 联系我

- 作者：Your Name
- 博客：[https://your-blog.com](https://your-blog.com)
- GitHub：[https://github.com/your-username](https://github.com/your-username)
- 邮箱：your-email@example.com

## License

[MIT License](LICENSE)
```

### 5.2 README 注意事项

| 要素 | 说明 | 作用 |
|------|------|------|
| **Badge** | 技术栈标签 | 一眼看清项目技术栈 |
| **演示链接** | 部署后的地址 | 面试官可以直接体验 |
| **技术亮点** | 2-3 个深度技术点 | 展示思考深度 |
| **项目结构** | 展示分包策略 | 展示工程化能力 |
| **快速开始** | 步骤清晰可执行 | 降低体验门槛 |
| **截图** | 功能截图/GIF | 视觉化展示效果 |

### 5.3 常见错误

```
❌ 错误写法：
"这是一个博客系统"

✅ 正确写法：
"基于 Spring Boot 3 + Vue 3 的博客系统，集成 JWT 认证、Redis 缓存、
Elasticsearch 搜索，已部署到 Docker 环境"

❌ 错误写法：
无技术亮点部分

✅ 正确写法：
列出 3-4 个具体的技术难点和解决方案
```

---

## 六、项目部署方案

### 6.1 免费/低成本部署选项

| 平台 | 免费额度 | 适用 | 注意事项 |
|------|---------|------|---------|
| **Railway** | $5/月免费额度 | Spring Boot 单体 | 自动 HTTPS，绑定域名 |
| **Render** | 免费 512MB RAM | Web 服务 | 15 分钟不活跃会休眠 |
| **Vercel** | 免费 | 前端 + Serverless | 不适合 Spring Boot |
| **阿里云 ECS** | 学生机 ~100元/年 | 所有项目 | 需要手动配置 |
| **腾讯云** | 新用户优惠 | 所有项目 | 活动价较低 |
| **Fly.io** | 免费 3 台 VM | Docker 容器 | 需要信用卡验证 |
| **Heroku** | 已停止免费 | - | - |

### 6.2 部署流程（以 Railway 为例）

```bash
# 1. 准备 Dockerfile
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY target/*.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-jar", "app.jar"]

# 2. 准备 docker-compose.yml（可选）
# 3. 推送到 GitHub
# 4. 在 Railway 中创建项目
#    → 选择 GitHub 仓库
#    → 设置环境变量
#    → 自动构建部署
# 5. 配置域名（Railway 自动分配 .up.railway.app 域名）
```

### 6.3 环境变量管理

不要硬编码敏感信息，使用环境变量：

```yaml
# application.yml
spring:
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/blog}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:root}

jwt:
  secret: ${JWT_SECRET:defaultSecretKey}
  expiration: ${JWT_EXPIRATION:86400000}

redis:
  host: ${REDIS_HOST:localhost}
  port: ${REDIS_PORT:6379}
```

### 6.4 部署检查清单

```
部署前检查：
□ 数据库连接使用环境变量
□ CORS 配置正确
□ 文件上传路径可配置
□ 日志级别正确（生产环境用 WARN/ERROR）
□ 所有 API 端点测试通过
□ Swagger/API 文档可访问
□ 资源文件路径正确
□ JAR 包大小优化（排除不必要依赖）
□ Docker 镜像大小优化（使用多阶段构建）
□ README 已更新部署地址
```

---

## 七、GitHub 主页优化

### 7.1 GitHub Profile README

创建同名仓库（`your-username/your-username`），README.md 就是你的个人主页：

```markdown
### 👋 你好，我是 Your Name

💻 **Java 后端开发工程师**

🔥 **技术栈：**
- 后端：Java • Spring Boot • Spring Cloud • MyBatis
- 数据库：MySQL • Redis • Elasticsearch
- 工具：Docker • Git • Maven • Linux
- 探索：AI Integration • RAG • LangChain4j

🚀 **项目作品**
| 项目 | 描述 | 技术栈 |
|------|------|--------|
| [博客系统](https://github.com/...)| Spring Boot 博客 | Spring Boot, Redis, Vue |
| [微服务商城](https://github.com/...)| Spring Cloud 商城 | Nacos, Sentinel, Seata |
| [AI 问答系统](https://github.com/...)| RAG 知识库问答 | LangChain4j, pgvector |

📊 **统计数据**
[![Your GitHub stats](https://github-readme-stats.vercel.app/api?username=your-username)]
[![Top Langs](https://github-readme-stats.vercel.app/api/top-langs/?username=your-username)]
```

### 7.2 项目 Pins

在 GitHub 主页选择 3-6 个项目 Pin 在顶部：
- 你的两个核心项目（必须已部署）
- 1 个 Fork 的开源项目（展示你参与了开源）
- 或者你在学习的项目

**标准**：
- 有 Star（自己先打）
- 有 README（漂亮的文档）
- 代码质量高（命名规范、有注释）
- 活跃状态（最近有提交）

### 7.3 Contribution Activity

绿点策略：
- 保持每天有提交（至少连续 30 天）
- 控制提交信息规范
- 使用 `git commit --allow-empty` 作为保底（不推荐）
- 建议：每天推进项目开发，自然有提交

---

## 八、项目面试话术

### 8.1 介绍项目的框架（STAR 变体）

```
S - Situation: 项目的背景是什么？
T - Task: 你负责的部分是什么？
A - Action: 你具体做了什么？为什么这样做？
R - Result: 取得了什么成果（性能提升、稳定性、用户体验）？
I - Insight: 你的思考和收获是什么？
```

### 8.2 项目介绍示例

```
面试官："介绍一下你的博客系统项目。"

回答框架：
"好的，我做一个基于 Spring Boot 3 的博客系统，
主要有三个亮点可以分享：

第一是缓存架构。我设计了 Redis + Caffeine 双级缓存，
热点文章走本地缓存，冷数据走 Redis，
同时用布隆过滤器解决了缓存穿透问题。
这个方案让文章接口的响应时间从 50ms 降到了 5ms 以内。

第二是安全设计。我使用 JWT 做无状态认证，
用 Spring Security 的过滤器链实现了细粒度的权限控制。
我还处理了 Token 刷新和黑名单的问题。

第三是搜索设计。初期用 MySQL 全文索引，
后期我设计了 Elasticsearch 的接入方案，
支持了更复杂的搜索需求。

总体来说，这个项目让我真正理解了缓存策略、
安全认证和搜索引擎的实际应用。"

提示：每个亮点准备 2-3 分钟的展开内容，
包括技术方案对比、遇到的问题、解决方案。
```

### 8.3 常见追问准备

为项目中的每个技术决策准备"为什么"的答案：

```
你在项目中为什么用 MyBatis 而不是 JPA？
→ 准备好回答：MyBatis vs JPA 的对比

为什么用 Redis 做缓存而不是只用本地缓存？
→ 准备好回答：分布式场景下的缓存一致性问题

JWT Token 过期了怎么处理？
→ 准备好回答：Access Token + Refresh Token 方案

项目中有没有遇到性能问题？怎么解决的？
→ 准备好回答：一个具体的性能优化案例

如果让你重新做这个项目，你会怎么改进？
→ 准备好回答：目前的不足和未来的改进方向
```

---

## 附录

### A. 项目进度追踪表

```
项目名：博客系统
状态：□ MVP完成 □ 完善中 □ 已部署

核心功能进度：
□ 用户认证（JWT）
□ 文章 CRUD
□ 评论
□ 分类/标签
□ 搜索
□ 文件上传
□ 点赞/收藏
□ 缓存优化
□ Docker 部署
□ README 文档

技术亮点记录：
1. 多级缓存策略 - 已完成
2. 布隆过滤器防穿透 - 待实现
```

### B. 面试话术练习表

```
项目：博客系统

亮点 1: 缓存策略
  技术要点：Redis + Caffeine + 布隆过滤器
  口语表达：...
  面试官可能的追问：...

亮点 2: 安全设计
  技术要点：JWT + Spring Security + Token 刷新
  口语表达：...
  面试官可能的追问：...
```

---

*"项目是你最好的简历。一个优秀的项目，胜过千言万语。"*
