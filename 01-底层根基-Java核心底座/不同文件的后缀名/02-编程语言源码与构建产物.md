# 编程语言源码与构建产物

> 💻 Java/JVM 生态、Python/JS/TS、C/C++/Go/Rust、构建产物 —— 30+ 源码与编译产物文件格式

---

## 📚 目录

1. [Java / JVM 生态](#1-java--jvm-生态)
2. [前端与脚本语言](#2-前端与脚本语言)
3. [系统与编译语言](#3-系统与编译语言)
4. [构建工具产物](#4-构建工具产物)
5. [IDE 与工具文件](#5-ide-与工具文件)

---

## 1. Java / JVM 生态

### 1.1 源码 → 编译 → 打包全链路

```text
.java → .class → .jar / .war
  ↓       ↓         ↓
源码    字节码    归档包

  Hello.java  ─── javac编译 ──→  Hello.class
  Hello.class  ─── jar打包 ──→  app.jar
  app.jar      ─── java执行 ──→  JVM 加载运行
```

| 后缀 | 全称/说明 | 阶段 | 打开方式 |
|:----:|------|:--:|---------|
| **.java** | Java 源码文件 | 编写 | IDE/文本编辑器 |
| **.class** | JVM 字节码（编译产物） | 编译后 | javap 反编译 / JD-GUI |
| **.jar** | Java Archive（ZIP 格式） | 打包 | zip 解压 / `java -jar` |
| **.war** | Web Application Archive | Web 部署 | Tomcat/Jetty 部署 |
| **.ear** | Enterprise Archive | 企业部署 | Java EE 服务器 |
| **.jsp** | JavaServer Pages（已过时） | Web 模板 | 文本编辑器 |
| **.kt** | Kotlin 源码 | 编写 | IntelliJ IDEA |
| **.kts** | Kotlin Script | 脚本 | `kotlinc -script` |
| **.groovy** | Groovy 源码 | 脚本/DSL | `groovy xxx.groovy` |
| **.scala** | Scala 源码 | 编写 | IntelliJ IDEA |

```bash
# 查看 .class 字节码
javap -c Hello.class        # 反汇编
javap -verbose Hello.class  # 详细信息

# 查看 .jar 内容
jar -tf app.jar             # 列出文件
jar -xf app.jar             # 解压

# 反编译 .class → .java
# 工具：JD-GUI, CFR, Procyon
java -jar cfr.jar Hello.class
```

### 1.2 JVM 相关特殊文件

| 后缀 | 说明 |
|:----:|------|
| **.jmod** | Java 9+ 模块文件（替代部分 jar 功能） |
| **.jlink** | 自定义 JRE 镜像 |
| **.hprof** | JVM 堆 dump 文件（MAT/JProfiler 分析） |
| **.jfr** | JDK Flight Recorder 记录 |
| **.jfc** | JFR 配置模板 |

---

## 2. 前端与脚本语言

### 2.1 Web 前端

| 后缀 | 语言 | 运行环境 | 说明 |
|:----:|------|---------|------|
| **.html** | HTML | 浏览器 | 网页结构标记 |
| **.css** | CSS | 浏览器 | 样式表 |
| **.scss / .sass** | Sass | 编译 → CSS | CSS 预处理器 |
| **.less** | Less | 编译 → CSS | CSS 预处理器 |
| **.js** | JavaScript | Node.js/浏览器 | JS 源码 |
| **.mjs** | ES Module JS | Node.js | ES 模块 JS |
| **.cjs** | CommonJS Module | Node.js | CommonJS 模块 |
| **.ts** | TypeScript | 编译 → JS | 类型安全的 JS |
| **.tsx** | TypeScript + JSX | 编译 → JS | React 组件 |
| **.jsx** | JavaScript + JSX | Babel → JS | React 组件 |
| **.vue** | Vue 单文件组件 | 编译 → JS+CSS | Vue SFC |

### 2.2 脚本与后端

| 后缀 | 语言 | 运行方式 |
|:----:|------|---------|
| **.py** | Python | `python xxx.py` |
| **.pyc** | Python 字节码 | Python 解释器缓存 |
| **.ipynb** | Jupyter Notebook | Jupyter/VSCode |
| **.rb** | Ruby | `ruby xxx.rb` |
| **.php** | PHP | PHP 解释器 / Web 服务器 |
| **.go** | Go | `go run xxx.go` → `go build` → 二进制 |
| **.rs** | Rust | `rustc` / `cargo build` |
| **.swift** | Swift | `swiftc` / Xcode |

---

## 3. 系统与编译语言

| 后缀 | 语言 | 编译 → 产物 |
|:----:|------|:----------:|
| **.c** | C | `.o` → 可执行文件 (Linux) / `.exe` (Win) |
| **.cpp / .cc / .cxx** | C++ | 同上 |
| **.h / .hpp** | C/C++ 头文件 | 编译时引用 |
| **.a** | 静态库 (Linux) | 链接进可执行文件 |
| **.so** | 共享库/动态库 (Linux) | 运行时加载 |
| **.dylib** | 动态库 (macOS) | 运行时加载 |
| **.dll** | 动态链接库 (Windows) | 运行时加载 |
| **.lib** | 静态/导入库 (Windows) | 链接阶段 |
| **.o** | 目标文件 | 编译中间产物 |
| **.obj** | 目标文件 (Windows) | 编译中间产物 |

```text
编译链路（C 语言为例）：
  hello.c ──预处理──▶ hello.i ──编译──▶ hello.s ──汇编──▶ hello.o ──链接──▶ hello(可执行)

静态链接 vs 动态链接：
  静态库(.a/.lib)：编译时嵌入可执行文件 → 独立但体积大
  动态库(.so/.dll)：运行时加载 → 共享但需路径配置
```

---

## 4. 构建工具产物

### 4.1 Java 构建

| 后缀 | 所属工具 | 说明 |
|:----:|---------|------|
| **pom.xml** | Maven | 项目对象模型（依赖+构建配置） |
| **build.gradle** | Gradle (Groovy DSL) | 构建配置 |
| **build.gradle.kts** | Gradle (Kotlin DSL) | 构建配置 |
| **settings.gradle** | Gradle | 多模块工程设置 |
| **.mvn/** | Maven Wrapper | 自带 Maven 版本 |
| **gradlew / gradlew.bat** | Gradle Wrapper | 自带 Gradle 版本 |

### 4.2 前端/其他构建

| 后缀 | 所属工具 | 说明 |
|:----:|---------|------|
| **package.json** | npm/Node.js | 依赖 + 脚本 |
| **package-lock.json** | npm | 锁定依赖版本 |
| **yarn.lock** | Yarn | 锁定依赖版本 |
| **pnpm-lock.yaml** | pnpm | 锁定依赖版本 |
| **tsconfig.json** | TypeScript | TS 编译配置 |
| **webpack.config.js** | Webpack | 打包配置 |
| **vite.config.ts** | Vite | 打包配置 |
| **Cargo.toml** | Rust Cargo | 依赖+构建配置 |
| **Cargo.lock** | Rust Cargo | 锁定依赖版本 |
| **go.mod** | Go Modules | 模块+依赖 |
| **go.sum** | Go | 依赖校验和 |
| **CMakeLists.txt** | CMake | C/C++ 构建配置 |
| **Makefile** | make | 通用构建任务 |
| **Dockerfile** | Docker | 容器构建指令 |

---

## 5. IDE 与工具文件

| 后缀 | 所属工具 | 说明 |
|:----:|---------|------|
| **.iml** | IntelliJ IDEA | 模块配置 |
| **.idea/** | IntelliJ IDEA | 项目配置目录 |
| **.project** | Eclipse | 项目描述 |
| **.classpath** | Eclipse | 类路径配置 |
| **.settings/** | Eclipse | 项目设置目录 |
| **.vscode/** | VS Code | 工作区配置 |
| **.code-workspace** | VS Code | 多根工作区 |
| **.editorconfig** | 通用 | 编辑器配置（缩进/换行/编码） ⭐ |
| **.gitignore** | Git | 忽略规则 |
| **.gitattributes** | Git | 属性配置（换行符等） |
| **.gitmodules** | Git | 子模块配置 |
| **.dockerignore** | Docker | 构建忽略规则 |

```ini
# .editorconfig 示例（推荐每个项目都放！）
root = true

[*]
charset = utf-8
end_of_line = lf
indent_style = space
indent_size = 4
insert_final_newline = true
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false

[Makefile]
indent_style = tab
```

---

> 🎯 **核心要点**：Java 开发关注 `.java→.class→.jar` 链路；跨语言项目关注构建文件（`pom.xml/package.json/go.mod`）；团队协作必加 `.editorconfig` 和 `.gitignore`。

---

*创建于：2026年7月*
