# 贡献指南（CONTRIBUTING）

> 欢迎向本知识库贡献内容。本仓库为 **5,752 篇 Markdown 技术文档** 的个人知识体系，贡献方式以内容撰写与修正为主。
> 🏁 当前处于**维护模式**（2026-08-12 起）：优先接受修正类贡献（死链/错字/过时信息校准），新知识体系按需创建。

---

## 📚 目录

1. [贡献类型](#1-贡献类型)
2. [环境准备](#2-环境准备)
3. [文档规范](#3-文档规范)
4. [提交规范](#4-提交规范)
5. [分支与 PR 流程](#5-分支与-pr-流程)
6. [评审清单](#6-评审清单)

---

## 1. 贡献类型

| 类型 | 说明 | 提交前缀 |
|------|------|:---:|
| **新增知识体系** | 新主题的完整体系（00 总览 + 按序模块，11 篇） | `docs` |
| **增强现有文档** | 就地补充内容，保留原有结构与风格 | `docs` |
| **内容修正** | 死链修复、错字、过时版本信息校准 | `fix` |
| **结构调整** | 目录重组、编号修正、索引更新 | `refactor` |
| **工具/配置** | CI、模板、构建配置变更 | `chore` / `build` |

> ⚠️ 维护模式下：新体系需先在 Issue 或 Discussion 说明主题与规划，确认后再动工，避免重复建设。

---

## 2. 环境准备

```bash
# ① 克隆仓库
git clone https://github.com/suxiangyu138/Java-AI-LLM-Learning-Records.git
cd Java-AI-LLM-Learning-Records

# ② 同步 develop（开发集成分支）
git checkout develop
git pull origin develop

# ③ 创建功能分支
git checkout -b feature/<体系名>
```

**写作工具链**（可选）：

| 工具 | 用途 |
|------|------|
| VS Code + markdownlint | 格式实时检查 |
| 终端 markdownlint-cli2 | CI 同款校验（仅根文档） |
| 仓库 doc-quality 技能 | 内容质量自检（结构/字数/锚点） |

---

## 3. 文档规范

所有 `.md` 文件遵循 `CLAUDE.md` 与根 README「文档规范」章节，核心要求：

```md
# 标题
> 一句话定位

## 📚 目录
1. [锚点链接](#section)

## 1. 章节
### 1.1 小节
（论述优先，表格仅用于对比/速查/清单）
```

- **论述优先**：机制/取舍/演进逻辑用文字论述（配代码示例）；表格只用于对比、API 速查、检查清单，禁止用表格代替论述
- **篇幅标准**：单篇中文字数 2000-2500（2026-08 统一门禁）
- **代码块标注语言**：\```java / \```yaml / \```bash，代码块只放代码
- **引用块语义**：`> ⚠️` 警告、`> 💡` 技巧、`> 🎯` 总结
- **时效性**：版本/特性必须标注来源与时间（如 Spring 7.0 GA 2025-11）；写前检索最新官方信息，禁止凭记忆写版本号
- **交叉引用**：文末 `**下一模块：**` / `**返回总览：**`，相对路径链接空格用 `%20` 编码
- **列表纪律**：清单/速查/题目类内容每项独占一行

---

## 4. 提交规范

遵循 Conventional Commits（完整策略见 `DEVELOPMENT.md`）：

```text
<type>(<scope>): <subject>

<body>（可选——为什么/怎么验证）
```

**本仓库示例**：

```text
# 新增知识体系（一次性提交整套）
docs: 新增 正则表达式 知识体系（8篇）
- 覆盖：基础语法/进阶/引擎原理/ReDoS 防护/Java API/实战/面试
- 遵循企业文档标准（背景/边界/权衡/工程细节）

# 内容修正
fix: 修复 xx 文档中过时的端口号信息

# 新体系登记
docs: 登记 xx 知识体系到 CLAUDE.md
```

**提交纪律**：

```text
✅ 必须：
├── 一个提交 = 一个逻辑单元
├── 信息清晰（做了什么 + 为什么）
├── 无死链（相对链接可访问）
└── 遵循文档规范

❌ 禁止：
├── 提交半成品
├── 提交临时文件/个人文件（contribute_*.bat、contributions.txt、.env 等）
├── 提交敏感信息（密钥/密码/真实证件）
└── 无意义空提交（如刷绿脚本生成的 daily contribution）
```

> ⚠️ 仓库历史中曾存在 `contribute_10.bat` 刷提交脚本产生的空提交，此类行为已禁止，请勿再使用。

---

## 5. 分支与 PR 流程

```text
main（展示，稳定）  ← 里程碑合并
develop（集成）     ← PR 目标
feature/xxx（开发） ← 从这里提交 PR
```

**标准流程**：

```bash
# ① 开发完成后推送
git push origin feature/<体系名>

# ② GitHub 创建 PR：feature/<体系名> → develop
# ③ 按模板填写（变更内容/为什么/验证方式/影响范围）
# ④ 评审通过 → squash 合并 → 删除分支
```

**维护模式简化版**（修正类小改动）：

```text
├── 方案 A：直接提交 develop → PR 合入 main（推荐，保持 main 纯净）
└── 方案 B：直接提交 main（个人仓库，修正类可直推）
```

> 🎯 核心原则：**main 永远稳定**（GitHub 展示分支）、**develop 永远前进**、**feature 用完即删**。

---

## 6. 评审清单

提交/PR 前逐项自检：

```text
[ ] 结构：00 总览 + 模块编号正确？
[ ] 格式：表格仅用于对比？代码块标注语言？列表逐行？
[ ] 篇幅：单篇中文字数 ≥ 2000？
[ ] 规范：锚点链接正确？交叉引用有效？
[ ] 一致：与仓库既有体系风格一致？
[ ] 无死链：所有相对链接可访问？
[ ] 时效：版本信息已检索校准并标注来源？
[ ] 无敏感：未提交密钥/个人文件/临时文件？
```

---

## 相关文档

- [README.md](./README.md) — 知识库总览与目录
- [DEVELOPMENT.md](./DEVELOPMENT.md) — Git 分支策略与开发规范
- [CHANGELOG.md](./CHANGELOG.md) — 版本变更历史
- [SECURITY.md](./SECURITY.md) — 安全策略
- [CLAUDE.md](./CLAUDE.md) — 写作规范与知识体系登记
