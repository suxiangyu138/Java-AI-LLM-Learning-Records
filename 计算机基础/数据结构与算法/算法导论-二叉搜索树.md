# 从 Java 后端开发角度深度剖析《算法导论》：二叉搜索树

## 📑 目录

- [一、二叉搜索树（BST）核心定义（后端视角）](#一二叉搜索树bst核心定义后端视角)
- [二、BST 核心性质（算法导论）](#二bst-核心性质算法导论)
- [三、BST 基本操作（Java 实现）](#三bst-基本操作java-实现)
- [四、BST 复杂度分析](#四bst-复杂度分析)
- [五、平衡二叉树（后端工程必备）](#五平衡二叉树后端工程必备)
- [六、Java 后端核心应用](#六java-后端核心应用)
- [七、BST vs 散列表（后端选型）](#七bst-vs-散列表后端选型)
- [八、总结（后端 BST 核心要点）](#八总结后端-bst-核心要点)

---

## 一、二叉搜索树（BST）核心定义（后端视角）

**二叉搜索树（Binary Search Tree, BST）** 是满足左小右大规则的二叉树，中序遍历可得到有序序列。

| 指标 | 值 |
|------|----|
| 平均时间复杂度 | O(logn) |
| 最坏时间复杂度 | O(n)（退化为链表） |
| 空间复杂度 | O(n) |

> **后端意义**：BST 是 TreeMap/TreeSet 的底层结构，用于有序存储、范围查询、动态排序。

---

## 二、BST 核心性质（算法导论）

1. 左子树所有节点值 < 根节点值
2. 右子树所有节点值 > 根节点值
3. 左右子树均为二叉搜索树
4. 中序遍历结果严格递增

---

## 三、BST 基本操作（Java 实现）

```java
class TreeNode {
    int val;
    TreeNode left, right;
    TreeNode(int val) { this.val = val; }
}

public class BST {
    private TreeNode root;

    // 插入
    public void insert(int val) {
        root = insert(root, val);
    }

    private TreeNode insert(TreeNode node, int val) {
        if (node == null) return new TreeNode(val);
        if (val < node.val) node.left = insert(node.left, val);
        else node.right = insert(node.right, val);
        return node;
    }

    // 查找
    public boolean search(int val) {
        return search(root, val);
    }

    private boolean search(TreeNode node, int val) {
        if (node == null) return false;
        if (val == node.val) return true;
        return val < node.val ? search(node.left, val) : search(node.right, val);
    }

    // 删除
    public void delete(int val) {
        root = delete(root, val);
    }

    private TreeNode delete(TreeNode node, int val) {
        if (node == null) return null;
        if (val < node.val) node.left = delete(node.left, val);
        else if (val > node.val) node.right = delete(node.right, val);
        else {
            if (node.left == null) return node.right;
            if (node.right == null) return node.left;
            TreeNode min = findMin(node.right);
            node.val = min.val;
            node.right = delete(node.right, min.val);
        }
        return node;
    }

    private TreeNode findMin(TreeNode node) {
        while (node.left != null) node = node.left;
        return node;
    }
}
```

---

## 四、BST 复杂度分析

| 操作 | 平均时间复杂度 | 最坏时间复杂度 |
|------|--------------|--------------|
| **插入** | O(logn) | O(n) |
| **查找** | O(logn) | O(n) |
| **删除** | O(logn) | O(n) |
| **空间** | O(n) | O(n) |

> **注意**：最坏情况发生在插入有序数据时，树退化为单链表。

---

## 五、平衡二叉树（后端工程必备）

普通 BST 易退化，工程中均使用自平衡树：

| 类型 | 平衡策略 | 特点 |
|------|---------|------|
| **AVL 树** | 高度差 <= 1，旋转平衡 | 查询略快，旋转频繁 |
| **红黑树** | 黑色高度平衡 | 旋转少，插入删除更高效，性能稳定 |

> **Java 工程实践**：JDK TreeMap/TreeSet 底层采用红黑树，保证最坏情况下仍是 O(logn)。

---

## 六、Java 后端核心应用

| 应用场景 | 技术实现 | 核心价值 |
|---------|---------|---------|
| **TreeMap/TreeSet** | 红黑树（平衡 BST） | 有序键值对，范围查询 O(logn) |
| **排行榜** | TreeMap 自定义排序 | 动态维护有序序列 |
| **时间序列数据** | TreeMap 按时间戳排序 | 按时间范围快速检索 |
| **区间统计** | TreeMap subMap() | 范围查询、最值查询 |

---

## 七、BST vs 散列表（后端选型）

| 对比维度 | BST（TreeMap） | 散列表（HashMap） |
|---------|---------------|-----------------|
| **时间复杂度** | O(logn) | O(1) 平均 |
| **有序性** | 有序 | 无序 |
| **范围查询** | 支持 | 不支持 |
| **适用场景** | 需要有序、范围查询 | 快速查找 |

> 有序场景选 BST，快速查找选散列表。

---

## 八、总结（后端 BST 核心要点）

1. BST 左小右大，中序递增，平均 O(logn)
2. 工程使用红黑树（TreeMap）避免退化
3. 核心应用：有序存储、范围查询、排行榜
4. 是 Java 后端有序数据结构的基础

---

## 📖 相关阅读

- [算法导论-二叉搜索树2](./算法导论-二叉搜索树2.md)
- [算法导论-红黑树](./算法导论-红黑树.md)
- [算法导论-红黑树2](./算法导论-红黑树2.md)
- [算法导论-散列表](./算法导论-散列表.md)
- [数据结构-树](./数据结构-树.md)

---

> 作者：suxiangyu
