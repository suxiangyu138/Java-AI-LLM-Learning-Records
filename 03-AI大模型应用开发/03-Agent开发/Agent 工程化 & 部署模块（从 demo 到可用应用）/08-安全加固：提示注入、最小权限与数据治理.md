# 08 安全加固：提示注入、最小权限与数据治理

> 定位：上线前的安全关——以 OWASP LLM Top 10 2025 为纲，覆盖提示注入、过度自主权、输出处理、数据治理（2026-08 基准）

## 📚 目录

1. [OWASP LLM Top 10 2025 全景](#1-owasp-llm-top-10-2025-全景)
2. [修复优先级：先修代价小的](#2-修复优先级先修代价小的)
3. [LLM01 提示注入：多层防御](#3-llm01-提示注入多层防御)
4. [LLM06 过度自主权：最小权限与审批门](#4-llm06-过度自主权最小权限与审批门)
5. [LLM05 输出处理：输出就是不可信输入](#5-llm05-输出处理输出就是不可信输入)
6. [LLM07/08：泄漏与数据治理](#6-llm0708泄漏与数据治理)
7. [LLM10 无界消耗：预算即安全](#7-llm10-无界消耗预算即安全)
8. [Agent 身份与工具供应链](#8-agent-身份与工具供应链)
9. [合规与审计](#9-合规与审计)
10. [加固检查清单](#10-加固检查清单)

## 1. OWASP LLM Top 10 2025 全景

```text
2025 版（2025-11 发布）聚焦 agentic 时代，三个新条目 + 两个重大升级：

LLM01 提示注入       —— 依然是 #1（直接 + 间接）
LLM02 敏感信息披露    —— 训练/输出中的敏感数据
LLM03 供应链         —— 模型/依赖/工具被投毒
LLM04 数据与模型投毒  —— RAG 语料、微调数据被污染
LLM05 不当输出处理    —— 输出未经验证直接进系统
LLM06 过度自主权【升级】—— 权限过大 + 一次注入 = 真实破坏
LLM07 系统提示泄漏【新】—— prompt 必然泄漏，别放秘密
LLM08 向量与嵌入弱点【新】—— RAG 投毒、跨租户泄漏
LLM09 错误信息【新】  —— 幻觉导致的决策错误
LLM10 无界消耗【新】  —— 成本失控也是安全事件
```

> 🎯 **本模块与[调优/排错/安全模块](../Agent%20调优、排错与安全模块（生产必备）/07-安全加固清单.md)的分工**：那里是"上线后逐项核查的现场清单"，这里讲"为什么和怎么设计"——设计错误上线后靠清单救不回来。

## 2. 修复优先级：先修代价小的

按已部署系统的实际破坏程度排序（2026 业界共识）：

| 优先级 | 条目 | 理由 |
|:---:|------|------|
| 1 | LLM05 不当输出处理 | 最便宜：把输出当不可信输入即可，却拦截最多事故 |
| 2 | LLM06 过度自主权 | 把提示注入从"尴尬"变成"昂贵"的放大器，必须最先收 |
| 3 | LLM01 提示注入 | 部分无解（指令与数据同信道），靠 LLM05/06 兜底 |
| 4 | LLM03 供应链 | pin 依赖、锁版本、审计工具清单 |

## 3. LLM01 提示注入：多层防御

### 3.1 攻击类型与本质

```text
直接注入：用户输入里藏指令（"忽略之前的指令…"）
间接注入：恶意内容藏在外部数据里——网页、文档、工具输出、检索到的 chunk

本质：指令与数据共用一条信道，没有任何单一过滤器能彻底解决
公认框架：受不可信内容影响的模型输出 = 不可信数据；
          权限在模型之外强制执行（不在 prompt 里靠"听话"）
```

### 3.2 多层防御（防一层被绕）

| 层 | 手段 |
|----|------|
| 输入分类 | 模式匹配注入惯用语（`ignore previous instructions`、`you are now`、`<|im_start|>` 等），safe/suspicious/blocked 三档处置 |
| 结构化边界 | 分隔符隔离 system/user/tool 内容；工具结果包在 sentinel 标签内（如 `<TOOL_RESULT_DATA>`），恶意值无法提前闭合数据块 |
| 入口净化 | 对检索/工具数据做 per-value 净化：过滤角色标记、jailbreak 惯用语、sentinel 标签 |
| egress 白名单 | 网关层只放行声明过的输出格式/目标（AI Gateway 输出验证） |
| 输出验证 | 检测系统提示泄漏、角色跳出（prompt 里出现"assistant/system"角色标记）；强制引用并校验引用范围 |
| 审计上报 | jailbreak 检测到后 flag-and-proceed（继续服务但记审计事件），不直接阻断（避免误伤） |

> ⚠️ **不可绕过原则**：安全决策（权限、预算、审批）必须由代码强制执行——凡是"让模型自己遵守"的规则（"不要执行危险操作哦"）都算没有防护。

## 4. LLM06 过度自主权：最小权限与审批门

### 4.1 最小权限

| 项 | 基线 |
|----|------|
| 工具白名单 | per-tool allow-list：文件读写限定路径、禁用 shell 执行、API 域名白名单、数据库只读 |
| 循环上限 | maxSteps ~20、maxToolCalls ~50、单工具超时 |
| 预算上限 | token/成本硬上限（超限中断） |
| 角色过滤 | 工具按角色暴露子集（02 篇 `list_for_role`） |
| 分层 gating | tier-2 Agent 不可从用户输入直接调用；工具签名 + 调用后验证 |

### 4.2 人类审批门（HITL）

```text
不可逆/高影响操作必须人工审批：
  删除文件、外部 API 调用、发送邮件、发起退款、修改权限

实现：LangGraph interrupt / 审批队列
审计：action 记录 approved_by: 'auto' | 'human' + 决策理由
```

> 💡 **审批门防的正是组合攻击**：一次成功注入 + 大权限工具 = 删库/泄露/退款。审批门把"模型自主"降为"模型提议、人决策"。

### 4.3 工具调用守卫

- AI 生成的查询/代码过静态分析（阻断危险命令如 SPL 的 `delete`/`sendemail`/`script`）
- 工具结果过 schema 校验再进上下文（防恶意数据注入）
- 所有工具调用留痕（trace + 审计）

## 5. LLM05 输出处理：输出就是不可信输入

| 规则 | 示例 |
|------|------|
| 渲染前编码 | HTML 输出转义（防 XSS）；Markdown 渲染走白名单 |
| 参数化 SQL | LLM 生成的查询只走预编译参数 |
| schema 验证 | 输出先过 JSON Schema 再进业务逻辑（05 篇契约测试同款） |
| 执行隔离 | LLM 提议的代码/命令在沙箱执行（容器 + 网络 egress 白名单） |
| 链接/重定向校验 | 生成 URL 只允许白名单域名 |

## 6. LLM07/08：泄漏与数据治理

### 6.1 LLM07 系统提示泄漏

```text
假设 prompt 必然泄漏（输出、日志、trace、第三方工具都会把它带出去）：
  ✗ 密钥/密码/token 进 prompt
  ✗ 租户标识、内部安全规则进 prompt
  ✓ 安全策略放代码（确定性执行）
  ✓ canary token：prompt 里埋随机 token，输出中监测到即报警
```

### 6.2 LLM08 向量与嵌入弱点（RAG 场景）

| 风险 | 防护 |
|------|------|
| 投毒文档 | 检索源白名单、来源溯源（provenance）、写入前内容审查 |
| 跨租户泄漏 | 向量库按租户隔离（库/分区级）、检索时 ACL 过滤 |
| embedding 供应商 | 固定 embedding 模型版本（pin），模型切换需回归 |

## 7. LLM10 无界消耗：预算即安全

```text
成本失控不是财务问题，是安全事件（被刷、被注入循环、被爬）：

  per-key 预算：网关硬上限（日/月），超限 429
  token 上限：输入/输出长度硬限制
  per-route 限流：单路由 QPS + 并发
  循环上限：max_tool_calls（死循环 8-47 次调用的止血点）
  成本遥测：token-per-success 滚动指标 + 突增告警

完整机制见 11 篇与 [LiteLLM 体系](../../02-大模型基础与Prompt工程/LiteLLM%20多模型适配/00-LiteLLM知识体系总览.md)
```

## 8. Agent 身份与工具供应链

### 8.1 Agent 身份（替代共享 key）

```text
每个 Agent 一个可验证身份：OAuth 2.1 + PKCE
  - 无共享服务账户、无静态 API key
  - 日志可区分、可独立吊销、可限定工具范围
  - 委派链：RFC 8693 token exchange——scope + 时限 + act 声明
    （吊销用户会话 = 吊销其委派给 Agent 的 token）
  - 细粒度授权：ReBAC（OpenFGA）——"Agent X 因代表 User Z 可读 Account Y"
```

### 8.2 工具供应链（MCP 时代必修）

```text
MCP STDIO RCE 事件（≥10 CVE）后的基线：
  - MCP server 版本 pin（锁死，不追最新）
  - 工具清单签名（signed manifest），审查插件供应链
  - 第三方工具凭据走密钥管理（03 篇），不进代码
  - 沙箱执行：容器 + 网络 egress 白名单 + 文件系统隔离
  - AiBOM（AI 物料清单）：记录模型/数据集/工具溯源
```

## 9. 合规与审计

| 合规点 | 内容 |
|--------|------|
| EU AI Act | 第 14 条（人类监督）2026-08-02 生效；部署方需保留监控与干预能力（审批门即落地） |
| 数据治理 | PII 脱敏（trace/日志/数据集三处）；租户数据隔离；留存策略 |
| 审计 | 全链路留痕：请求、决策、工具调用、审批、模型输出 |
| SDLC 集成 | 设计评审含威胁建模（十大风险逐条落位）；CI 跑 OWASP 映射的红队 eval 门禁；生产运行时强制 |

## 10. 加固检查清单

```text
□ 工具最小权限：白名单 + 循环/预算上限 + 角色过滤
□ 不可逆操作已接人类审批门（含审计 approved_by）
□ 输出处理：编码/参数化/schema 验证/沙箱执行
□ 提示注入多层防线（输入分类/边界/净化/输出验证/审计上报）
□ system prompt 零敏感信息，canary token 已埋
□ RAG 检索 ACL 与租户隔离生效
□ per-key 预算与限流已配（LLM10）
□ Agent 身份化（OAuth 2.1 PKCE），无共享 key
□ MCP/工具版本 pin + 供应链审计
□ EU AI Act 第 14 条人类监督落地，审计日志齐全
```

---

## 【参考来源】

- [OWASP LLM Top 10 for LLM Applications 2025（官方）](https://owasp.org/www-project-top-10-for-large-language-model-applications/)
- [futureagi.com: OWASP LLM Top 10 (2025) Risks, Mitigations, and the Tools](https://futureagi.com/blog/owasp-llm-top-10-2025-risks-mitigations-2026/)
- [AWS Samples: Oh My AI DLC Ops — OWASP LLM Top 10 mapping](https://aws-samples.github.io/sample-oh-my-aidlcops/docs/compliance/owasp-llm-top10)
- [Cyber Institute: What's new in OWASP's 2025 GenAI/LLM Top 10](https://www.cyber-institute.org/post/what-s-new-in-owasp-s-2025-genai-llm-top-10-and-why-it-matters-now)
- [Gravitee: OWASP Top 10 for LLM Applications (2025) Practical Guide](https://www.gravitee.io/blog/owasp-top-10-for-llm-applications-2025-a-practical-guide)
- [Mindflow: 9 Engineering Practices（沙箱、治理工具箱、审批）](https://mindflow.io/blog/the-production-ai-agent-reality-check-9-engineering-practices-that-actually-work)
- 现场加固清单与 MCP RCE 事件：见 [Agent 调优、排错与安全模块 07 篇](../Agent%20调优、排错与安全模块（生产必备）/07-安全加固清单.md)

---

**返回总览**：[00-总览：Agent 工程化与部署模块（从 demo 到可用应用）](00-总览：Agent%20工程化与部署模块（从%20demo%20到可用应用）.md)

**下一模块**：[09-部署架构：同步流式、异步队列与网关拓扑](09-部署架构：同步流式、异步队列与网关拓扑.md)
