# 07 端到端闭环与代码全解

> 把 02-06 篇的零件拼成完整系统：一份可运行的 rag.py 全解 + Java Spring AI 对照 + 运行验证清单 + 报错排查表——阶段 2 的"毕业作品"。

## 📚 目录

1. [完整代码（rag.py）](#1-完整代码ragpy)
2. [代码逐段讲解](#2-代码逐段讲解)
3. [运行验证清单](#3-运行验证清单)
4. [Java 对照：Spring AI 最小实现](#4-java-对照spring-ai-最小实现)
5. [常见报错排查表](#5-常见报错排查表)
6. [扩展方向（学完本阶段后的进阶路标）](#6-扩展方向学完本阶段后的进阶路标)
7. [代码自测与重构练习](#7-代码自测与重构练习)

## 1. 完整代码（rag.py）

```python
"""Basic RAG：文档 → 切片 → 向量化 → FAISS → 检索 → DeepSeek 生成"""
import json
import os
import re
import numpy as np
from pathlib import Path

from sentence_transformers import SentenceTransformer
from openai import OpenAI
import faiss

# ---------- 配置 ----------
MODEL_NAME = "BAAI/bge-large-zh-v1.5"   # 中文 Embedding
DIM = 1024                              # 必须与模型维度一致
CHUNK_SIZE, CHUNK_OVERLAP = 400, 80
TOP_K = 5
DATA_DIR = Path("data")

# ---------- 1. 加载与清洗 ----------
def load_clean(path: Path) -> str:
    text = path.read_text(encoding="utf-8")
    text = re.sub(r"\s+", " ", text)
    text = re.sub(r"\n{3,}", "\n\n", text)
    return text.strip()

# ---------- 2. 切片 ----------
def fixed_chunk(text: str) -> list[str]:
    chunks, start = [], 0
    while start < len(text):
        chunks.append(text[start:start + CHUNK_SIZE])
        start += CHUNK_SIZE - CHUNK_OVERLAP
    return chunks

# ---------- 3. 建库 ----------
def build_index(model):
    index = faiss.IndexHNSWFlat(DIM, 32)
    chunks = []  # 与向量 id 一一对应
    for path in DATA_DIR.iterdir():
        if path.suffix.lower() not in (".txt", ".md"):
            continue
        for c in fixed_chunk(load_clean(path)):
            chunks.append({"text": c, "meta": {"title": path.stem}})
    vecs = model.encode([c["text"] for c in chunks], normalize_embeddings=True)
    index.add(np.array(vecs, dtype=np.float32))
    return index, chunks

# ---------- 4. 检索 ----------
def retrieve(model, index, chunks, query, k=TOP_K):
    qv = model.encode([query], normalize_embeddings=True)
    _, ids = index.search(np.array(qv, dtype=np.float32), k)
    return [chunks[i] for i in ids[0] if i != -1]

# ---------- 5. 生成 ----------
client = OpenAI(api_key=os.environ["DEEPSEEK_API_KEY"], base_url="https://api.deepseek.com")

def ask(model, index, chunks, query):
    results = retrieve(model, index, chunks, query)
    context = "\n\n".join(
        f"[资料{i+1}]（来源：{r['meta']['title']}）\n{r['text']}" for i, r in enumerate(results)
    )
    prompt = f"""请严格依据【参考资料】回答问题，不要编造；资料中没有则回答"知识库中没有相关内容"。

【参考资料】
{context}

【用户问题】
{query}"""
    resp = client.chat.completions.create(
        model="deepseek-chat", temperature=0.3,
        messages=[{"role": "user", "content": prompt}],
    )
    return resp.choices[0].message.content

# ---------- 主流程 ----------
if __name__ == "__main__":
    model = SentenceTransformer(MODEL_NAME)
    index, chunks = build_index(model)
    print(f"建库完成：{len(chunks)} 个切片，索引大小 {index.ntotal}")
    while True:
        q = input("\n提问（输入 exit 退出）: ").strip()
        if q in ("exit", "quit"):
            break
        print(ask(model, index, chunks, q))
```

## 2. 代码逐段讲解

| 段 | 对应 | 关键点 |
|---|---|---|
| 配置区 | — | MODEL_NAME/DIM 一致性：换模型必须同步改 DIM |
| 加载清洗 | 02 篇 | 简化版（TXT/MD），PDF 请扩展 load 分派 |
| 切片 | 03 篇 | 固定窗口入门版；生产换递归/语义切块 |
| 建库 | 04-05 篇 | **chunks 列表顺序 = 向量 id 顺序**（核心约定） |
| 检索 | 06 篇 | 查询用同一模型；返回切片含 meta 溯源 |
| 生成 | 06 篇 | temperature=0.3 忠实引用；无 Key 换 Ollama |
| 主流程 | — | 先建库（一次性）再交互问答 |

### 三个最容易写错的地方

| 位置 | 易错 | 检查 |
|---|---|---|
| `DIM` | 与模型维度不一致 | 换模型必改 DIM |
| `dtype=np.float32` | 漏掉报类型错 | FAISS 要求 float32 |
| `chunks` 顺序 | 与 id 错位 | add 与 extend 必须同序同量 |

## 3. 运行验证清单

```bash
export DEEPSEEK_API_KEY=sk-xxx
python rag.py
```

| 验证项 | 预期 | 失败检查 |
|---|---|---|
| 建库输出 | "建库完成：N 个切片" | data/ 目录有文档；模型路径正确 |
| 提问"文档内的事实" | 回答正确且有依据 | 检索 Top-K 是否命中（先加打印） |
| 提问"文档外的问题" | 回答"知识库中没有" | Prompt 约束是否生效 |
| 提问"同义改写" | 仍能命中 | 中文 BGE 的语义能力（04 篇实测） |

验证顺序：**先看检索（Top-K 对不对）→ 再看生成（Prompt 用没用）**——问题一定先定位到层。

### 验收问题集模板（用自己的文档造）

| 问题类型 | 例子 | 验证什么 |
|---|---|---|
| 原文事实 | "文档里 XX 的规定是？" | 检索命中 + 正确回答 |
| 同义改写 | 用不同词问同一件事 | 语义检索能力 |
| 跨块综合 | 答案分散在两个切片 | 多块召回 + 生成整合 |
| 库外问题 | "今天天气怎么样" | 拒答机制 |

## 4. Java 对照：Spring AI 最小实现

```java
@Configuration
public class RagConfig {
    // Embedding：BGE 通过 Ollama 或 API 提供；此处以 Ollama 为例
    @Bean
    public EmbeddingModel embeddingModel() {
        return new OllamaEmbeddingModel(OllamaApi.builder()
            .baseUrl("http://localhost:11434")
            .build(), "bge-m3");
    }

    // 向量库：pgvector（需先建 extension vector）
    @Bean
    public VectorStore vectorStore(JdbcTemplate jdbc, EmbeddingModel emb) {
        return new PgVectorStore(jdbc, emb);
    }

    // ChatClient + RAG Advisor：自动完成检索增强
    @Bean
    public ChatClient chatClient(ChatModel chatModel, VectorStore vs) {
        return ChatClient.builder(chatModel)
            .defaultAdvisors(QuestionAnswerAdvisor.builder(vs).build())
            .build();
    }
}
```

```java
@RestController
public class AskController {
    private final ChatClient chatClient;

    @PostMapping("/ask")
    public String ask(@RequestBody String question) {
        return chatClient.prompt()
            .user(question)
            .call()
            .content();   // QuestionAnswerAdvisor 自动：检索 Top-K → 增强 Prompt → 生成
    }
}
```

```yaml
spring:
  ai:
    openai:
      api-key: ${DEEPSEEK_API_KEY}
      base-url: https://api.deepseek.com
      chat:
        options: { model: deepseek-chat, temperature: 0.3 }
    vectorstore:
      pgvector:
        index-type: HNSW
        distance-type: COSINE_DISTANCE
        dimensions: 1024
```

对照理解：Java 的 `QuestionAnswerAdvisor` 自动完成了 Python 版手写的"检索 + Prompt 组装"——**原理完全相同，框架替你封装**。阶段 3 起两种栈并行理解。

### Python vs Java 逐环节对照

| 环节 | Python（手写） | Java（框架） |
|---|---|---|
| 切片 | fixed_chunk | TokenTextSplitter（可配） |
| 向量化 | SentenceTransformer.encode | EmbeddingModel.embed |
| 入库 | FAISS add | PgVectorStore.add |
| 检索 | index.search | VectorStore.similaritySearch |
| Prompt 组装 | build_prompt | QuestionAnswerAdvisor |

## 5. 常见报错排查表

| 报错 | 原因 | 修复 |
|---|---|---|
| `Dimension mismatch` | 索引 DIM 与向量维度不符 | 统一为 1024（或改模型时同步） |
| `RuntimeError: Model not found` | 模型下载失败 | HF_ENDPOINT 镜像或本地下载（01 篇） |
| `API key not found` | 环境变量未设置 | export DEEPSEEK_API_KEY |
| `401 Unauthorized` | Key 无效 | 检查 Key 与 base_url |
| 中文乱码 | 文件编码 | 读取指定 encoding="utf-8" |
| 检索全返回 -1 | 索引为空/查询维度错误 | 检查 add 是否成功、DIM |
| 回答不基于资料 | 检索没命中 | 先打印 Top-K 调试检索层 |
| 内存不足 | 切片过多 | 缩小 batch 或换 IndexFlatIP |
| `TypeError: float32 required` | numpy 默认 float64 | 显式 dtype=np.float32 |
| 交互卡死 | API 超时 | 加 timeout 参数 + 重试 |

### 排错方法论：一层一层查

```
① 数据层：data/ 有文档？加载清洗 OK？→ 打印 len(text)
② 切片层：块数合理？断点正常？→ audit_chunks（03 篇）
③ 向量层：维度 1024？归一化？→ 自检检索分数≈1.0（05 篇）
④ 检索层：Top-K 相关？→ debug_retrieve（06 篇）
⑤ 生成层：Prompt 组装对？API 正常？→ 单独调 generate
```

## 6. 扩展方向（学完本阶段后的进阶路标）

| 扩展 | 位置 |
|---|---|
| 换递归切片/语义切片 | 03 篇 + 阶段 3 |
| 加相似度阈值与拒答 | 08 篇任务三 |
| 换 pgvector/Milvus | 05 篇对照 + 阶段 3 |
| 加 Rerank 精排 | 阶段 3 |
| 加评估（RAGAS） | 阶段 3 |
| 多文档路由/Agentic | 阶段 4 |

## 7. 代码自测与重构练习

### 自测用例（给自己的代码写测试）

```python
# test_rag.py（最小冒烟测试）
import pytest

def test_retrieve_hits_query_in_doc(store, model):
    """检索自检：库内原文查询应自命中"""
    for c in store.chunks[:3]:
        qv = model.encode([c["text"]], normalize_embeddings=True)
        qv = np.array(qv, dtype=np.float32)
        D, I = store.index.search(qv, 1)
        assert I[0][0] != -1 and D[0][0] > 0.9, "自命中失败"

def test_chunks_cover_docs(store, data_dir):
    """覆盖自检：切片总数应大于 0 且文本非空"""
    assert len(store.chunks) > 0
    assert all(c["text"] for c in store.chunks)

def test_ask_has_source(ask, query="文档里的一个问题"):
    """生成自检：答案应包含来源（若 Prompt 要求）"""
    answer = ask(query)
    assert "来源" in answer or "资料" in answer
```

### 重构练习（检验代码理解）

| 练习 | 检验点 |
|---|---|
| 把 build_index 拆成 3 个函数 | 是否理解每个环节边界 |
| 给 retrieve 加 threshold 参数 | 是否理解阈值语义 |
| 把 Prompt 抽成模板文件 | 是否理解 Prompt 结构 |
| 支持多目录数据源 | 是否理解 data 抽象 |
| 加日志与耗时统计 | 是否理解链路耗时分布 |

### 代码质量自查清单

| 项 | 标准 |
|---|---|
| 无魔法数字 | DIM/TOP_K 等用配置常量 |
| 错误处理 | API 失败有 try/except |
| 注释 | 每段函数有"做什么/为什么" |
| 可复用 | 与业务无关的能独立成模块 |
| 可测试 | 关键函数能单测 |

> 🎯 核心要点：本模块的代码就是阶段 2 的毕业作品——**能跑通 + 能讲清每一段** = 基本 RAG 掌握。跑通后别急着优化，先做 08 篇的扩展实验加深理解；再做 07 篇的自测与重构练习，把"会跑"变成"会写"。

---

**下一模块**：[08-阶段实战与自测](08-阶段实战与自测.md) / **返回总览**：[00-阶段总览与学习路径](00-阶段总览与学习路径.md)
