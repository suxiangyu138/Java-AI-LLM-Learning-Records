# 01 环境准备与双技术栈选型

> 动手第一步：把 Python 与 Java 两套环境搭好——虚拟环境、依赖清单、模型下载与离线方案、常见坑一次排清。

## 📚 目录

1. [Python 环境准备](#1-python-环境准备)
2. [Java 环境准备](#2-java-环境准备)
3. [Embedding 模型下载与离线](#3-embedding-模型下载与离线)
4. [验证环境就绪](#4-验证环境就绪)
5. [常见安装坑](#5-常见安装坑)
6. [选型建议：什么时候选哪套](#6-选型建议什么时候选哪套)
7. [环境基线核对表](#7-环境基线核对表)

## 1. Python 环境准备

### 1.1 创建虚拟环境

```bash
# Python 3.12+ 建议用 uv（快 10 倍+）
uv venv rag-env
# 或传统方式
python -m venv rag-env

# 激活（Windows）
rag-env\Scripts\activate
# 激活（macOS/Linux）
source rag-env/bin/activate
```

> 💡 虚拟环境是铁律：避免依赖冲突污染全局 Python（详见本仓库 `03-AI.../Python虚拟环境/` 体系）。

### 1.2 安装依赖

```bash
pip install sentence-transformers faiss-cpu numpy
# 可选（阶段 3 会用）：
pip install langchain langchain-community langchain-huggingface
```

| 依赖 | 版本基线（2026） | 用途 |
|---|---|---|
| sentence-transformers | 3.x | Embedding 与 Reranker 模型加载 |
| faiss-cpu | 1.x | 向量索引与检索 |
| numpy | 2.x | 向量运算 |
| torch | 2.x（随 sentence-transformers 自动装） | 模型推理后端 |
| transformers | 4.x（自动依赖） | BGE 模型底座 |
| openai | 1.x | DeepSeek API 调用（OpenAI 兼容） |

### 1.3 安装顺序建议

```bash
# 推荐顺序（避免依赖冲突）
pip install numpy
pip install faiss-cpu
pip install sentence-transformers   # 会带 torch/transformers
pip install openai
```

> ⚠️ 如果先装 sentence-transformers 再装 faiss，numpy 版本可能被重新解析——出现版本冲突时**全部装完后执行 `pip check` 验证**。

## 2. Java 环境准备

### 2.1 技术栈

| 组件 | 选择 | 说明 |
|---|---|---|
| JDK | 21+ | Spring Boot 4 要求 |
| 框架 | Spring Boot 4.x + Spring AI 2.x | 2026 主流 |
| 向量存储 | PostgreSQL + pgvector | Docker 一键起 |
| LLM | DeepSeek（OpenAI 兼容） | base-url 指 DeepSeek |

### 2.2 启动 PostgreSQL（Docker）

```bash
docker run -d --name pg-rag \
  -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres \
  -p 5432:5432 pgvector/pgvector:pg16
```

```sql
-- 建库后启用扩展（Java 项目初始化时执行）
CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS pg_trgm;
```

### 2.3 Maven 依赖

```xml
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-pgvector-store</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.ai</groupId>
    <artifactId>spring-ai-openai-spring-boot-starter</artifactId>
</dependency>
```

> 💡 2026 年 Spring AI 2.x 的依赖坐标已稳定；使用 BOM 管理版本（`spring-ai-bom`），避免版本漂移。

## 3. Embedding 模型下载与离线

### 3.1 模型选择

| 模型 | 维度 | 体积 | 适用 |
|---|---|---|---|
| BAAI/bge-small-zh-v1.5 | 512 | 约 100MB | 轻量、CPU 快 |
| BAAI/bge-large-zh-v1.5 | 1024 | 约 1.3GB | 中文效果好（本阶段推荐） |
| BAAI/bge-m3 | 1024 | 约 2.2GB | 多语言+多功能（阶段 3） |

> 为什么中文选 BGE：实验数据——"退款申请"与"申请退款"这类近义词，通用多语言模型相似度约 0.72，BGE 中文模型 0.95+；BGE-large-zh 1024 维比 1536 维通用模型存储降低 44%、检索提速 35%。

### 3.2 提前下载（离线部署关键）

```bash
huggingface-cli download BAAI/bge-large-zh-v1.5 \
  --local-dir ./models/bge-large-zh-v1.5 \
  --local-dir-use-symlinks False
```

代码中强制离线：

```python
import os
os.environ["TRANSFORMERS_OFFLINE"] = "1"
os.environ["HF_HUB_OFFLINE"] = "1"
os.environ["TOKENIZERS_PARALLELISM"] = "false"

from sentence_transformers import SentenceTransformer
model = SentenceTransformer("./models/bge-large-zh-v1.5")  # 本地路径
```

### 3.3 国内下载加速

| 方案 | 做法 |
|---|---|
| 镜像 | `export HF_ENDPOINT=https://hf-mirror.com`（下载前设置） |
| 本地缓存 | 下载一次后走 `~/.cache/huggingface/hub`，无需重复下载 |
| 企业代理 | 配置 `http_proxy/https_proxy` 环境变量 |

> ⚠️ 镜像必须在**首次下载前**设置（代码 import 前），否则已经尝试直连的缓存会混乱。

## 4. 验证环境就绪

```python
# check_env.py
import faiss, numpy as np
from sentence_transformers import SentenceTransformer

model = SentenceTransformer("BAAI/bge-large-zh-v1.5")
v = model.encode(["验证环境"], normalize_embeddings=True)
index = faiss.IndexFlatIP(len(v[0]))
index.add(np.array(v, dtype=np.float32))
D, I = index.search(np.array(v, dtype=np.float32), 1)
print("环境 OK，维度:", len(v[0]), "相似度:", D[0][0])
```

预期输出：`环境 OK，维度: 1024 相似度: 1.0`（自问自答相似度应为 1）。

### 验证失败的快速定位

| 失败点 | 检查 |
|---|---|
| import faiss 报错 | faiss-cpu 是否安装成功（01 篇坑表） |
| 模型下载失败 | 网络/镜像（3.3 节） |
| 维度不是 1024 | 模型路径是否指向 bge-large-zh（别指成 small） |
| 相似度不是 1.0 | 正常情况必为 1.0（同一向量自比）——不等于 1 说明向量有异常 |

## 5. 常见安装坑

| 坑 | 现象 | 解法 |
|---|---|---|
| faiss 安装失败 | ModuleNotFoundError / 编译报错 | `pip install faiss-cpu`（不要装 faiss）；Windows 用官方 wheel |
| 模型下载超时 | HF 连接失败 | 镜像 `export HF_ENDPOINT=https://hf-mirror.com` 或提前本地下载 |
| torch 巨大 | 安装 2GB+ | CPU 版 `pip install torch --index-url https://download.pytorch.org/whl/cpu` |
| 版本冲突 | sentence-transformers 与 transformers 版本不兼容 | 用虚拟环境 + `pip install -U` 对齐 |
| Java 启动报维数错误 | pgvector dimensions 与实际不符 | yml 中 dimensions 必须等于模型维度（bge-large-zh = 1024） |
| Windows 下路径中文 | 模型路径中文导致编码问题 | 项目路径避免中文（模型缓存目录尤甚） |
| pip 安装极慢 | 默认 PyPI 源慢 | `pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple` |
| Docker 拉取 pgvector 慢 | 镜像源问题 | 配置国内镜像加速器 |

## 6. 选型建议：什么时候选哪套

| 你的情况 | 首选 |
|---|---|
| 学原理、快速验证、AI 生态 | Python 全套 |
| Java 后端团队、要上生产 | Java + pgvector |
| 个人知识库 Demo | Python + FAISS |
| 企业客服系统 | Java + Milvus/pgvector |
| 两者都学 | Python 先跑通 → Java 对照（本阶段即此路径） |

### 本阶段的选型结论

- **理解原理**用 Python（07 篇完整代码）
- **对照生产**看 Java（07 篇第 4 节）
- 两条线并行推进：Python 跑通 Demo，Java 看懂装配——阶段 3 起以 Java 为主做生产化

## 7. 环境基线核对表

动手前逐项核对，全部 ✅ 再开始：

### Python 栈

| 项 | 核对内容 | ✅ |
|---|---|---|
| Python 版本 | `python --version` ≥ 3.12 | ☐ |
| 虚拟环境 | 激活状态（终端提示符带 env 名） | ☐ |
| 依赖完整 | `pip list` 含 sentence-transformers/faiss-cpu/numpy/openai | ☐ |
| 依赖无冲突 | `pip check` 无报错 | ☐ |
| 模型可达 | 提前下载或镜像可用 | ☐ |
| 环境验证 | `python check_env.py` 输出"环境 OK" | ☐ |

### Java 栈

| 项 | 核对内容 | ✅ |
|---|---|---|
| JDK | `java -version` ≥ 21 | ☐ |
| Docker | `docker ps` 可用 | ☐ |
| PG 容器 | `docker ps` 见 pg-rag | ☐ |
| 扩展启用 | `\dx` 见 vector/pg_trgm | ☐ |
| Maven 依赖 | 编译通过 | ☐ |
| DeepSeek Key | 环境变量已配 | ☐ |

### 环境出问题的黄金顺序

```
1. 看报错全文（不是第一行）
2. 查 05 节坑表是否已覆盖
3. pip check / mvn -v 验证工具链
4. 最小化复现（单独 import 一个库测试）
5. 问社区（报错信息 + 版本号）
```

> 🎯 核心要点：环境搭建的目标是"一次成功"——虚拟环境隔离 + 模型提前下载（离线模式）+ faiss-cpu 三件事做好，后面所有报错基本只剩代码逻辑问题。动手前过一遍基线核对表，环境问题就不进代码环节。

---

**下一模块**：[02-文档加载与预处理](02-文档加载与预处理.md) / **返回总览**：[00-阶段总览与学习路径](00-阶段总览与学习路径.md)
