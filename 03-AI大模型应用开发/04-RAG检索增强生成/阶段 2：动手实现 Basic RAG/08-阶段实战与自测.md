# 08 阶段实战与自测

> 阶段 2 收尾：三个动手扩展任务（建立参数手感）、20 道自测题（附答案）、毕业检查单——通过后进入阶段 3 进阶调优。

## 📚 目录

1. [动手任务一：切片参数实验](#1-动手任务一切片参数实验)
2. [动手任务二：换文档与换模型](#2-动手任务二换文档与换模型)
3. [动手任务三：加相似度阈值与溯源](#3-动手任务三加相似度阈值与溯源)
4. [20 道自测题](#4-20-道自测题)
5. [毕业检查单](#5-毕业检查单)
6. [下一阶段预告](#6-下一阶段预告)

## 1. 动手任务一：切片参数实验

目标：用**同一份文档**分别建 3 个库，对比回答质量。

```python
for size, overlap in [(200, 40), (400, 80), (800, 160)]:
    # 修改 CHUNK_SIZE/CHUNK_OVERLAP 重建索引
    # 对同一组 5 个问题提问，记录回答质量
```

| 实验维度 | 记录内容 |
|---|---|
| 块数变化 | size↑ → 块数↓（预期） |
| 断点质量 | 抽 3 块看是否从句子中间切开 |
| 回答完整度 | 信息是否齐全（size 太小时易碎） |
| 回答精确度 | 是否引入无关上下文（size 太大时噪音多） |

**实验结论模板**：在本阶段文档规模下，最优 size 是 ____，理由是 ____。

## 2. 动手任务二：换文档与换模型

### 2.1 换文档类型

- 加一篇 PDF（需扩展 02 篇的 load 分派，用 pypdf）
- 加一篇 HTML（用 BeautifulSoup 清洗）
- 提问对比：混合格式库能否正确溯源到各自的文档标题

### 2.2 换 Embedding 模型（体验一致性铁律）

```python
# 建库用 bge-large-zh-v1.5，查询故意换 bge-small-zh-v1.5
# 观察：Top-K 是否明显变差？
```

> 结论：换模型必须重建索引——亲手验证一次"语义空间错位"，比背口诀印象深 10 倍。

## 3. 动手任务三：加相似度阈值与溯源

```python
def retrieve(model, index, chunks, query, k=TOP_K, threshold=0.45):
    qv = model.encode([query], normalize_embeddings=True)
    D, ids = index.search(np.array(qv, dtype=np.float32), k)
    results = []
    for dist, idx in zip(D[0], ids[0]):
        if idx == -1 or float(dist) < threshold:   # 阈值过滤
            continue
        results.append({**chunks[idx], "score": float(dist)})
    return results
```

验证：

- 用"知识库外问题"测试：阈值过滤后结果为空 → Prompt 走"知识库中没有相关内容"分支
- 在生成回答里打印"参考了哪些文档、相似度多少"——形成**溯源输出**

## 4. 20 道自测题

### 环境与加载（1-5）

1. 本阶段推荐的 Python 依赖三件套？为什么必须用虚拟环境？
2. bge-large-zh-v1.5 的维度是多少？维度不一致会怎样？
3. PDF 扫描件提取文本返回空，原因与对策？
4. 文本清洗至少做哪几件事？
5. 元数据一般存哪些字段？用途是什么？

### 切片（6-9）

6. 固定窗口切片的最大缺点？
7. 递归字符切片的断点原则？（优先在段落/句子边界断开）
8. chunk_size 过大的缺点？（检索粒度粗、噪音多、费上下文）
9. chunk_overlap 的作用？（保留边界语义连续）

### 向量化与索引（10-14）

10. 为什么编码后要归一化？（归一化后内积=余弦相似度）
11. 建库与查询为什么必须用同一个 Embedding 模型？
12. FAISS 的 IndexHNSWFlat 比 IndexFlatIP 快在哪？（图索引近似检索）
13. FAISS 只存向量不存文本，如何拿到原文？（id → chunks 列表顺序映射）
14. 本阶段为什么选 FAISS？（零部署、原理可见）

### 检索与生成（15-20）

15. Top-K 的 K 一般取多少？K 太大有什么问题？
16. 相似度阈值过滤的作用？
17. RAG Prompt 的四条设计原则？（只依据资料/没有就拒/注来源/中文）
18. 为什么 RAG 问答用低 temperature？（忠实引用减少发散）
19. 检索 Top-K 结果不相关时，问题在哪一层？
20. 无 DeepSeek Key 时如何跑通全链路？（Ollama 本地模型）

### 参考答案（简版）

1. sentence-transformers + faiss-cpu + numpy；隔离依赖防冲突
2. 1024；报错或静默错位
3. 扫描件无文本层；需 OCR
4. 空白统一/去控制字符/去零宽/去多余空行
5. doc_id/title/section/page；溯源与过滤
6. 感知不到语义边界，可能切断句子
7. 优先段落→句子→标点逐级断开
8. 粒度粗、噪音多、浪费上下文窗口
9. 相邻块边界语义连续，避免切断后信息丢失
10. 归一化后 |A|=|B|=1，内积即余弦相似度
11. 语义空间必须一致，否则检索随机
12. HNSW 图索引近似最近邻，避免全量暴力比较
13. 向量 id 与 chunks 列表下标一一对应
14. pip 即用无部署，索引机制可见
15. 5 左右；太大引入噪音、费上下文
16. 过滤低相似度结果，触发拒答
17. 只依据资料/缺失明说/标注来源/中文回答
18. 忠实引用检索内容，避免发散编造
19. 检索层（先看 Top-K 再调生成）
20. Ollama 本地模型（OpenAI 兼容端点）

## 5. 毕业检查单

| 检查项 | 达标标准 | 状态 |
|---|---|---|
| 端到端问答 | 真实文档建库 + 5 个问题回答正确 | ☐ |
| 代码讲解 | 每段代码能讲清链路角色 | ☐ |
| 参数实验 | 完成 3 组切片实验并给出结论 | ☐ |
| 阈值溯源 | 外问题触发拒答 + 回答带来源 | ☐ |
| 自测题 | 正确率 ≥ 80%（16/20） | ☐ |

未达标项：回到对应模块（切片→03 篇、维度→04-05 篇、Prompt→06 篇）。

## 6. 下一阶段预告

**阶段 3：进阶调优（工程核心）**——把 Basic RAG 升级为可用系统：

- 检索层：混合检索（BM25 + 向量）、查询改写、粗召回 + Rerank 精排
- 数据层：语义切块、父子文档索引、增量更新
- 评估层：RAGAS 评估、指标看板、回归测试
- 工程层：pgvector 生产化、缓存与成本优化

> 🎯 阶段 2 毕业标准总结：**链路能跑、代码能讲、参数有手感、错误能定位**——四项齐备，进入阶段 3。

---

**返回总览**：[00-阶段总览与学习路径](00-阶段总览与学习路径.md)
