# 模板与格式类问题

> 2026 年确认的"第一大静默杀手"：chat template 不匹配——每个组件单独看都对，组合起来模型直接崩；渲染 diff 是唯一防线的第一检查

## 1. 为什么模板问题是"静默杀手"

模板错误的可怕之处在于**不报任何错**：数据加载正常、loss 正常下降、checkpoint 正常保存——**只有推理时模型输出垃圾**。2026 年多个生产案例实锤了它的隐蔽性：某个微调项目"数据正确、模板官方、loss 收敛，没有报任何错误"，但工具调用 0/6 成功——事后定位是模板的 think 块不对称。

模板是**位置依赖的函数**，不是序列化格式：训练模式与推理模式的渲染输出**不保证一致**——同一个模板在"训练上下文"（有金标回答）与"推理上下文"（只有 prompt）下可能渲染出不同的 token 序列。模型学到的是"看到格式 X 就输出 Y"的隐含规则，训练与推理格式一旦错位，规则就全错。

## 2. 最高频的四个模板坑

**坑一：模板家族用错**。不同模型家族的特殊 token 完全不同（ChatML 的 `<|im_start|>`、LLaMA 的 `[INST]`、Gemma 的 `<start_of_turn>`）——2026 案例：Gemma-4 发布了全新模板（`<|turn>user…<turn|>`），而 Unsloth 的旧版 gemma 模板还基于 `<start_of_turn>`——**用旧模板训练了完全错误的格式**，且没有任何报错。修复：换底座必须核对模板家族版本；训练前渲染 20 条人工检查。

**坑二：训练/推理渲染不对称**。Qwen3 案例（2026）：`enable_thinking=False` 时模板只在最后一轮 assistant 附加空的 `<think>` 块，中间工具轮次裸渲染——模型学到"上下文以裸 assistant 标记结尾 → 调用工具；以空 think 块结尾 → 直接总结"的意外规则；推理时 vLLM 在第一轮预填了空 think 块，模型直接幻觉输出最终答案、一个工具都不调（0/6 → 6/6 的修复只需要把 think 块统一到每个 assistant 轮次）。**每个组件单独看都对，只有组合是坏的**。

**这类"对称性"问题的共性教训**：模型学的不是"语义"，是"格式线索"——任何训练与推理不一致的格式差异（哪怕是一个空块、一个标记位置），都会被模型学成行为开关。排查时**不要只检查"训练数据对不对"，要检查"训练与推理的渲染结果是否逐 token 一致"**——这是模板问题的第一排查方向，也是预防的第一检查项。

**坑三：stop token 不在 loss 掩码里**。`assistant_only_loss=True` 时，如果 assistant 的结束 token（EOS）不在 `{% generation %}` 掩码内（被归给下一条消息），**模型推理时无限生成**——TRL 已加检查（is_chat_template_stop_token_trained）。注意旧版 VLM 处理器（Llava/Gemma3/Qwen2-VL）的 assistant token 掩码返回全零（掩码失效），新版（Qwen3-VL 起）才正确传播。

**坑四：特殊 token 双重处理**。文本格式数据里的特殊 token 字符串（`<|im_start|>`）在转 messages 时没反解析为角色，训练时模型看到双重标记（`../数据集体系（非常重要）/06-数据格式与转换.md` 03 节）——症状是输出里出现"标记的标记"。**检出**：渲染后的文本里搜特殊 token 字符串出现两次的位置。

还有一个隐蔽变体：**BOS/EOS 重复**——加载模型时 tokenizer 自动加 BOS、模板渲染又手动加一遍，序列开头出现双重 BOS——症状是输出首 token 异常、生成长度不稳定；渲染 diff 一眼可见（序列开头两个 BOS token）。

## 3. 诊断：三个动作定位模板问题

**渲染 diff**（第一动作）：同一条样本分别用训练模式与推理模式渲染成文本/token 序列，逐字节对比——不一致即模板问题（01 篇第 4 节）。**注意对比的是"实际 token 序列"而非"看起来一样的文本"**（空格/换行/BOS 差异肉眼难辨，token 序列 diff 最可靠）。

**nonce 测试**（快速确认链路）：4 样本 + 不可能标签快测——模型连训练样本的 nonce 都吐不出，模板/掩码/标签必有错（01 篇第 3 节）。

**输出探测**：对微调后模型做"原始输出探测"（不带任何 grammar/约束直接生成）——如果裸生成也是垃圾（自由生成到 token 上限），问题在权重层（训练本身）而非格式层；如果裸生成正常、约束生成崩，问题在格式/约束层。这个区分决定了排查方向（权重问题查 04/05 篇，格式问题查本篇）——**输出探测是模板问题与权重问题的分水岭，先做探测再选方向**。

## 4. 预防：模板检查进启动清单

模板问题"修复一次、预防永久"——把三个检查写进启动前清单（`../完整微调工程流程/06-训练配置与启动.md` 05 节）：

1. **家族核对**：目标模型的官方模板版本 vs 框架内置模板（Unsloth 等框架的内置模板可能滞后新模型——查框架发布说明）。
2. **渲染抽检**：随机 20 条样本渲染（训练模式）人工检查——角色标记、特殊 token、结束标记位置全对。
3. **对称性验证**：训练/推理双模式渲染 diff（或跑 prefix-preserving 属性测试——`render([user, asst_tool_call, tool_result])` 必须以 `render([user, asst_tool_call])` 开头，12 行测试可在毫秒级检查任意 tokenizer；Qwen3 曾因一行 `{%- if loop.last %}` 条件破坏前缀保持）。

三项检查全部脚本化（模板文件 + 渲染对比脚本进 git），**换底座/换模板/换框架时强制重跑**——模板问题"修复一次、预防永久"的前提是检查进了启动清单而不是靠人记得。

## 5. 工具调用格式的专项检查

Agent 类任务（工具微调）的模板问题额外多一层：tool_call 与 tool_result 的角色序列（`../基础概念/08-指令数据与对话格式基础.md` 07 节）——专项检查：tool_call 后必须紧跟 tool_result（顺序错 = 模型学到错误的调用节奏）；arguments 渲染后必须是合法 JSON；工具结果追加后前缀保持（prefix-preserving，见第 4 节）。

2026 年 19 个开源家族里 18 个默认满足前缀保持，Qwen3 是已知例外（一行修复）——**换新家族模型时跑一遍 prefix-preserving 测试是 Agent 任务上线前的必做项**。

> 🎯 **核心要点**：模板不匹配 = 静默杀手（不报错、loss 正常、推理崩）；四坑——家族用错（Gemma4 案例）、训练/推理不对称（Qwen3 think 块案例）、stop token 出掩码（无限生成）、特殊 token 双重处理；三诊断动作——渲染 diff（token 级）、nonce 测试、输出探测（区分权重问题 vs 格式问题）；三预防检查进启动清单——家族核对/渲染抽检/对称性验证（prefix-preserving 测试）；工具调用额外查角色序列与 JSON 合法性。

---

**参考来源**：

- [Check that training chat templates keep the stop token in the loss mask（TRL PR #5988）](https://github.com/huggingface/trl/pull/5988)
- [Two Silent Traps in Agentic LLM Evaluation: Vanishing Tool Calls and Disagreeing Judges（HF Blog）](https://huggingface.co/blog/GurkanOz/agentic-eval-two-traps)
- [Agentic RL: Token-In, Token-Out Done Right（HF Blog）](https://huggingface.co/blog/huggingface/tito)
- [VLM Fine tuning: Near-Zero Training Loss but Poor Inference Accuracy（HF 论坛）](https://discuss.huggingface.co/t/vlm-fine-tuning-near-zero-training-loss-but-poor-inference-accuracy-on-train-set-gemma-4-e2b-it/176224/3)

---

**下一模块**：[03-数据类问题.md](./03-数据类问题.md) / **返回总览**：[00-微调常见问题总览](./00-微调常见问题总览.md)
