# 数据格式与转换

> 数据的四种形态（Alpaca/ShareGPT/ChatML/messages）与转换工程：格式是数据的"接口"，接口错了，模型输出的就是垃圾

## 1. 四种数据形态

社区与工业界常见四种数据格式（概念层的基础认知见 `../基础概念/08-指令数据与对话格式基础.md`，本篇讲工程转换）：

**Alpaca 格式**：单轮指令的标准形态，三个字段——`instruction`（指令）、`input`（可选输入）、`output`（回答）。最简单、兼容性最广（几乎所有微调框架原生支持），但**只有单轮**，多轮对话要拆或转。

**ShareGPT 格式**：多轮对话数组——`{"conversations": [{"from": "human", "value": "..."}, {"from": "gpt", "value": "..."}]}`。来自 ShareGPT 爬取数据的命名习惯，多轮原生支持，但 human/gpt 的角色命名与主流模板不通用，**必须映射**。

**ChatML 格式**：带特殊 token 的文本格式（`<|im_start|>system` ... `<|im_end|>`），Qwen/Yi 等家族原生——直接贴进训练器的文本形态。

**messages 格式**：通用角色消息数组——`[{"role": "system"...}, {"role": "user"...}, {"role": "assistant"...}]`。**2026 年的事实标准**：HuggingFace 生态（datasets、TRL）默认形态，与 tokenizer 的 apply_chat_template 直接对接（模板铁律见 `../基础概念/08-指令数据与对话格式基础.md` 03 节）。

## 2. 转换目标：统一到 messages

生产建议：**所有来源的数据最终统一转成 messages 形态**（JSONL 存储，每行一条样本、UTF-8、schema 版本记录在文件头注释或配套 manifest），训练时再交给 apply_chat_template 渲染成目标模型的 token 序列。为什么统一到 messages 而不是直接存文本：

- **模型无关**：同一份数据可以渲染成任意家族的模板（ChatML/LLaMA/Mistral），换底座不重做数据。
- **多轮原生**：角色数组天然表达多轮对话、工具调用扩展。
- **生态对接**：datasets 库 + SFTTrainer 全链路原生支持。

转换映射表（最常见的五种来源 → messages）：

| 来源格式 | 映射规则 |
|---------|---------|
| Alpaca（instruction/input/output） | system 可空；instruction+input 拼 user；output 为 assistant |
| ShareGPT（human/gpt） | human → user；gpt → assistant；首条可转 system |
| ChatML 文本 | 按特殊 token 反解析回角色数组 |
| CSV/Excel（问答两列） | user/assistant 两角色，system 留空或统一模板 |
| 工具调用数据 | assistant 消息内嵌 tool_call，后续加 tool 角色消息 |

转换实例（Alpaca → messages，最常用的一条）：

```json
{"instruction": "翻译成英文：今天很冷", "input": "", "output": "It is cold today."}
→
{"messages": [
  {"role": "user", "content": "翻译成英文：今天很冷"},
  {"role": "assistant", "content": "It is cold today."}
]}
```

注意实例中的两个细节：input 为空时**不生成空 user 消息**（空消息会污染训练）；instruction 直接作 user 内容、**不追加"请回答"类模板词**（模板词会在训练后要求模型每次都等模板——输出依赖病）。

## 3. 转换工程的五个坑

**system 消息的归属**：来源数据带 system 时，优先保留原始 system（业务上下文）；来源无 system 时，**不要全局塞一个统一 system 模板**（会让模型对 system 过拟合，且与生产真实 system 不一致）——要么每批用真实业务 system，要么留空。同一份数据里 system 出现 10+ 个不同版本是健康信号（07 篇检查清单）。

**input 字段的拼接**：Alpaca 的 instruction 与 input 拼接方式要统一（换行/冒号），**所有样本同一规则**——否则模型学到的是"随机拼接的坏样例"。

**角色命名规范化**：human/gpt、HUMAN/ASSISTANT、user/assistant 各种变体——统一小写规范角色名，转换脚本里做字典映射。

**特殊 token 的双重处理**：文本格式数据里的特殊 token 字符串（`<|im_start|>`）在转 messages 时**必须反解析为角色**，不能留着当普通文本（否则模型看到双重标记）；反向，转回文本时只能用目标模型的模板渲染，不能拼。

**长度与截断**：转换时记录每条样本的 token 长度（用目标 tokenizer 预计算），超长样本标记——供训练时的 max_seq_length 策略（截断/拆分/packing，见 `../基础概念/06-训练循环Epoch-Batch-Step.md` 06 节）决策。

**转换脚本的工程化**：转换必须幂等（同一输入跑两次结果一致）、可重跑（支持断点续跑）；输出与输入分离（绝不原地覆盖），输出文件带转换脚本版本号；全量转换一次完成并生成统计报告（各来源转换条数、失败条数、失败原因分布）——格式转换决定了训练数据的最终形态，是全链路最需要工程化的环节，也是最容易被"随手跑一次"糊弄过去的环节。

## 4. 转换后的验证

转换脚本写完，三条验证线（缺一不可）：

**Schema 校验**：全量跑 messages 合法性——role 在合法集合内、content 非空、每轮 user→assistant 配对完整（缺 assistant 的轮次要处理）、tool 消息紧随 tool_call、无孤立 system（system 只允许出现在首位）。

校验脚本进 CI：数据变更自动触发全量校验（含去重/污染快检），而不是靠人记得跑——格式错误越早发现，返工成本越低。

**往返一致性**：对每条样本做"messages → 模板渲染 → 反解析回 messages"，内容应一致——不一致说明特殊 token 处理有漏洞。抽样 20 条人工检查。

**渲染抽检**：随机 20 条用目标模型 tokenizer 渲染成文本，人工看渲染结果——检查 system 位置、结束 token、assistant 起始标记是否都在正确位置（格式错误的快速排查法见 `../基础概念/08-指令数据与对话格式基础.md` 05 节）。

## 5. 格式与任务的匹配决策

格式不是纯技术选择，它承载任务语义：

- **单轮问答**（分类/抽取/改写）→ Alpaca 起步足够，但生产建议仍转 messages（统一性）。
- **多轮对话**（客服/引导式）→ messages 原生支持，掩码策略选 final-turn only 或 all turns（`../基础概念/08-指令数据与对话格式基础.md` 04 节）。
- **工具调用** → messages + tool_call 扩展字段（`../基础概念/08-指令数据与对话格式基础.md` 07 节）。
- **长文档处理** → messages + 文档段，训练时配合 packing。

一句话：**Alpaca 是入门格式，messages 是生产格式**——转换成本一次投入，后续换模型零成本。

存储与格式决策要进 manifest（09 篇）：存储形态（JSONL 一行动一条，不推荐单文件 JSON 数组——多进程处理与增量追加都不方便）、schema 版本、分段策略（长文档任务）、工具角色 schema——**格式决策记录成文，换人也能复现**，训练出问题能按 manifest 回溯到格式环节。

> 🎯 **核心要点**：四种形态（Alpaca 单轮/ShareGPT 多轮/ChatML 文本/messages 数组），生产统一到 messages（模型无关 + 生态对接）；转换五坑——system 不全局塞模板、input 拼接统一、角色命名规范化、特殊 token 必须反解析、转换时预计算长度；三条验证线（schema/往返一致/渲染抽检）缺一不可。

---

**参考来源**：

- [Qwen 数据格式基础：指令、输入与回答（CSDN）](https://blog.csdn.net/l35633/article/details/161343377)
- [how-to-train-your-gpt: 04_data_preparation.md（GitHub）](https://github.com/bhardwajRahul/how-to-train-your-gpt/blob/master/fine-tuning/04_data_preparation.md)
- [ShareGPT 数据集用于 Mistral 模型微调的实践（php.cn）](https://www.php.cn/faq/2549523.html)

---

**下一模块**：[07-数据质量评估与筛选.md](./07-数据质量评估与筛选.md) / **返回总览**：[00-数据集体系总览](./00-数据集体系总览.md)
