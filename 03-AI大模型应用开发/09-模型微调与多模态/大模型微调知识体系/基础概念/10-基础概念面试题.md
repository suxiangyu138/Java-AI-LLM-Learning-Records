# 基础概念面试题

> 微调面试的第一关是概念题：本质、范式、损失、优化器、训练循环、方法对比——每题给标准答案与答题范式，背熟即得分

## 1. 必问基础 8 题

### Q1：微调的本质是什么？和 Prompt 工程、RAG 的根本区别？

**答案**：微调是在预训练权重基础上用任务数据继续训练，本质是**更新模型权重**；Prompt 工程只改输入文本、RAG 在推理时挂检索管道，二者都不改权重。这个区别推导出微调的三个特性：行为固化进权重（格式稳定、可省长指令）、成本高（数据+算力+评估+治理）、不可逆于单次调用（改错要重训）。所以微调是最后手段：先试不改权重的方案，仍有关键缺口才动权重。

**加分点**：主动接"知识缺口 vs 行为缺口"诊断——知识缺口用 RAG（微调注入知识反而增加幻觉），行为缺口先 Prompt 再微调。

### Q2：大模型训练的经典三阶段是什么？2026 年有什么扩展？

**答案**：经典三段是预训练（万亿级语料 next-token 预测，解决"能力"）、SFT（指令-回答对监督学习，解决"行为"）、偏好对齐（RLHF/DPO，解决"价值观"）。2026 年扩展为完整链条：预训练 → SFT → 偏好优化（DPO/IPO/KTO）→ RL 强化（GRPO/PPO）→ RLVR（可验证奖励）→ 对齐模型。企业落点 99% 在 SFT（+DPO）。

**加分点**：强调"预训练决定能力上限，后训练决定可用性"；指出 SFT 只教模仿不保证对齐。

### Q3：交叉熵损失在 LLM 里怎么计算？初始 loss 大概多少？

**答案**：预训练目标是 next-token 预测——对每个位置，模型输出词表上的概率分布，与真实下一个 token 计算交叉熵（即负对数似然），对所有序列所有位置取平均：Loss = -1/(B×T) × Σ log P。随机初始化时模型输出近似均匀分布，loss ≈ log(vocab_size)，128K 词表约 11.8。困惑度 PPL = exp(loss)，现代预训练模型 6-10。SFT 用同一损失但做 prompt masking（user 位置 label 设 -100）。

### Q4：为什么 LLM 训练用 AdamW？关键超参是什么？

**答案**：AdamW 是 Adam 加**解耦权重衰减**——权重衰减直接作用于权重而不是加进梯度，避免 L2 正则与自适应学习率互相干扰。Adam 的逐参数自适应学习率适合 LLM 这种大规模、梯度稀疏、量级差异大的训练。关键超参：β₁=0.9、β₂=0.95（比 Adam 默认 0.999 更敏感）、ε=1e-8、weight decay=0.1。学习率：7B 预训练 1e-4~3e-4、全量微调 5e-5 量级、LoRA 微调 2e-4 常用起点。

### Q5：warmup 和梯度累积分别是干什么的？

**答案**：warmup（预热）是训练初期用很小的学习率逐步升到峰值，两个原因：训练初期激活/梯度不稳定，直接上大学习率会打爆训练；Adam 的二阶矩估计需要数据积累，预热给足时间再放大大更新。惯例：线性 warmup 占总步数 1-2%。梯度累积是显存装不下大 batch 时的解法：累积 N 个 micro-batch 的梯度后统一更新一次，等效放大 batch size 得到更稳的梯度估计；实现上 loss 要除以累积步数，否则等效学习率被放大。

### Q6：SFT 训几个 epoch？为什么不能多训？

**答案**：1-3 个 epoch 是共识：数据量大 1 个就够，中等 2-3，小数据（<1k）也建议不超过 3。超过 3 个 epoch 收益递减且过拟合风险陡增——模型从"学习泛化规律"转向"记忆训练样本"（能复读原样回答）。判断标准不是训满 3，而是盯验证 loss：train-eval gap 持续拉大就是该停的信号。搭配早停（early stopping）使用。

### Q7：DPO 和 RLHF 的核心区别？GRPO 解决什么问题？

**答案**：RLHF 需要训练奖励模型给回答打分，再用 PPO 优化策略——优点是线上循环、策略可持续进化，缺点是显存翻倍、训练不稳定、易奖励黑客。DPO 把偏好直接写成损失函数（提高 chosen 概率、降低 rejected 概率），**不需要奖励模型**，简单稳定显存省——2026 年主流；缺点是性能受限于静态配对数据。GRPO（DeepSeek-R1）对同一 prompt 采样 8-64 个回答按组内相对优劣算奖励，**砍掉 critic 模型**，显存减半，推理类任务首选。

### Q8：chat template 是什么？为什么不能手写？

**答案**：chat template 是把 system/user/assistant 消息列表序列化为 token 序列的配方，核心是各模型家族的特有特殊 token（ChatML 的 `<|im_start|>`、LLaMA 的 `[INST]`、通用 `<|system|>` 等）。不能手写的原因：模板必须与模型训练时的格式完全一致——用错 token 模型无法识别角色边界，生成垃圾输出。铁律是走官方 API `tokenizer.apply_chat_template(messages, tokenize=True, add_generation_prompt=True)`，且不要修改预训练 tokenizer 自带模板。

## 2. 概念辨析场景题

**场景一**："我们想让模型学会公司内部的 JSON 输出规范，1,000 条样本，怎么选方法？"——行为缺口，SFT 正解：1,000 条指令对是甜点量级（质量>数量），LoRA/QLoRA 微调 2-3 epoch，验证 JSON 格式正确率 + 通用能力回测防遗忘。

**场景二**："训练日志显示 train loss 0.3、eval loss 1.8，且模型能背出训练数据，怎么办？"——过拟合：立即 early stopping，回溯最优 checkpoint（load_best_model_at_end），下次降 epoch/降学习率，检查是否混入重复数据。

**场景三**："微调后模型会回答业务问题，但普通对话变得很怪，什么原因？"——灾难性遗忘：小数据+多 epoch 的典型症状。对策：降 epoch、混入 ~10% 通用对话数据、低学习率、LoRA 冻结底座、评估加入通用能力回测。

**场景四**："微调后模型答业务问题还是幻觉，怎么排查？"——先分清是知识缺口还是行为缺口：知识类问题幻觉（私有/时效信息）是微调本质边界，交给 RAG；格式类问题才归微调管。微调改"怎么输出"，不改"知道什么"。

## 3. 答题范式总结

概念题的标准答题结构（四步）：**下定义**（一句话说清是什么）→ **讲机制**（怎么工作，带公式/参数）→ **做对比**（与相邻概念的区别）→ **给量级**（关键数字：1-3 epoch、2e-4、-100、6-10 PPL）。面试官听到"量级"和"对比"会明显加分——这证明概念不是背的，是可用的。

> 🎯 **核心要点**：基础概念面试 = 8 题全覆盖（本质/三阶段/损失/优化器/训练循环/方法对比/模板/评估）；答题四步 = 定义→机制→对比→量级；场景题先诊断缺口（知识 vs 行为）再选方法；进阶追问会导向边界判断体系（什么时候该微调）与执行层体系（怎么微调）——概念层答得稳，才有资格进入下一层。

---

**参考来源**：

- [Post-Training Pipeline 2026: SFT, DPO, GRPO, and the Rise of Verifiable Rewards（CallSphere）](https://callsphere.ai/blog/post-training-pipeline-2026-sft-dpo-grpo-verifiable-rewards)
- [Epochs: 1-3 typical for SFT（The Neural Base）](https://theneuralbase.com/fine-tuning-fundamentals/learn/beginner/epochs-1-3-typical-for-sft/)
- [LoRA Fine-tuning Hyperparameters Guide（Unsloth）](https://unsloth.ai/docs/get-started/fine-tuning-llms-guide/lora-hyperparameters-guide)
- [OpenAI Fine-Tuning: When and How (2026 Guide)（Respan）](https://www.respan.ai/articles/openai-fine-tuning-guide)

---

**返回总览**：[00-基础概念知识体系总览](./00-基础概念知识体系总览.md)
