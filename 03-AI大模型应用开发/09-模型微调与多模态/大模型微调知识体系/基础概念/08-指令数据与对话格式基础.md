# 指令数据与对话格式基础

> 微调数据不是"随便写写"的文本：指令对、system/user/assistant 三角色、chat template 特殊 token、prompt masking——格式错了，模型直接输出垃圾

## 1. 指令对：最小数据单元

SFT 数据的最小单元是**指令对**（instruction-response pair）：一条指令（instruction，可选带 input）+ 一条标准回答（response）。经典结构：

```json
{
  "instruction": "把这句话翻译成英文：今天天气很好",
  "input": "",
  "output": "The weather is nice today."
}
```

数据集的每条样本最终要标准化为**消息列表**（messages）形态——带角色标签的数组，因为现代模型是按对话训练的。数据集的清洗、构建、平衡细节在 `../数据集体系（非常重要）/` 体系，本篇只建立"数据长什么样"的概念。

## 2. 三角色：system / user / assistant

指令微调模型的对话由三个角色组成：

- **system**：首条常驻指令，设定行为/风格/安全约束（"你是 XX 客服，语气专业，不回答与业务无关的问题"），通常对终端用户隐藏。
- **user**：人类输入，即请求本身。
- **assistant**：模型输出，**训练时损失只聚焦预测这个角色的 token**。

为什么需要角色：预训练模型只会"续写"，角色标记让它学会"看到 user 就停，该我 assistant 说话了"。**使用错误的模板会导致模型无法识别格式、生成垃圾输出**——这是微调新手第一大坑：数据格式与模型期望的 chat template 不匹配。

## 3. chat template：角色消息 → token 序列

**chat template** 是把消息列表序列化为统一 token 序列的"配方"——不同模型家族的配方不同，核心是各自的**特殊 token**（special tokens）：

| 模型家族 | 特殊 token 示例 |
|---------|----------------|
| ChatML（Qwen 等） | `<|im_start|>system`、`<|im_end|>` |
| LLaMA | `[INST]` / `[/INST]` |
| Mistral | `<s>[INST]` / `[/INST]` |
| 通用开源 ChatML | `<|system|>`、`<|user|>`、`<|assistant|>`、`<|end|>` |

**铁律**：不要手写模板、不要修改预训练 tokenizer 自带的模板——用官方 API：`tokenizer.apply_chat_template(messages, tokenize=True, add_generation_prompt=True)`。`add_generation_prompt=True` 会在末尾追加 assistant 起始标记，告诉模型"轮到你了"。自 transformers 4.46 起该 API 原生支持生成训练 labels。推理评测（如 needle-in-a-haystack）也必须走 apply_chat_template 格式化 prompt，否则指令微调模型可能得 0 分。

一个具体例子，看清模板展开的形态（以 ChatML 家族为例，`add_generation_prompt=True`）：

```text
输入 messages：
[{"role": "system", "content": "你是客服，回答简短"},
 {"role": "user", "content": "运费多少？"}]

模板展开后的 token 序列：
<|im_start|>system\n你是客服，回答简短<|im_end|>\n
<|im_start|>user\n运费多少？<|im_end|>\n
<|im_start|>assistant\n
```

模型见到末尾的 `<|im_start|>assistant` 就知道"轮到我输出了"——训练时 `add_generation_prompt` 不加（最后一段 assistant 回答是标签，不在输入里），推理时加。

## 4. prompt masking：只学 assistant

上一节说的"损失只聚焦 assistant token"在实现上就是 **prompt masking**：把 system/user 位置（以及部分策略下的非目标 assistant 轮次）的 label 设为 **-100**（PyTorch 交叉熵忽略索引），这些位置不产生梯度、不被学习。

多轮对话的两种掩码策略：**final-turn only**（只训练最后一轮 assistant 回答）——省算力、防止模型在早期轮次浪费容量，多数任务默认；**all turns**（训练所有 assistant 轮次）——长对话/多轮任务更稳。

TRL 等框架用 `{% generation %}` 标记包裹目标输出来实现 `assistant_only_loss`——选择时看任务形态：最终结果型任务用 final-turn only，多轮流程型任务（客服、引导式对话）用 all turns。

## 5. token 与分词：算账的基础单位

token 是模型处理文本的基本单位（中英文混合下约 1 汉字 ≈ 1-2 token，1 英文单词 ≈ 1-1.5 token）。分词由 tokenizer 完成，现代主流是子词分词（BPE/SentencePiece 等）——把文本切成词表内可表示的 token 序列，词表与分词器是模型固有的（随预训练固定），**微调阶段不要改词表**。token 概念在微调中的三个用途：

**算成本**：训练/推理按 token 计费，max_seq_length、batch、显存预算都按 token 算。

**定长度**：`max_seq_length` 决定样本截断——超长样本要么截断（丢信息）要么用 packing（多轮样本拼接为等长序列，拼接处用 eos token 分隔、position_ids 正确重置）提升利用率。

**查模板**：训后验证时检查首字符是否为 assistant 角色标记、末尾是否含结束 token——格式出错的快速排查法。

## 6. 数据量级参考（概念版）

| 数据量 | 适用任务 | 说明 |
|:------:|---------|------|
| 100 条 | 底线 | 可能无法泛化，仅当 Prompt 完全无效时试 |
| 1,000 条 | 分类/简单 QA | 多数简单任务的甜点 |
| 5,000 条 | 指令遵循/摘要 | 覆盖性明显提升 |
| 10,000+ 条 | 复杂任务 | 之后收益递减，先查质量问题 |

**质量 > 数量**：1,000 条干净数据常胜过 10,000 条噪声数据（2026 年各指南一致强调）。平衡要点：约 10% 普通对话数据可缓解灾难性遗忘（机制见 09 篇）；同一指令配 3-4 种措辞变体增强鲁棒性；90/10 划分训练/验证，验证集与训练集严格隔离且包含边界样本；去重必做——重复样本在训练中反复出现等于无形中提高权重，还会在验证集泄漏时伪造 eval loss。完整方法论在 `../数据集体系（非常重要）/` 体系。

## 7. 工具调用数据格式（2026 Agent 时代）

2026 年工具调用（Function Calling）成为微调数据集的标准组成部分——微调模型要学的不只是"回答"，还有"调用工具"：

**格式**：对话中插入工具角色与特殊 token——通用 ChatML 扩展用 `<|tool_call|>`、`<|tool_result|>` 标记工具调用与结果；Qwen 家族用 `<|tool_calls|>`；部分家族用 JSON 结构内嵌 `{"name": "search", "arguments": {...}}`。训练时 tool_call 内容作为 assistant 输出的一部分学习，tool_result 属于"系统事实"（类似 user 位置，可掩码或按框架默认处理）。

**与主模型分工**：底座模型若原生支持工具（如 Qwen 系列），微调只需校准调用格式与参数习惯；若底座不支持，微调可以"教会"结构化的工具调用序列——这是 Agent 系统（`03-AI大模型应用开发/03-Agent与MCP协议/`）的模型侧基石。2026 年各框架（TRL 已支持 Cohere/DeepSeek/Qwen2.5 等模板家族的 assistant-only 掩码）把工具数据纳入标准 SFT 流程，无需手写特殊处理。

**避坑**：工具调用数据里 arguments 必须是**合法 JSON**（模型输出解析失败是 Agent 链路最常见的错误源）；工具结果要完整（截断的结果会让模型学不到正确用法）；工具名与参数名全数据保持一致，不要同义混用。

> 🎯 **核心要点**：数据最小单元 = 指令对（instruction/output），标准形态 = 消息列表；system/user/assistant 三角色由 chat template 序列化，特殊 token 各家族不同——必须用 apply_chat_template、禁止手写；prompt masking（label=-100）让模型只学 assistant；工具数据 arguments 必须合法 JSON；数据量 100 底线/1k 分类/5k 指令/10k+ 复杂，质量永远大于数量。

---

**参考来源**：

- [Qwen 数据格式基础：指令、输入与回答（CSDN）](https://blog.csdn.net/l35633/article/details/161343377)
- [full-structured-instruction-sft-dataset（HuggingFace, 2026-03）](https://huggingface.co/datasets/mdonigian/full-structured-instruction-sft-dataset/blob/main/README.md)
- [how-to-train-your-gpt: 04_data_preparation.md（GitHub）](https://github.com/bhardwajRahul/how-to-train-your-gpt/blob/master/fine-tuning/04_data_preparation.md)
- [Add Cohere training chat template（TRL PR #5627）](https://github.com/huggingface/trl/pull/5627)

---

**下一模块**：[09-评估基础损失曲线与过拟合.md](./09-评估基础损失曲线与过拟合.md) / **返回总览**：[00-基础概念知识体系总览](./00-基础概念知识体系总览.md)
