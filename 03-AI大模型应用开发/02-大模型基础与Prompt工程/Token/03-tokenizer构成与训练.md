# 03 - tokenizer 构成与训练

> 定位：tokenizer 的内部结构——词表、特殊 token、训练流程——"一个 tokenizer = 一份词表 + 一套切分规则 + 一组特殊 token；训练是'语料 → 统计 → 合并'的流水线，推理是'查表'"

---

## 📚 目录

1. [tokenizer 的三件套](#1-tokenizer-的三件套)
2. [词表结构](#2-词表结构)
3. [特殊 token 家族](#3-特殊-token-家族)
4. [训练流程](#4-训练流程)
5. [词表大小选型](#5-词表大小选型)
6. [glitch token 与词表安全](#6-glitch-token-与词表安全)
7. [五个常见坑](#7-五个常见坑)
8. [练习 5 题](#8-练习-5-题)

---

## 1. tokenizer 的三件套

一个 tokenizer 由三部分组成：**词表**（vocab：token → ID 的映射表）、**切分规则**（合并规则/正则 pretokenize 规则，02 篇的算法产物）、**特殊 token**（BOS/EOS/PAD 等结构标记）。**训练时三件套一起产出，使用时三件套一起加载**——这就是为什么"tokenizer 文件"通常是一个目录（如 LLaMA 的 tokenizer.model + tokenizer_config.json）而不是一个文件：**"词表是字典，规则是语法，特殊 token 是标点——缺一不可"**。模型加载链上 tokenizer 与模型权重**必须配套**（同一个训练产物），混用直接错位（08 篇）。

## 2. 词表结构

**词表 = token → ID 的双向映射**（encode 查 token→ID、decode 查 ID→token）。几个结构性事实：

**其一，ID 空间是连续的**——从 0 到 vocab_size-1，**空位 = 浪费 embedding 显存**（embedding 表按 vocab_size 分配）；**其二，排序有讲究**——GPT 系把字节/常用子词放前、特殊 token 放后，**ID 顺序不影响语义但影响"加新 token"的兼容性**（在词表尾部追加是安全做法）；**其三，词表 ≠ 全量字符**——byte-level 词表里"中文单字"是字节合并的产物，**覆盖密度由语料决定**（中文语料占比高，中文字词条就多，05 篇）；**其四，词表大小直接决定 embedding 层参数量**——50K 词表 × 4096 维 ≈ 2 亿参数，**"词表膨胀 = 显存与算力成本"**（选型看第 5 节）；**其五，词表文件的形态随库而异**——HF 的 tokenizer.json（统一 JSON 形态）、SentencePiece 的 .model（二进制）、BPE 的 merges.txt（合并规则表）——**HF 仓库里 tokenizer.json 是 2026 事实标准**（04 篇加载）。

## 3. 特殊 token 家族

| 特殊 token | 作用 | 代表写法 |
|-----------|------|---------|
| BOS（begin of sequence） | 序列开始标记 | `<s>`/`<|begin_of_text|>` |
| EOS（end of sequence） | 序列结束标记（生成停止信号） | `</s>`/`<|end_of_text|>` |
| PAD | 批内对齐（padding） | `<pad>` |
| UNK | 未知（byte-level 时代几乎绝迹） | `<unk>` |
| 工具/角色 token | 对话结构（角色分隔/工具调用边界） | `<|user|>`/`<|tool|>` |
| 保留 token | 预留扩展位 | `<reserved_0>` 等 |

**两个关键认知**：其一，**EOS 是生成循环的停止信号**——模型输出 EOS token 时停止生成（max_new_tokens 之外的第二重保险）；**生成 API 的 stop 参数本质上就是"遇到这些 token 就停"**；其二，**角色/工具 token 是"模型的语言"**——ChatML（`<|im_start|>` 系）与 Llama 系（`<|begin_of_text|>` 系）结构不同，**prompt 格式必须用模型自己的模板**（`apply_chat_template` 的职责，HuggingFace 体系交叉）——"**模板错了，模型读不懂你的对话结构**"。

## 4. 训练流程

**训练 tokenizer = 从语料学"切分规则 + 词表"**，五步流水线：

```text
① 语料准备：尽量覆盖目标语言/领域（tokenizer 的词表密度由语料决定）
② 预分词（pretokenize）：正则把文本切成"候选单元"
   （英文按空格/标点切；中文按字切——这是语言差异的第一层）
③ 统计与合并：BPE 反复合并最频繁相邻对（02 篇），直到目标词表大小
④ 特殊 token 追加：BOS/EOS/PAD/工具 token 追加进词表
⑤ 产出文件：tokenizer.model（规则 + 词表）+ config（特殊 token ID 等）
```

**三个要点**：其一，**预分词是语言差异的第一道门**——英文语料预分词后是"单词 + 空格"候选，中文是"单字"候选，**"语料语言结构决定了词表长什么样"**；其二，**特殊 token 在训练后追加**——合并规则不会自己学出 `<|user|>`，**追加保证它们不可被合并拆开**（保留为独立词条）；其三，**训练完的 tokenizer 是"冻结的"**——部署期只查表不改表（改表 = 重训模型，除非做增量词表扩展）。**"训练一次，冻结一生"是 tokenizer 的宿命**——这也是 08 篇"版本纪律"的根源。

## 5. 词表大小选型

| 词表规模 | 代表 | 特征 |
|---------|------|------|
| 32K-50K | GPT-2（50,257）/Llama-2（32K） | 经典规模，英文主导 |
| 100K-150K | LLaMA-3（128K）/Qwen 系 | 多语言 + 代码覆盖 |
| 200K 级 | o200k_base（GPT-4o/GPT-5 系） | 多语言 + 符号 + 工具全都要 |

**权衡三角**：**词表大 → 文本 token 少（省钱）但 embedding 层大（占显存/算力）**；词表小 → 省显存但 token 多（费钱费序列）。**2026 主流趋势：100K-200K 级**——多语言是刚需、embedding 显存在大显存时代可接受。**选型心法**：**tokenizer 跟着模型走，选模型就是选词表**——"别自己训 tokenizer，除非你是模型团队"（08 篇）。

## 6. glitch token 与词表安全

**glitch token（故障 token）是 2026 年新增的安全话题**：词表里有些 token 在训练语料中出现次数趋近于零（近零样本），**模型对这些 token 的表示"没学好"**——输入它们可能触发异常输出，甚至被用作对抗触发词（攻击者把 glitch token 混进 prompt 干扰模型）。

**2026 缓解实践**：**训练时过滤近零样本词条**（词表构建阶段就清理）；**运行时维护 per-tokenizer 的 deny-list**（拒绝词表，可与内容安全过滤词表复用一套——"一套词表两用"）；**安全团队扫描词表**（找出"没人用过但存在于词表"的条目）。**给工程者的启示**：**"词表不是越大越好"多了一层理由**——大词表的尾部（保留位/低频位）就是 glitch token 的温床；生产环境的 prompt 过滤词表（内容安全）与 token 级 deny-list 可以合并治理。

## 7. 五个常见坑

- **坑一**：手工往词表里塞 token 不重训——**ID 空间变化 = 模型 embedding 错位**；要扩展走"词表尾部追加 + embedding 初始化"的专业流程（08 篇）；
- **坑二**：生成时只靠 max_new_tokens 不设 stop——**EOS 之外的角色 token 也要停**（工具调用场景），否则模型自己演双簧；
- **坑三**：模板用错（ChatML 换 Llama 系）——**角色结构错乱；用 apply_chat_template**（HuggingFace 体系交叉）；
- **坑四**：忽略词表大小对 embedding 显存的影响——**小显存场景优先小词表模型**；
- **坑五**：用 50K 词表时代的经验估 200K 词表的 token 数——**词表越大同文本 token 越少**，估算系数要按实际编码测（04/05 篇）。

## 8. 练习 5 题

1. tokenizer 三件套是什么？为什么"词表是字典、规则是语法、特殊 token 是标点"？
2. 词表大小为什么影响显存？ID 空间连续意味着什么？
3. 特殊 token 家族各自的作用？EOS 在生成循环里扮演什么？
4. 训练流程五步是什么？预分词为什么是语言差异第一道门？
5. glitch token 是什么？2026 的三个缓解实践？

> 🎯 **核心要点**：tokenizer = **词表 + 切分规则 + 特殊 token**；训练是"语料 → 预分词 → 合并 → 追加特殊 token"的流水线，**"训练一次，冻结一生"**；词表大小是"token 数 vs 显存"的权衡，**2026 主流 100K-200K 级**；glitch token 让"词表安全"成了新话题。

---

**下一模块**：[04-编码解码实战.md](04-编码解码实战.md) / **返回总览**：[00-Token总览.md](00-Token总览.md)
