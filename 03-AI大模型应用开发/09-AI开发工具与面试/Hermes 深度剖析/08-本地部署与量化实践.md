# 08 - 本地部署与量化实践

> 🎯 Hermes 系列支持从 3B 到 405B 的全尺寸模型，覆盖树莓派到云端集群。掌握 Ollama / vLLM / GGUF 三种部署路径和量化策略，是让 Hermes 落地的关键一步

---

## 目录

1. [部署路径全景](#1-部署路径全景)
2. [Ollama 快速部署](#2-ollama-快速部署)
3. [vLLM 生产级部署](#3-vllm-生产级部署)
4. [GGUF 量化部署](#4-gguf-量化部署)
5. [量化策略对比](#5-量化策略对比)
6. [硬件选型指南](#6-硬件选型指南)

---

## 1. 部署路径全景

```text
选择部署路径
├── 🏠 个人开发 / 学习
│   └── Ollama → 一行命令，开箱即用
│
├── 🏢 生产环境 / 高并发
│   └── vLLM → 高吞吐、PagedAttention、OpenAI 兼容 API
│
├── 💻 低配置设备 / 边缘端
│   └── GGUF + llama.cpp → CPU 推理、极致量化
│
├── ☁️ 云端 / API 消费
│   └── OpenRouter / Together AI / DeepInfra → 免部署
│
└── 📦 混合方案
    └── Ollama(开发) → vLLM(生产) → GGUF(边缘)
```

## 2. Ollama 快速部署

### 2.1 一行命令启动

```bash
# 安装 Ollama
# Windows: https://ollama.com/download
# Linux: curl -fsSL https://ollama.com/install.sh | sh

# 拉取并启动 Hermes 3 8B
ollama run hermes3:8b

# 拉取 DeepHermes 3 8B
ollama run deephermes3:8b
```

### 2.2 可用模型列表

| Ollama 模型名 | 对应 HuggingFace 模型 | 大小 | 量化 |
|-------------|---------------------|:---:|:---:|
| `hermes3:3b` | Hermes-3-Llama-3.2-3B | ~2GB | Q4_K_M |
| `hermes3:8b` | Hermes-3-Llama-3.1-8B | ~5GB | Q4_K_M |
| `hermes3:70b` | Hermes-3-Llama-3.1-70B | ~40GB | Q4_K_M |
| `deephermes3:8b` | DeepHermes-3-Llama-3-8B | ~5GB | Q4_K_M |

### 2.3 Ollama API 调用（OpenAI 兼容）

```python
from openai import OpenAI

client = OpenAI(
    base_url="http://localhost:11434/v1",
    api_key="ollama"  # Ollama 不校验，随便填
)

# 普通对话
response = client.chat.completions.create(
    model="hermes3:8b",
    messages=[
        {"role": "system", "content": "You are Hermes, a helpful AI."},
        {"role": "user", "content": "解释一下什么是 JIT 编译"}
    ]
)
print(response.choices[0].message.content)

# Function Calling
response = client.chat.completions.create(
    model="hermes3:8b",
    messages=[{"role": "user", "content": "北京今天天气怎么样？"}],
    tools=[{
        "type": "function",
        "function": {
            "name": "get_weather",
            "description": "获取指定城市的天气",
            "parameters": {
                "type": "object",
                "properties": {
                    "city": {"type": "string", "description": "城市名"}
                },
                "required": ["city"]
            }
        }
    }]
)
```

### 2.4 自定义 Modelfile

```dockerfile
# Modelfile
FROM hermes3:8b

# 设置系统提示词
SYSTEM """
You are a Java backend expert. Always provide code examples in Java 17+.
Use Spring Boot conventions. Prefer concise, production-ready code.
"""

# 调整参数
PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER num_ctx 8192

# 创建
# ollama create java-hermes -f Modelfile
# ollama run java-hermes
```

## 3. vLLM 生产级部署

### 3.1 标准部署

```bash
# 安装
pip install vllm

# 启动 OpenAI 兼容 API 服务
python -m vllm.entrypoints.openai.api_server \
  --model NousResearch/Hermes-3-Llama-3.1-8B \
  --dtype auto \
  --max-model-len 32768 \
  --gpu-memory-utilization 0.90 \
  --enable-auto-tool-choice \
  --tool-call-parser hermes \
  --max-num-seqs 8 \
  --port 8000
```

### 3.2 FP8 量化版本（Hermes 3 70B 推荐）

```bash
python -m vllm.entrypoints.openai.api_server \
  --model NousResearch/Hermes-3-Llama-3.1-70B-FP8 \
  --quantization fp8 \
  --dtype auto \
  --max-model-len 32768 \
  --enable-auto-tool-choice \
  --tool-call-parser hermes
```

> 💡 Neural Magic 的 FP8 量化将 Hermes 3 405B 的 VRAM 需求从 ~810GB 砍到 ~400GB，使得在单节点（8×H100）上运行成为可能

### 3.3 性能调优参数

| 参数 | 默认 | 建议值 | 作用 |
|------|:---:|:---:|------|
| `--max-num-seqs` | 256 | 4-16 | 并发请求数（越大延迟越高） |
| `--gpu-memory-utilization` | 0.90 | 0.85-0.95 | KV Cache 可用显存比例 |
| `--max-model-len` | 自动 | 最大上下文长度 | 超过会截断 |
| `--enable-prefix-caching` | ❌ | ✅ 开启 | 相同前缀复用 KV Cache |
| `--enable-chunked-prefill` | ❌ | 高并发✅ | 分块预填充，降低首 token 延迟 |

## 4. GGUF 量化部署

### 4.1 llama.cpp 推理

```bash
# 下载 GGUF 量化文件
# HuggingFace: NousResearch/Hermes-3-Llama-3.1-8B-GGUF

# 使用 llama.cpp
git clone https://github.com/ggerganov/llama.cpp
cd llama.cpp && make -j

# CPU 推理（仅用 CPU）
./main \
  -m Hermes-3-Llama-3.1-8B-Q4_K_M.gguf \
  -p "<|im_start|>user\nHello<|im_end|>\n<|im_start|>assistant\n" \
  -n 256 \
  -t 8 \
  --temp 0.7

# GPU 加速（Metal/CUDA）
./main \
  -m model.gguf \
  -ngl 33 \          # 33 层放 GPU（8B 模型全部层）
  -c 4096 \          # 上下文长度
  -n 512
```

### 4.2 GGUF 量化格式对比

| 格式 | 质量 | 大小 (8B) | 适用 |
|------|:---:|:---:|------|
| Q2_K | 最低 | ~3GB | 极限压缩 |
| Q3_K_M | 低 | ~3.8GB | 嵌入式设备 |
| **Q4_K_M** | **推荐** | **~5GB** | **日常使用** |
| Q5_K_M | 高 | ~5.8GB | 重要任务 |
| Q6_K | 很高 | ~6.8GB | 质量优先 |
| Q8_0 | 接近 FP16 | ~8.5GB | 评估对比 |
| F16 | 无损 | ~16GB | 基准测试 |

> 🎯 **Q4_K_M 是性价比之王**：质量几乎无感知损失，大小仅 ~5GB，8GB 显存的 RTX 3060/4060 完美运行

## 5. 量化策略对比

| 量化方法 | 典型压缩比 | 质量损失 | 硬件支持 | 框架 |
|---------|:---:|:---:|------|------|
| **FP8** (Neural Magic) | 2x | 极小 | H100/B200 | vLLM, SGLang |
| **GPTQ** (4-bit) | 4x | 小 | CUDA GPU | AutoGPTQ, vLLM |
| **AWQ** (4-bit) | 4x | 小 | CUDA GPU | AutoAWQ, vLLM |
| **GGUF Q4_K_M** | 4.5x | 小 | CPU + GPU offload | llama.cpp, Ollama |
| **bitsandbytes 4bit** | 4x | 小 | CUDA GPU | HuggingFace |
| **GGUF Q2_K** | 7x | 大 | 任何设备 | llama.cpp |

## 6. 硬件选型指南

```text
                  ┌─────────────────────────────────────┐
Hermes 模型        │  GPU 显存需求（Q4_K_M 量化）          │
───────────────────┼─────────────────────────────────────┤
Hermes 3 3B        │  ~2GB  ████                        │ 树莓派 5 (8GB)
Hermes 3 8B        │  ~5GB  ██████████                  │ RTX 3060 12GB ✅
DeepHermes 3 8B    │  ~5GB  ██████████                  │ RTX 4060 8GB ✅
Hermes 3 70B       │ ~40GB  ████████████████████████████ │ RTX A6000 48GB
DeepHermes 24B     │ ~14GB  ██████████████████████████   │ RTX 4090 24GB ✅
Hermes 3 405B      │ ~200GB ████████████████████████████ │ 8×H100 集群
                   └─────────────────────────────────────┘
```

| 硬件配置 | 可运行模型 (Q4_K_M) | 推荐场景 |
|---------|-------------------|---------|
| 笔记本电脑 (无独显) | 3B (CPU) | 学习/轻量 Agent |
| RTX 3060 12GB | 8B 全量，24B 部分 | 个人开发主力 |
| RTX 4090 24GB | 8B/24B 全量，70B Q2 | 高性能开发 |
| MacBook M2 16GB | 8B (Metal) | 移动开发 |
| Mac Studio M2 Ultra 192GB | 70B 全量 | 高性能本地推理 |
| A100 80GB | 70B 全量 FP16 | 企业级部署 |
| 8×H100 集群 | 405B FP8 | 大规模服务 |

## 核心要点回顾

- 三种部署路径：Ollama（开发快速）→ vLLM（生产高并发）→ GGUF（边缘端/CPU）
- Ollama = `ollama run hermes3:8b`，API 完全兼容 OpenAI
- vLLM 必须加 `--tool-call-parser hermes` 才能正确解析 Function Calling
- GGUF Q4_K_M 是性价比之王：5GB 文件 8B 模型，质量几乎无损
- Hermes 3 405B 通过 FP8 量化从 810GB → 400GB，单节点可跑
- 硬件推荐：3060 12GB = 入门，4090 24GB = 舒适，A100 80GB = 专业

## 参考资料

1. Ollama 官方 - Hermes 3 / DeepHermes 3 模型页面
2. vLLM 官方文档 - Tool Calling Parser 配置
3. HuggingFace - NousResearch Hermes GGUF 模型集合
4. Lambda AI - Hermes 3 405B FP8 量化技术博客
