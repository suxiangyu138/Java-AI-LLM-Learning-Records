# 05 - 指令数据构建与清洗

> 🎯 微调效果 80% 取决于数据质量 — 1000条高质量指令 > 10万条噪声数据。数据工程是微调中最被低估的环节

## 数据格式

### Alpaca 格式（SFT 最常用）

```json
[
  {
    "instruction": "将以下 Java 代码改为 Stream API",
    "input": "List<String> result = new ArrayList<>(); for(String s : list) { if(s.length() > 3) result.add(s); }",
    "output": "List<String> result = list.stream().filter(s -> s.length() > 3).collect(Collectors.toList());"
  }
]
```

### ShareGPT 格式（多轮对话）

```json
[{
  "conversations": [
    {"from": "human", "value": "什么是 Java 多态？"},
    {"from": "gpt", "value": "多态是指..."},
    {"from": "human", "value": "能举个例子吗？"},
    {"from": "gpt", "value": "比如 List 接口..."}
  ]
}]
```

## 数据质量检查

```python
def validate_dataset(data):
    issues = []
    for i, item in enumerate(data):
        # ① 长度检查
        if len(item["output"]) < 10:
            issues.append(f"[{i}] 输出太短")
        if len(item["output"]) > 4096:
            issues.append(f"[{i}] 输出太长")
        
        # ② 重复检查
        if item["instruction"] == item.get("input", ""):
            issues.append(f"[{i}] instruction=input")
        
        # ③ 语言一致性（中英混用检查）
        if has_mixed_language(item["output"]):
            issues.append(f"[{i}] 中英混杂")
        
        # ④ 格式检查
        if "{" in item["output"] and not is_valid_json_extract(item["output"]):
            issues.append(f"[{i}] JSON 格式异常")
    
    return issues
```

## 数据增强

```python
# ① 回译增强 → 生成同义不同表述
def back_translate(text, src="zh", tgt="en"):
    en = translator.translate(text, src, tgt)
    return translator.translate(en, tgt, src)

# ② LLM 生成 → 用强模型"教"弱模型
def generate_by_gpt4(instruction):
    """用 GPT-4 生成高质量回答 → 作为微调数据训练小模型"""
    return gpt4_client.chat(instruction)

# ③ 模板多样性 → 同一任务不同表述
templates = [
    "请解释 {topic}",
    "帮我理解 {topic} 的概念",
    "{topic} 是什么？用简单的话说明",
    "作为 Java 架构师，请详细说明 {topic}",
]
```

## 数据配比策略

```text
SFT 数据配比建议：

  通用对话:     40%  (保持模型通用能力)
  目标任务:     40%  (领域适配核心)
  代码:         10%  (保持代码能力)
  数学/推理:     5%  (保持推理能力)
  安全/拒绝:     5%  (保持安全对齐)
```

## 数据来源与成本

| 来源 | 质量 | 成本 | 适用 |
|------|:---:|:---:|------|
| 人工标注 | ⭐⭐⭐⭐⭐ | $$$$ | 领域专有 |
| GPT-4 生成 | ⭐⭐⭐⭐ | $$ | 通用增强 |
| 开源数据集 | ⭐⭐⭐ | 免费 | 基础训练 |
| 用户反馈 | ⭐⭐⭐⭐ | 免费 | 持续优化 |
| 日志挖掘 | ⭐⭐⭐ | 免费 | 长尾补充 |
