# 07 - 测试与部署

> 🎯 TestClient 单元测试 + pytest 集成测试 + Docker 容器化 + Gunicorn/Uvicorn 生产部署 = 完整工程闭环

---

## 目录

1. [TestClient 测试框架](#1-testclient-测试框架)
2. [依赖覆盖与 Mock](#2-依赖覆盖与-mock)
3. [异步测试与数据库](#3-异步测试与数据库)
4. [Docker 容器化](#4-docker-容器化)
5. [生产部署配置](#5-生产部署配置)

---

## 1. TestClient 测试框架

### 1.1 基础测试

```python
"""
FastAPI TestClient — 基于 httpx/Starlette 的同步测试客户端
"""
from fastapi.testclient import TestClient
from main import app

client = TestClient(app)


def test_read_root():
    """最简单的测试"""
    response = client.get("/")
    assert response.status_code == 200
    assert response.json() == {"message": "Hello, FastAPI!"}


def test_create_item():
    """POST + JSON Body"""
    response = client.post(
        "/items/",
        json={"name": "Test Item", "price": 9.99},
    )
    assert response.status_code == 201
    data = response.json()
    assert data["name"] == "Test Item"
    assert data["price"] == 9.99


def test_validation_error():
    """参数校验失败"""
    response = client.post(
        "/items/",
        json={"name": ""},       # name 为空 — 违反 min_length=1
    )
    assert response.status_code == 422
    errors = response.json()["detail"]
    assert any("name" in str(e["loc"]) for e in errors)


def test_not_found():
    """404 错误"""
    response = client.get("/items/99999")
    assert response.status_code == 404


def test_query_params():
    """查询参数"""
    response = client.get("/search?q=test&page=1&size=10")
    assert response.status_code == 200


def test_headers():
    """自定义 Header"""
    response = client.get(
        "/users/me",
        headers={"Authorization": "Bearer test-token-xxx"},
    )
    assert response.status_code in (200, 401)
```

### 1.2 AI 应用的测试用例

```python
def test_chat_stream():
    """流式 LLM 响应测试"""
    with client.stream(
        "POST",
        "/api/chat/stream",
        json={"messages": [{"role": "user", "content": "Hello"}], "model": "gpt-4o"},
    ) as response:
        assert response.status_code == 200

        chunks = []
        for line in response.iter_lines():
            if line.startswith("data: "):
                data = line[6:]
                if data == "[DONE]":
                    break
                chunks.append(data)

        assert len(chunks) > 0
        # 验证每个 chunk 是有效 JSON
        for chunk in chunks:
            import json
            json.loads(chunk)


def test_rag_upload():
    """RAG 文档上传测试"""
    with open("tests/fixtures/sample.txt", "rb") as f:
        response = client.post(
            "/api/rag/upload",
            files={"file": ("sample.txt", f, "text/plain")},
            headers={"Authorization": f"Bearer {get_test_token()}"},
        )
    assert response.status_code == 200
    data = response.json()
    assert data["status"] == "processing"
    assert "doc_id" in data
```

---

## 2. 依赖覆盖与 Mock

### 2.1 覆盖依赖注入

```python
"""
测试中替换真实依赖为 Mock
"""
from fastapi import FastAPI
from main import app, get_current_user, get_db


# ===== Mock 用户 =====
MOCK_USER = User(
    id=1,
    username="testuser",
    email="test@example.com",
    is_active=True,
)


async def override_get_current_user():
    return MOCK_USER


# 覆盖依赖
app.dependency_overrides[get_current_user] = override_get_current_user


def test_with_mock_user():
    """使用 Mock 用户测试"""
    response = client.get("/users/me")
    assert response.status_code == 200
    assert response.json()["username"] == "testuser"


# ===== Mock 数据库 =====
@pytest.fixture
def mock_db_session():
    """创建 Mock 数据库 Session"""
    session = AsyncMock(spec=AsyncSession)
    session.get.return_value = MOCK_USER
    session.execute.return_value.scalar_one_or_none.return_value = MOCK_USER
    return session


async def override_get_db():
    return mock_db_session()


app.dependency_overrides[get_db] = override_get_db
```

### 2.2 HTTP Mock

```python
"""
Mock 外部 LLM API 调用
"""
import respx  # pip install respx
from httpx import Response


@respx.mock
def test_chat_with_mock_llm():
    """Mock LLM API，不实际调用"""
    # Mock OpenAI API
    respx.post("https://api.openai.com/v1/chat/completions").mock(
        return_value=Response(
            200,
            json={
                "id": "chatcmpl-123",
                "object": "chat.completion",
                "choices": [
                    {
                        "index": 0,
                        "message": {
                            "role": "assistant",
                            "content": "你好！我是 AI 助手。"
                        },
                        "finish_reason": "stop",
                    }
                ],
                "usage": {
                    "prompt_tokens": 10,
                    "completion_tokens": 5,
                    "total_tokens": 15,
                },
            },
        )
    )

    response = client.post(
        "/api/chat",
        json={
            "model": "gpt-4o",
            "messages": [{"role": "user", "content": "Hello"}],
        },
    )

    assert response.status_code == 200
    assert "AI 助手" in response.json()["content"]
```

---

## 3. 异步测试与数据库

### 3.1 Async 测试

```python
"""
异步测试 — 使用 pytest-asyncio
"""
import pytest
from httpx import AsyncClient, ASGITransport
from main import app


@pytest.fixture
async def async_client():
    """创建异步测试客户端"""
    transport = ASGITransport(app=app)
    async with AsyncClient(transport=transport, base_url="http://test") as client:
        yield client


@pytest.mark.asyncio
async def test_async_endpoint(async_client):
    """异步测试"""
    response = await async_client.get("/api/chat/models")
    assert response.status_code == 200


@pytest.mark.asyncio
async def test_concurrent_requests(async_client):
    """并发请求测试"""
    import asyncio

    async def make_request(i: int):
        return await async_client.get(f"/items/{i}")

    results = await asyncio.gather(*[make_request(i) for i in range(10)])

    statuses = [r.status_code for r in results]
    assert statuses.count(200) + statuses.count(404) == 10  # 全部完成
```

### 3.2 测试数据库

```python
"""
使用测试数据库（SQLite 内存 或 独立 PG）
"""
from sqlalchemy.ext.asyncio import create_async_engine, AsyncSession, async_sessionmaker


@pytest.fixture(scope="session")
def engine():
    """Session 级别的测试引擎"""
    return create_async_engine("sqlite+aiosqlite:///:memory:", echo=False)


@pytest.fixture(scope="session")
async def create_tables(engine):
    """创建所有表"""
    async with engine.begin() as conn:
        await conn.run_sync(Base.metadata.create_all)


@pytest.fixture
async def db_session(engine, create_tables):
    """每个测试独立的数据库 Session"""
    session_factory = async_sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)

    async with session_factory() as session:
        async with session.begin():
            yield session
        # 测试结束后自动回滚


@pytest.mark.asyncio
async def test_create_conversation(db_session):
    """测试创建对话"""
    repo = ConversationRepository(db_session)

    conv = await repo.create(user_id=1, title="Test Chat", model="gpt-4o")
    assert conv.id is not None
    assert conv.title == "Test Chat"

    # 添加消息
    msg = await repo.add_message(conv.id, "user", "Hello", token_count=5)
    assert msg.content == "Hello"
```

---

## 4. Docker 容器化

### 4.1 多阶段 Dockerfile

```dockerfile
# ===== Stage 1: 构建 =====
FROM python:3.12-slim AS builder

WORKDIR /app
RUN pip install --no-cache-dir poetry

COPY pyproject.toml poetry.lock ./
RUN poetry config virtualenvs.create false \
    && poetry install --no-dev --no-interaction --no-ansi

# ===== Stage 2: 运行 =====
FROM python:3.12-slim AS runtime

WORKDIR /app

# 非 root 用户
RUN groupadd -r fastapi && useradd -r -g fastapi fastapi

# 复制依赖
COPY --from=builder /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages
COPY --from=builder /usr/local/bin /usr/local/bin

# 复制应用代码
COPY app/ ./app/
COPY alembic.ini .
COPY migrations/ ./migrations/

# 创建必要目录
RUN mkdir -p /app/logs /app/uploads && chown -R fastapi:fastapi /app

USER fastapi

# 健康检查
HEALTHCHECK --interval=30s --timeout=5s --retries=3 \
    CMD curl -f http://localhost:8000/health || exit 1

EXPOSE 8000
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 4.2 Docker Compose

```yaml
# docker-compose.yml
version: "3.8"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://user:pass@db:5432/aidb
      - REDIS_URL=redis://redis:6379
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - SECRET_KEY=${SECRET_KEY}
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    volumes:
      - ./uploads:/app/uploads
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: aidb
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d aidb"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Nginx 反向代理
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./certs:/etc/nginx/certs
    depends_on:
      - app

volumes:
  pgdata:
```

---

## 5. 生产部署配置

### 5.1 Gunicorn + Uvicorn Workers

```bash
# 生产启动命令
gunicorn app.main:app \
    --worker-class uvicorn.workers.UvicornWorker \
    --workers 4 \
    --bind 0.0.0.0:8000 \
    --timeout 120 \
    --graceful-timeout 30 \
    --keep-alive 5 \
    --max-requests 10000 \
    --max-requests-jitter 1000 \
    --access-logfile /var/log/fastapi/access.log \
    --error-logfile /var/log/fastapi/error.log \
    --log-level info

# Worker 数量建议：
# workers = (2 × CPU 核心数) + 1
# 或：workers = CPU 核心数（纯 IO 密集型 AI API）
```

### 5.2 中间件配置

```python
"""
生产环境中间件栈
"""
from fastapi import FastAPI
from fastapi.middleware.trustedhost import TrustedHostMiddleware
from fastapi.middleware.gzip import GZipMiddleware
from starlette.middleware.sessions import SessionMiddleware
import time


def setup_production_middleware(app: FastAPI):
    """生产环境中间件"""

    # ===== Trusted Host =====
    app.add_middleware(
        TrustedHostMiddleware,
        allowed_hosts=[
            "api.example.com",
            "*.example.com",
        ],
    )

    # ===== GZip 压缩 =====
    app.add_middleware(
        GZipMiddleware,
        minimum_size=1000,  # 1KB 以上才压缩
    )

    # ===== 请求 ID =====
    from uuid import uuid4

    @app.middleware("http")
    async def request_id_middleware(request, call_next):
        request_id = request.headers.get("X-Request-ID", str(uuid4()))
        request.state.request_id = request_id
        response = await call_next(request)
        response.headers["X-Request-ID"] = request_id
        return response

    # ===== 请求计时 =====
    @app.middleware("http")
    async def timing_middleware(request, call_next):
        start = time.perf_counter()
        response = await call_next(request)
        elapsed = time.perf_counter() - start
        response.headers["X-Response-Time"] = f"{elapsed:.4f}s"
        return response


# ===== 健康检查 =====
@app.get("/health")
async def health_check():
    """综合健康检查"""
    health = {
        "status": "healthy",
        "version": "1.0.0",
        "timestamp": datetime.now(timezone.utc).isoformat(),
        "checks": {},
    }

    # 数据库检查
    try:
        async with engine.connect() as conn:
            await conn.execute(text("SELECT 1"))
        health["checks"]["database"] = "ok"
    except Exception as e:
        health["checks"]["database"] = f"error: {e}"
        health["status"] = "degraded"

    # Redis 检查
    try:
        await redis_client.ping()
        health["checks"]["redis"] = "ok"
    except Exception as e:
        health["checks"]["redis"] = f"error: {e}"
        health["status"] = "degraded"

    # LLM API 连通性检查
    try:
        # 快速检查 OpenAI API 可达性
        health["checks"]["llm_api"] = "ok"
    except Exception:
        health["checks"]["llm_api"] = "unreachable"
        health["status"] = "degraded"

    return health


@app.get("/ready")
async def readiness_check():
    """就绪检查（K8s Readiness Probe）"""
    return {"status": "ready"}
```

> 🎯 **核心要点**：TestClient 做单元测试 + dependency_overrides 做 Mock + pytest-asyncio 做异步测试。部署用 Docker 多阶段构建 + Gunicorn Uvicorn Workers + Nginx 反向代理 + 健康检查端点

---

**上一模块**：[06 - 认证与安全](./06-认证与安全.md)  
**下一模块**：[08 - AI 应用实战](./08-FastAPI-AI应用实战.md)  
**返回总览**：[00 - FastAPI 知识体系总览](./00-FastAPI知识体系总览.md)
