# 04-分词器与输入构造
> 定位：分词器是模型与文本之间的唯一桥梁——token 化、消息构造、padding/truncation 三个动作做错任何一个，模型输出都是垃圾；chat template 是 2026 年不可跳过的规范。

## 📚 目录
1. [分词器的角色](#1-分词器的角色)
2. [三件套 API](#2-三件套-api)
3. [padding 与 truncation](#3-padding-与-truncation)
4. [chat template 消息构造](#4-chat-template-消息构造)
5. [特殊 token 治理](#5-特殊-token-治理)
6. [五个坑](#6-五个坑)
7. [练习](#7-练习)

## 1. 分词器的角色

模型只能吃整数（token id），文本必须经过分词器转换。分词器与模型**严格配对**：每个模型仓库自带 `tokenizer.json`（5.x 起统一格式，Rust 实现，加载快且跨平台），它包含词表、合并规则、特殊 token 定义、以及 chat template 模板。用错分词器（如拿 Llama 的分词器喂 Qwen 模型）词表对不上，输出全乱码——这是「模型输出乱码」的第一排查点。

5.x 的分词器结构：`AutoTokenizer` 统一入口，内部加载 tokenizer.json + 可选的附加词表；`tokenizer.json` 是唯一分词文件，不再有 fast/slow 之分（5.0 起取消）。

## 2. 三件套 API

```python
from transformers import AutoTokenizer
tok = AutoTokenizer.from_pretrained("Qwen/Qwen2.5-0.5B-Instruct")

tokens = tok.tokenize("hello world")          # ['hello', ' world'] 可视化预览
ids = tok.encode("hello world")               # [9906, 1917] token id 列表
inputs = tok("hello world", return_tensors="pt")   # 一键进模型：input_ids + attention_mask
text = tok.decode(ids)                        # 反向解码（用于输出）
```

三件套的职责：`tokenize` 看切分（调试分词是否合理）、`encode` 拿 id 列表（手动控制）、**直接调用 `tok(text, ...)` 进模型**（生产标准——自动返回 `input_ids` 与 `attention_mask` 的 BatchEncoding）。`return_tensors="pt"` 转 PyTorch 张量（"tf" 给 TensorFlow，"np" 给 numpy）。批量输入：`tok([text1, text2])` 自动 padding；显式批处理用 `tok.batch_encode_plus`。

## 3. padding 与 truncation

**truncation（截断）**：文本超长必须截，`truncation=True` + `max_length=2048`——按 token 数截断（不是字符数！）。不截断时超出位置模型无位置编码可用，报长度错误或输出退化。

**padding（补齐）**：批量推理时序列长度必须一致，短序列补 pad token 对齐。两个关键参数：

```python
inputs = tok(batch_texts,
             padding=True,            # 自动按 batch 最长补齐
             truncation=True, max_length=2048,
             return_tensors="pt")
```

**LLM 的 padding 必须左对齐**：`tok.padding_side = "left"`——LLM 生成时把输入拼在左侧，pad 放右侧会让生成的 token 位置错乱（模型按右侧继续生成，却读到左侧的 pad）。默认右对齐是为分类任务设计的，LLM 场景必须显式改左对齐——这是新手最容易漏的一行。

**attention_mask 的作用**：1 表示真实 token、0 表示 pad，模型注意力不计算 pad 位置。批量输入时 transformers 自动生成，但手动构造输入（如拼接 context）时容易漏——漏掉的后果是 pad 位置参与了注意力计算，结果偏差（06/08 篇 KV cache 场景尤其要注意）。

## 4. chat template 消息构造

2024 年起各模型要求严格的消息格式（`<|im_start|>system...`、`<|im_end|>` 这类特殊标记），**手写格式必错**——不同模型的标记不同。正确姿势是模型自带的 chat template：

```python
messages = [
    {"role": "system", "content": "你是助手，回答简洁。"},
    {"role": "user", "content": "什么是 RAG？"},
]
inputs = tok.apply_chat_template(
    messages,
    tokenize=True,          # True=直接出 token（推荐），False=出字符串
    add_generation_prompt=True,   # 末尾加助手起始标记，让模型开始回复
    return_tensors="pt",
)
```

三个要点：**消息结构**——system（可选，设全局行为）/user/assistant 交替，多轮对话把历史完整放入（模型无记忆，服务端无状态）；**`add_generation_prompt=True`**——不加则模型输出会重复一遍用户问题（它不知道该从哪开始）；**模板存在模型仓库里**——同一代码换模型自动适配，这就是「用模板不用手写」的根本理由。

## 5. 特殊 token 治理

**pad_token 缺失是经典事故**：很多开源模型（Llama 系、部分 Qwen）未定义 pad token，`padding=True` 时直接报错 `Tokenizer does not have a padding token`。标准修复：

```python
if tok.pad_token is None:
    tok.pad_token = tok.eos_token      # 用 eos 充当 pad（通用做法）
    tok.pad_token_id = tok.eos_token_id
```

**eos 与 bos**：生成会在遇到 eos token 时停止（配合 `skip_special_tokens=True` 解码时剔除特殊标记）。`decode(..., skip_special_tokens=True)` 是生成输出的标准解码姿势——不跳过会看到满屏 `<|im_end|>` 等标记。个别模型还要求 `tokenizer.add_special_tokens` 扩展词表（微调后场景，推理少见）。

**token 视角的预算与成本意识**：理解分词后，「token 数」这个概念就有了实体——它是模型的计价单位（API 场景按 token 收费）、显存的计算单位（KV cache 按 token 增长）、速度的衡量单位（TPS = token/秒）。几个必须刻进脑子的估算：**1 个中文字 ≈ 1-2 token，1 个英文单词 ≈ 1.3 token**；一段 1000 字的 prompt 约 1500-2000 token，一次 500 token 的回答约 0.3-0.6 秒（7B bf16 GPU）——预算、超时、成本估算全靠这个换算。**多轮对话的 token 经济**：每次请求带全量历史，10 轮对话后历史可能占 80% 的输入预算——「历史要精简、总结代替原文」是长对话的工程纪律（配合 `tokenizer.encode` 检查实际 token 数：`len(tok.encode(text))`，别凭感觉）。

**编码问题的正确姿势**：分词是字节级操作，输入文本的编码错误（GBK 字符串喂进来）会导致 token 乱码、输出崩坏。铁律两条：**所有输入文本统一 UTF-8**（文件读取带 `encoding="utf-8"`，API 层校验）；**decode 时用 `skip_special_tokens=True` 而非 `clean_up_tokenization_spaces=False` 等偏方**。中文场景的经典事故是「文件读进来是 GBK，模型输出乱码」——先查输入编码再查模型，90% 是前者。

## 6. 五个坑

**坑一：分词器与模型不配对**。乱码输出第一排查点——同仓库 ID 加载，绝不混用。

**坑二：LLM 批量推理忘左对齐**。右对齐 pad 导致生成 token 位置错乱——`tok.padding_side = "left"`。

**坑三：手写消息格式**。模型标记各不相同，手写格式必然在某模型上失败——永远 `apply_chat_template`。

**坑四：`max_length` 按字符数理解**。截断单位是 token——中文 1 字约 1 token，英文 1 词约 1.3 token，`max_length=2048` 对应约 2048 token。

**坑五：解码不带 `skip_special_tokens`**。输出混入特殊标记——解码参数与生成参数同样重要。

**坑六：多轮对话历史不裁剪**。对话轮数增长，输入无限膨胀直到撞 max_length——历史裁剪策略（截断最早的轮次、摘要压缩中间轮次）是长对话的必备设计，配合 token 计数监控。

**坑七：`encode` 与 `__call__` 混用歧义**。`tok(text)` 返回带 attention_mask 的结构、`tok.encode(text)` 只返回 id 列表——手动构造输入时两者别混着用，`return_tensors="pt"` 只对前者生效。

**token 与字符的换算纪律**：任何「长度」判断（截断、计费、超时预算）都以 token 为准，绝不按字符数拍脑袋——`len(tok.encode(text))` 是唯一标准。中文场景的两个高频误解：**1 个中文字不是 1 个字符 = 1 token**（常用字约 1 token，生僻字/专业词可能 2-4 token）；**`max_length` 的单位是 token 不是字符**（2048 上限写 2048 个「字」必然截断）。批量推理的截断检查：`tok(text, truncation=True, max_length=2048)` 后抽查 `inputs.input_ids.shape[-1]` 是否真到上限——截断发生时业务要感知（截断可能砍掉关键指令），别默默接受。

## 7. 练习

1. 为什么分词器必须与模型配对？
2. 左对齐 padding 对 LLM 生成的意义是什么？
3. `apply_chat_template` 的三个关键参数各干什么？
4. pad_token 缺失时标准修复是什么？
5. 多轮对话的 messages 结构怎么构造？

> 🎯 **核心要点**：分词器与模型严格配对；`tok(text, padding=True, truncation=True, max_length=...)` + LLM 左对齐是批量推理标准输入；聊天必须 `apply_chat_template(messages, add_generation_prompt=True)`；pad 缺失用 eos 补；解码带 `skip_special_tokens`。

---

**下一模块**：[05-生成参数与采样](05-生成参数与采样.md)｜**返回总览**：[00-transformers总览](00-transformers总览.md)
