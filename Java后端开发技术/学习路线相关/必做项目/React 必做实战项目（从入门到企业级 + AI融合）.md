# React 实战项目清单

## 一、核心能力

React 是现代前端开发主流框架，必须掌握：

- JSX 语法与组件化
- Hooks 与状态管理
- React Router 路由
- TypeScript 类型约束
- 性能优化
- 前后端联调
- AI 融合应用

---

## 二、技术能力拆解

- 基础层：JSX / 组件 / Props / State / 事件处理
- Hooks 层：useState / useEffect / useContext / 自定义 Hooks
- 路由层：React Router / 路由守卫 / 动态路由
- 状态层：Redux / Zustand / Context
- UI 层：Ant Design / Material-UI / Tailwind CSS
- 工程层：Vite / TypeScript / Axios / 请求拦截
- 优化层：虚拟列表 / 懒加载 / 防抖节流 / memo
- 全栈层：JWT / 跨域 / WebSocket / SSE

---

## 三、必做项目（求职优先级）

### 1. React TodoList 待办清单
**技术栈**：React + Vite

**实现**：
- 新增待办
- 删除待办
- 标记完成
- 状态筛选（全部 / 已完成 / 未完成）
- 本地存储持久化
- 批量删除

**核心知识点**：
- useState
- useEffect
- 组件拆分
- Props 传参
- localStorage

**产出**：
- React 基础组件与 Hooks 能力

**耗时**：1~2天

---

### 2. 计数器 + 暗黑模式
**技术栈**：React + Vite

**实现**：
- 数值增减
- 主题切换
- 主题持久化
- CSS 变量动态切换

**核心知识点**：
- 自定义 Hooks
- Context
- CSS 变量
- 状态共享

**产出**：
- 主题切换与自定义 Hooks 能力

**耗时**：1天

---

### 3. 购物车
**技术栈**：React + Vite

**实现**：
- 商品数量加减
- 全选 / 反选
- 总价计算
- 数量限制
- 删除商品

**核心知识点**：
- 父子组件通信
- 状态提升
- 复杂数据状态管理

**产出**：
- 组件通信与状态管理能力

**耗时**：2天

---

### 4. React 极简后台管理系统
**技术栈**：React + Vite + React Router + Zustand + Ant Design + Axios

**实现**：
- 登录鉴权
- 侧边栏导航
- 面包屑
- 表格 CRUD
- 分页
- 表单校验
- 权限控制

**核心知识点**：
- React Router
- Zustand 状态管理
- Ant Design 组件库
- Axios 请求封装
- 路由守卫

**产出**：
- 企业级后台基础能力

**耗时**：4~5天

---

### 5. React + TypeScript 企业级权限管理系统
**技术栈**：React + TypeScript + React Router + Zustand + Ant Design

**实现**：
- 动态路由
- RBAC 权限模型
- 按钮级权限
- 菜单管理
- 角色分配
- 用户管理
- 权限指令

**核心知识点**：
- TypeScript 类型约束
- 动态路由
- 权限控制
- 接口类型定义

**产出**：
- 企业级权限系统能力

**耗时**：5~7天

---

### 6. AI 对话聊天 Web 端
**技术栈**：React + TypeScript + SSE / WebSocket

**实现**：
- 对话气泡
- 打字动画
- 历史会话
- Markdown 代码高亮
- 流式输出
- 自动滚动
- 对话上下文管理

**核心知识点**：
- SSE 流式响应
- WebSocket 实时通信
- Markdown 渲染
- 自定义滚动
- 对接 Spring AI 后端

**产出**：
- AI 对话前端能力

**耗时**：4~5天

---

## 四、进阶项目（提升上限）

### 7. 个人博客前台
**技术栈**：React + React Router + Markdown

**实现**：
- 文章列表
- 文章详情
- 分类 / 标签
- 搜索
- 评论
- 暗黑模式

**产出**：
- 博客前端能力

---

### 8. 音乐播放器
**技术栈**：React + Audio API

**实现**：
- 播放 / 暂停
- 切歌
- 进度条拖拽
- 音量调节
- 歌词同步

**产出**：
- 媒体控制与自定义组件能力

---

### 9. 数据可视化大屏
**技术栈**：React + ECharts / Recharts

**实现**：
- 多维度图表
- 实时数据刷新
- 自适应缩放
- 主题切换

**产出**：
- 数据可视化能力

---

### 10. 仿抖音短视频页面
**技术栈**：React + IntersectionObserver

**实现**：
- 无限滚动
- 视频懒加载
- 点赞 / 评论
- 下拉刷新

**产出**：
- 性能优化与虚拟列表能力

---

### 11. 前后端分离电商系统
**技术栈**：React + SpringBoot + JWT + Redis

**实现**：
- 首页
- 商品详情
- 购物车
- 订单结算
- 地址管理
- 支付页

**产出**：
- 全栈联调能力

---

### 12. 在线考试系统前端
**技术栈**：React + WebSocket + Java 后端

**实现**：
- 登录
- 题库展示
- 在线答题
- 自动交卷
- 成绩统计

**产出**：
- WebSocket 实时能力

---

### 13. RAG 知识库问答系统前端
**技术栈**：React + TypeScript + SpringBoot

**实现**：
- 文档上传
- 知识库管理
- 问答交互
- 引用溯源
- 文件预览

**产出**：
- RAG 系统前端能力

---

### 14. AI 绘图生成页面
**技术栈**：React + TypeScript

**实现**：
- 提示词输入
- 风格选择
- 图片生成
- 历史记录
- 图片下载

**产出**：
- AI 应用前端能力

---

## 五、必练核心知识点

### 1. 基础 Hooks
```jsx
import { useState, useEffect } from 'react';

const [count, setCount] = useState(0);

useEffect(() => {
  // 副作用
}, [count]);
```

### 2. 自定义 Hooks
```jsx
function useLocalStorage(key, initialValue) {
  const [value, setValue] = useState(() => {
    return localStorage.getItem(key) || initialValue;
  });
  
  useEffect(() => {
    localStorage.setItem(key, value);
  }, [key, value]);
  
  return [value, setValue];
}
```

### 3. React Router
```jsx
import { BrowserRouter, Routes, Route } from 'react-router-dom';

<BrowserRouter>
  <Routes>
    <Route path="/" element={<Home />} />
    <Route path="/about" element={<About />} />
  </Routes>
</BrowserRouter>
```

### 4. Zustand
```jsx
import create from 'zustand';

const useStore = create((set) => ({
  count: 0,
  increment: () => set((state) => ({ count: state.count + 1 })),
}));
```

### 5. Axios 封装
```jsx
import axios from 'axios';

const request = axios.create({
  baseURL: '/api',
  timeout: 5000,
});

request.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});
```

---

## 六、项目架构标准

必须覆盖：

- 组件化拆分
- Hooks 合理使用
- 状态管理规范
- 路由设计
- 请求封装
- 错误处理
- TypeScript 类型约束
- 代码注释

---

## 七、简历表达（核心关键词）

- React Hooks 组件化开发
- React Router 路由管理
- Zustand / Redux 状态管理
- TypeScript 类型约束
- Ant Design / Tailwind CSS UI 库
- Axios 请求封装与拦截
- 前后端分离与 JWT 鉴权
- SSE / WebSocket 实时通信
- AI 对话前端与 RAG 系统前端

---

## 八、技术栈推荐

- 构建工具：Vite
- 语言：TypeScript
- 路由：React Router
- 状态管理：Zustand / Redux Toolkit
- UI 库：Ant Design / Material-UI / Tailwind CSS
- 请求库：Axios
- 可视化：ECharts / Recharts
- Markdown：react-markdown
- 代码高亮：highlight.js

---

## 九、React vs Vue 选型建议

| 特性 | React | Vue |
|------|-------|-----|
| 学习曲线 | 陡峭 | 平缓 |
| 灵活性 | 高 | 中 |
| 生态 | 丰富（AI / 可视化） | 成熟（企业级） |
| TypeScript | 强 | 强 |
| 适用场景 | AI 应用 / 大型项目 | 企业后台 / 快速开发 |

**建议**：
- 两条线都掌握
- 以 React 作为全栈 + AI 的主力前端技术栈
- Vue 用于快速做业务项目

---

## 十、你的优先级

结合你的 **Java 后端 + AI 大模型应用** 路线，最值得优先完成的是：

1. React TodoList 待办清单
2. 购物车
3. React 极简后台管理系统
4. React + TypeScript 企业级权限管理系统
5. AI 对话聊天 Web 端
6. RAG 知识库问答系统前端

---

## 十一、项目成果要求

- GitHub 完整代码（结构清晰）
- README 写明：项目背景、技术栈、功能模块、启动方式
- 至少 1 个可运行 Demo
- 代码规范：组件拆分、Hooks 使用、TypeScript 类型约束
- 能清晰展示前后端联调能力
- 项目亮点突出 AI 融合与工程化能力

---