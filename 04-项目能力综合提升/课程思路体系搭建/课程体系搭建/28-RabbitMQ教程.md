# RabbitMQ教程

> 从基础到高级，系统学习 RabbitMQ 消息队列的核心概念、Java客户端操作、可靠性机制与延迟消息

### MQ基础

- `001` RabbitMQ课程介绍
- `002` 初识MQ-同步调用
- `003` 初识MQ-异步调用
- `004` 初识MQ-技术选型
- `005` RabbitMQ-认识和安装
- `006` RabbitMQ-快速入门
- `007` RabbitMQ-数据隔离
- `008` Java客户端-快速入门
- `009` Java客户端-work模型
- `010` Java客户端-Fanout交换机
- `011` Java客户端-Direct交换机
- `012` Java客户端-Topic交换机
- `013` Java客户端-声明队列和交换机的方式一
- `014` Java客户端-声明队列和交换机的方式二
- `015` Java客户端-消息转换器
- `016` 改造业务代码

### MQ高级

- `017` 今日课程介绍
- `018` 生产者可靠性-生产者重连
- `019` 生产者可靠性-生产者确认原理
- `020` 生产者可靠性-生产者确认代码实现
- `021` MQ的可靠性-数据持久化
- `022` MQ的可靠性-LazyQueue
- `023` 消费者可靠性-消费者确认
- `024` 消费者可靠性-失败重试机制
- `025` 消费者可靠性-业务幂等性
- `026` 延迟消息-什么是延迟消息
- `027` 延迟消息-死信交换机
- `028` 延迟消息-延迟消息插件
- `029` 延迟消息-取消超时订单的基本思路
- `030` 延迟消息-发送延迟检测订单的消息
- `031` 延迟消息-监听延迟消息
