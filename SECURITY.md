# 安全策略（SECURITY）

> 本仓库为**技术文档知识库**（5,752 篇 Markdown），不含可运行代码，安全风险面较小；但仍遵守开源安全最佳实践。

---

## 支持的版本

| 版本 | 支持状态 |
|------|:---:|
| main（最新） | ✅ 持续维护 |
| v1.0.0（2026-08-12 收官版） | ✅ 里程碑快照 |

> 文档仓库无 LTS 概念：以 `main` 为唯一事实来源，版本 tag 仅作里程碑快照。

---

## 报告漏洞

发现以下类型的问题，请通过 **GitHub Security Advisory（私有披露）** 或 **Issue（公开）** 报告：

| 类型 | 示例 |
|------|------|
| **敏感信息泄露** | 文档中出现的密钥、密码、真实证件信息（含历史提交中的泄露） |
| **恶意内容** | 注入的恶意链接、误导性安装命令、钓鱼内容 |
| **链接安全** | 指向恶意域名/已失效域名劫持的外链 |
| **过时高危信息** | 已公开 CVE 的软件版本被推荐为"安全" |

**报告方式**：

```text
GitHub → 本仓库 → Security → Report a vulnerability（推荐，私有）
或 GitHub → Issues → New issue（标记 [security]）
```

**响应承诺**：

```text
┌────────────────────────────────────────────┐
│ 确认收到：48 小时内                          │
│ 处理中：修复计划 7 天内给出                   │
│ 修复发布：确认后 14 天内合入 main             │
└────────────────────────────────────────────┘
```

---

## 安全注意事项（贡献者必读）

- **禁止提交**：密码、API Key、私钥（`*.pem`/`*.key`）、`.env` 文件（.gitignore 已防护，提交前自查）
- **密钥泄露处置**：先吊销并轮换密钥，再重写 Git 历史；仅删除当前文件中的密钥，旧提交仍可恢复
- **自动扫描**：`Secret Scan` 工作流对 push、PR 和每周定时任务执行完整 Git 历史扫描
- **禁止外链劫持风险**：引用外链时确认域名归属，倾向引用官方文档/官方 GitHub
- **历史泄露**：若发现历史提交中已有敏感信息，报告后评估历史重写（filter-repo）方案
- **依赖安全**：本仓库无运行时依赖；CI 工具链（markdownlint 等）随 GitHub Actions 自动更新，不构成供应链风险

---

## 安全清单（自检）

```text
[ ] 文档中无明文密钥/密码？
[ ] 外链均指向可信域名？
[ ] 推荐的工具版本未被公开 CVE 影响（2026 检索基准）？
[ ] 无个人隐私信息（真实身份证号/手机号/家庭住址）？
```

---

**报告入口**：[GitHub Security Advisory](https://github.com/suxiangyu138/Java-AI-LLM-Learning-Records/security/advisories) | [Issues](https://github.com/suxiangyu138/Java-AI-LLM-Learning-Records/issues)
