# 多模态大模型核心知识点

> **核心认知**：多模态大模型不仅能处理文本，还能理解图像、生成图像、处理音视频。对于 Java 后端开发者，重点掌握**图像理解 API 调用**和**文生图**即可覆盖 80% 的应用场景。
> **难度等级**：理解为主、调 API 为辅，不做模型训练

---

## 1. 多模态大模型全景

```
                    多模态 AI 能力矩阵

    ┌──────────┬──────────────┬──────────────────┐
    │   模态    │   理解（输入） │   生成（输出）      │
    ├──────────┼──────────────┼──────────────────┤
    │  文本     │  GPT-4       │  GPT-4 / Claude  │
    │  图像     │  GPT-4V      │  DALL-E / SD     │
    │  音频     │  Whisper     │  ElevenLabs      │
    │  视频     │  Gemini Pro  │  Sora (OpenAI)   │
    │  代码     │  Copilot     │  Copilot / Cursor│
    └──────────┴──────────────┴──────────────────┘
```

---

## 2. 图像理解（GPT-4V / Qwen-VL）

### 2.1 Qwen-VL（推荐：免费 + 中文强）

```python
from openai import OpenAI

client = OpenAI(
    api_key="sk-your-key",
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
)

# 图像分析
response = client.chat.completions.create(
    model="qwen-vl-max",
    messages=[
        {
            "role": "user",
            "content": [
                {"type": "text", "text": "这张图片里有什么？请详细描述。"},
                {"type": "image_url", "image_url": {"url": "https://example.com/photo.jpg"}}
            ]
        }
    ]
)
print(response.choices[0].message.content)
```

### 2.2 本地图片转 Base64

```python
import base64
from pathlib import Path


def image_to_base64(image_path: str) -> str:
    """将本地图片转为 Base64（API 需要）"""
    with open(image_path, "rb") as f:
        return base64.b64encode(f.read()).decode("utf-8")


def analyze_image_local(image_path: str, question: str) -> str:
    """分析本地图片"""
    b64 = image_to_base64(image_path)
    ext = Path(image_path).suffix.lower().replace(".", "")

    response = client.chat.completions.create(
        model="qwen-vl-max",
        messages=[{
            "role": "user",
            "content": [
                {"type": "text", "text": question},
                {"type": "image_url", "image_url": {"url": f"data:image/{ext};base64,{b64}"}}
            ]
        }]
    )
    return response.choices[0].message.content


# 使用场景
# 1. UI 截图 → 生成前端代码
# 2. 架构图 → 生成文字描述
# 3. 错误截图 → 自动定位问题
# 4. 证件照 → 信息提取
```

### 2.3 OCR 文字识别

```python
def extract_text_from_image(image_path: str) -> str:
    """从图片中提取文字"""
    return analyze_image_local(
        image_path,
        "请识别并提取图片中所有的文字内容，保持原有的排版格式。"
    )


def extract_table_from_image(image_path: str) -> str:
    """从图片中识别表格"""
    return analyze_image_local(
        image_path,
        "将图片中的表格提取为 Markdown 格式，保持表头和数据完整性。"
    )
```

---

## 3. 文生图（Stable Diffusion / DALL-E）

### 3.1 DALL-E 3 API 调用

```python
# 使用 DALL-E 3 生成图片
response = client.images.generate(
    model="dall-e-3",
    prompt="一个Java开发者坐在电脑前，屏幕上有Spring Boot代码和AI对话界面，数字绘画风格",
    size="1024x1024",
    quality="standard",
    n=1
)
image_url = response.data[0].url
# 下载图片
import requests
img_data = requests.get(image_url).content
with open("generated.png", "wb") as f:
    f.write(img_data)
```

### 3.2 Stable Diffusion 本地部署

```python
from diffusers import StableDiffusionPipeline
import torch

# 加载模型（首次会下载 ~4GB）
pipe = StableDiffusionPipeline.from_pretrained(
    "runwayml/stable-diffusion-v1-5",
    torch_dtype=torch.float16,
    safety_checker=None  # 开发调试时关闭安全检测加速
)
pipe = pipe.to("cuda")  # 移到 GPU


def generate_image(
    prompt: str,
    negative_prompt: str = "blurry, low quality, distorted",
    width: int = 512,
    height: int = 512,
    num_inference_steps: int = 25,
    seed: int = 42
):
    """生成图片"""
    generator = torch.Generator("cuda").manual_seed(seed)
    image = pipe(
        prompt=prompt,
        negative_prompt=negative_prompt,
        width=width,
        height=height,
        num_inference_steps=num_inference_steps,
        generator=generator
    ).images[0]
    return image


# 使用
img = generate_image(
    prompt="A modern tech office with clean design, natural lighting, 8k detailed",
    negative_prompt="people, text, watermark"
)
img.save("office.png")
```

### 3.3 Prompt 工程（文生图）

```python
# 文生图的 Prompt 公式
PROMPT_TEMPLATE = """
{subject}, {style}, {lighting}, {composition}, {quality_tags}

示例：
主词: A Java developer coding at desk
风格: digital illustration, flat design
光照: soft studio lighting
构图: medium shot, centered
质量: 8k, highly detailed, professional
负面: blurry, distorted face, bad anatomy
"""

# 实战场景
prompts = {
    "技术博客封面": "Modern software architecture diagram, blueprint style, dark blue background with glowing orange data flows, clean lines, tech aesthetic, 16:9 aspect ratio",
    "PPT配图": "Simple flat illustration of cloud computing, minimalist style, pastel colors, white background, corporate friendly",
    "产品Logo": "Abstract geometric shape representing AI and data, gradient blue to purple, minimal, vector style, on white background",
}
```

---

## 4. 多模态应用架构

### 4.1 图片理解 + 文本生成链路

```python
class MultimodalPipeline:
    """多模态处理流水线"""

    def __init__(self, vision_model: str = "qwen-vl-max", text_model: str = "deepseek-chat"):
        self.vision_client = OpenAI(base_url="https://dashscope.aliyuncs.com/compatible-mode/v1")
        self.text_client = OpenAI(base_url="https://api.deepseek.com/v1")
        self.vision_model = vision_model
        self.text_model = text_model

    def analyze_and_generate(self, image_path: str, task: str) -> dict:
        """图片分析 + 内容生成"""
        # 第1步：视觉模型理解图片
        description = analyze_image_local(image_path, "请详细描述这张图片的内容。")
        print(f"[视觉阶段] {description[:100]}...")

        # 第2步：文本模型基于理解做进一步处理
        prompt = f"""根据以下图片描述，{task}

图片描述：
{description}"""

        result = self.text_client.chat.completions.create(
            model=self.text_model,
            messages=[{"role": "user", "content": prompt}],
            temperature=0.7
        )
        return {
            "description": description,
            "result": result.choices[0].message.content
        }


# 使用场景：UI 设计稿 → 前端代码
pipeline = MultimodalPipeline()
result = pipeline.analyze_and_generate(
    "ui-mockup.png",
    "根据这个UI界面的描述，生成对应的 HTML + Tailwind CSS 代码。"
)
```

### 4.2 服务化封装

```python
# 统一的多模态服务接口
from abc import ABC, abstractmethod


class MultimodalService(ABC):
    @abstractmethod
    def image_to_text(self, image: bytes, prompt: str) -> str: ...
    @abstractmethod
    def text_to_image(self, prompt: str) -> bytes: ...


class QwenVLService(MultimodalService):
    def image_to_text(self, image: bytes, prompt: str) -> str:
        b64 = base64.b64encode(image).decode()
        response = self.client.chat.completions.create(
            model="qwen-vl-max",
            messages=[{
                "role": "user",
                "content": [
                    {"type": "text", "text": prompt},
                    {"type": "image_url", "image_url": {"url": f"data:image/png;base64,{b64}"}}
                ]
            }]
        )
        return response.choices[0].message.content

    def text_to_image(self, prompt: str) -> bytes:
        # 调用文生图服务
        response = self.client.images.generate(
            model="dall-e-3",
            prompt=prompt,
            size="1024x1024",
            n=1
        )
        return requests.get(response.data[0].url).content
```

---

## 5. 选型指南

| 场景 | 推荐方案 | 原因 |
|------|----------|------|
| 中文图片理解 | Qwen-VL-Max | 中文最强、有免费额度 |
| 英文图片理解 | GPT-4V / Claude 3.5 | 综合能力最强 |
| 文生图（快速） | DALL-E 3 API | 质量好、调用简单 |
| 文生图（免费） | Stable Diffusion 本地 | 免费、可精调 |
| OCR 中文 | Qwen-VL / 百度 OCR | 中文 OCR 准确率高 |
| 视频理解 | Gemini Pro Vision | 原生支持视频帧分析 |

---

## 6. 实战项目：简历优化器

```python
class ResumeOptimizer:
    """多模态简历优化器"""

    def __init__(self):
        self.vision = OpenAI(base_url="...")  # Qwen-VL
        self.writer = OpenAI(base_url="...")  # DeepSeek

    def optimize(self, resume_image_path: str, target_job: str) -> dict:
        """输入简历截图，输出优化后的简历"""
        # 1. OCR 提取简历文字
        resume_text = extract_text_from_image(resume_image_path)

        # 2. AI 分析简历问题
        analysis = self._analyze_resume(resume_text, target_job)

        # 3. AI 优化重写
        optimized = self._rewrite_resume(resume_text, analysis, target_job)

        # 4. 生成 Markdown 和 PDF
        md = self._format_to_markdown(optimized)

        return {
            "original_text": resume_text,
            "analysis": analysis,
            "optimized_text": optimized,
            "markdown": md
        }

    def _analyze_resume(self, resume: str, job: str) -> str:
        prompt = f"""分析以下简历，找出与目标岗位 '{job}' 的差距：
简历：{resume}
请指出：1. 缺少的技能 2. 表达不清晰的项目描述 3. 格式问题 4. 改进建议"""
        return self.writer.chat(prompt)

    def _rewrite_resume(self, resume: str, analysis: str, job: str) -> str:
        prompt = f"""根据分析结果重写简历，面向岗位：{job}
原简历：{resume}
分析：{analysis}
要求：1. 用STAR法则描述项目 2. 突出量化成果 3. 匹配岗位关键词"""
        return self.writer.chat(prompt)

    def _format_to_markdown(self, text: str) -> str:
        prompt = f"将以下简历内容格式化为美观的 Markdown：\n{text}"
        return self.writer.chat(prompt)
```

---

## 快速调试检查清单

- [ ] 图片 Base64 编码是否正确？（检查 `data:image/{ext};base64,{b64}` 格式）
- [ ] Vision API 是否支持该图片格式？（常见：PNG、JPEG、WebP）
- [ ] 图片大小是否超过 API 限制？（一般 < 20MB）
- [ ] Stable Diffusion 首次加载是否下载了足够空间？（模型文件 ~4GB）
- [ ] GPU 显存是否足够运行 SD？（512x512 需要 ~4GB）
